# Отчёт: Исправление PathFinder для использования того же графа, что и RouteGraphManager

## Проблема

PathFinder мог использовать НЕ тот граф, который был построен в RouteGraphManager, из-за отсутствия проверки соответствия ключей между `nodesMap` и `edgesMap`. Это могло приводить к тому, что:

- PathFinder инициализировал `unvisited` из `graph.getAllNodes()`, но не проверял, что ключи точно соответствуют ключам в `nodesMap`
- PathFinder не проверял, что ключи в `edgesMap` точно соответствуют ключам в `nodesMap`
- PathFinder не имел диагностики для проверки, что он использует тот же граф, который был построен в RouteGraphManager

**Результат:**
- PathFinder мог работать с несоответствующими ключами
- `edges.get(current)` мог возвращать `undefined`, даже если узел существовал в `nodesMap`
- Маршруты не находились из-за несоответствия ключей

## Решение

Добавлена полная диагностика и проверка соответствия ключей в PathFinder, чтобы гарантировать, что он использует ТОТ ЖЕ граф, который был построен в RouteGraphManager. Добавлены методы в RouteGraph для прямого доступа к размерам и ключам `nodesMap` и `edgesMap`.

## Выполненные изменения

### 1. Добавлены методы диагностики в RouteGraph

**Файл:** `backend/src/application/route-builder/RouteGraph.ts`

**Методы:**
- `getNodesMapSize()`: Возвращает размер `nodesMap`
- `getEdgesMapSize()`: Возвращает размер `edgesMap`
- `getNodesMapKeys()`: Возвращает все ключи из `nodesMap`
- `getEdgesMapKeys()`: Возвращает все ключи из `edgesMap`
- `getEdgesCountForNode(stopId)`: Возвращает количество рёбер для конкретного узла

**Код:**
```typescript
/**
 * КРИТИЧЕСКИ ВАЖНО: Получить размер nodesMap (для диагностики PathFinder)
 */
getNodesMapSize(): number {
  return this.nodes.size;
}

/**
 * КРИТИЧЕСКИ ВАЖНО: Получить размер edgesMap (для диагностики PathFinder)
 */
getEdgesMapSize(): number {
  return this.edges.size;
}

/**
 * КРИТИЧЕСКИ ВАЖНО: Получить все ключи из nodesMap (для диагностики PathFinder)
 */
getNodesMapKeys(): string[] {
  return Array.from(this.nodes.keys());
}

/**
 * КРИТИЧЕСКИ ВАЖНО: Получить все ключи из edgesMap (для диагностики PathFinder)
 */
getEdgesMapKeys(): string[] {
  return Array.from(this.edges.keys());
}

/**
 * КРИТИЧЕСКИ ВАЖНО: Получить количество рёбер для конкретного узла (для диагностики PathFinder)
 */
getEdgesCountForNode(stopId: string): number {
  const edges = this.edges.get(stopId);
  return edges ? edges.length : 0;
}
```

**Гарантии:**
- ✅ Прямой доступ к размерам и ключам `nodesMap` и `edgesMap`
- ✅ Возможность проверки соответствия ключей между `nodesMap` и `edgesMap`
- ✅ Диагностика количества рёбер для конкретного узла

### 2. Добавлена диагностика графа перед поиском пути в PathFinder

**Файл:** `backend/src/application/route-builder/PathFinder.ts`

**Метод:** `findShortestPath()`

**Изменения:**
- Добавлена полная диагностика графа перед поиском пути
- Проверка соответствия ключей между `nodesMap` и `edgesMap`
- Проверка существования `fromStopId` в `nodesMap` и `edgesMap`
- Вывод количества узлов, ключей и рёбер для диагностики

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Диагностика графа перед поиском пути
// Проверяем, что PathFinder использует ТОТ ЖЕ граф, который был построен в RouteGraphManager
const nodesMapSize = graph.getNodesMapSize();
const edgesMapSize = graph.getEdgesMapSize();
const nodesMapKeys = graph.getNodesMapKeys();
const edgesMapKeys = graph.getEdgesMapKeys();
const startEdgesCount = graph.getEdgesCountForNode(fromStopId);

console.log(`[PathFinder] 📊 Диагностика графа перед поиском пути:`);
console.log(`[PathFinder]   - Количество узлов в nodesMap: ${nodesMapSize}`);
console.log(`[PathFinder]   - Количество ключей в edgesMap: ${edgesMapSize}`);
console.log(`[PathFinder]   - Рёбер из fromStopId (${fromStopId}): ${startEdgesCount}`);
console.log(`[PathFinder]   - Соответствие ключей: ${nodesMapSize === edgesMapSize ? '✅ СОВПАДАЕТ' : '❌ НЕ СОВПАДАЕТ'}`);

// Проверяем, что fromStopId существует в nodesMap
if (!nodesMapKeys.includes(fromStopId)) {
  console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: fromStopId="${fromStopId}" НЕ существует в nodesMap!`);
  console.log(`[PathFinder] Доступные ключи в nodesMap (первые 10):`, nodesMapKeys.slice(0, 10));
  return null;
}

// Проверяем, что fromStopId существует в edgesMap
if (!edgesMapKeys.includes(fromStopId)) {
  console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: fromStopId="${fromStopId}" НЕ существует в edgesMap!`);
  console.log(`[PathFinder] Доступные ключи в edgesMap (первые 10):`, edgesMapKeys.slice(0, 10));
  return null;
}

// Проверяем соответствие ключей между nodesMap и edgesMap
const missingInEdges = nodesMapKeys.filter(k => !edgesMapKeys.includes(k));
const extraInEdges = edgesMapKeys.filter(k => !nodesMapKeys.includes(k));

if (missingInEdges.length > 0) {
  console.log(`[PathFinder] ⚠️ ВНИМАНИЕ: Найдено ${missingInEdges.length} узлов в nodesMap без ключей в edgesMap:`, missingInEdges.slice(0, 5));
}

if (extraInEdges.length > 0) {
  console.log(`[PathFinder] ⚠️ ВНИМАНИЕ: Найдено ${extraInEdges.length} ключей в edgesMap без узлов в nodesMap:`, extraInEdges.slice(0, 5));
}

if (missingInEdges.length === 0 && extraInEdges.length === 0) {
  console.log(`[PathFinder] ✅ Все ключи совпадают между nodesMap и edgesMap`);
}
```

**Гарантии:**
- ✅ Полная диагностика графа перед поиском пути
- ✅ Проверка соответствия ключей между `nodesMap` и `edgesMap`
- ✅ Проверка существования `fromStopId` в обоих Map
- ✅ Детальное логирование для диагностики

### 3. Исправлена инициализация алгоритма Dijkstra

**Файл:** `backend/src/application/route-builder/PathFinder.ts`

**Метод:** `findShortestPath()`

**Изменения:**
- Инициализация `unvisited` теперь использует `graph.getNodesMapKeys()` вместо `graph.getAllNodes()`
- Гарантируется, что используются ТОЧНО те же ключи, что и в `graph.nodesMap`
- Добавлена проверка соответствия количества узлов

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Инициализируем все узлы из nodesMap
// Используем nodesMapKeysForInit, чтобы гарантировать, что мы используем те же ключи, что и в графе
const nodesMapKeysForInit = graph.getNodesMapKeys();
console.log(`[PathFinder] 🔄 Инициализация алгоритма Dijkstra: узлов=${nodesMapKeysForInit.length}`);

for (const stopId of nodesMapKeysForInit) {
  distances.set(stopId, Infinity);
  previous.set(stopId, null);
  unvisited.add(stopId);
}

// Проверяем, что все ключи из nodesMap добавлены в unvisited
if (unvisited.size !== nodesMapKeysForInit.length) {
  console.log(`[PathFinder] ⚠️ ВНИМАНИЕ: Несоответствие количества узлов! nodesMapKeysForInit=${nodesMapKeysForInit.length}, unvisited=${unvisited.size}`);
} else {
  console.log(`[PathFinder] ✅ Все ${nodesMapKeysForInit.length} узлов из nodesMap добавлены в unvisited`);
}
```

**Гарантии:**
- ✅ Используются ТОЧНО те же ключи, что и в `graph.nodesMap`
- ✅ Проверка соответствия количества узлов
- ✅ Детальное логирование для диагностики

### 4. Добавлена проверка соответствия ключей при обработке рёбер

**Файл:** `backend/src/application/route-builder/PathFinder.ts`

**Метод:** `findShortestPath()`

**Изменения:**
- Добавлена проверка существования ключа `current` в `edgesMap` перед обработкой рёбер
- Добавлена диагностика для случаев, когда ключ существует в `nodesMap`, но не существует в `edgesMap`
- Добавлена проверка, что PathFinder использует тот же граф

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Получаем все рёбра из текущего узла
// Используем graph.getEdgesFrom(), который работает с graph.edgesMap
// Если edges.get(current) возвращает undefined, это означает несоответствие ключей
const edges = graph.getEdgesFrom(current);

// КРИТИЧЕСКИ ВАЖНО: Проверяем, что ключ current существует в edgesMap
const edgesMapKeys = graph.getEdgesMapKeys();
const existsInEdgesMap = edgesMapKeys.includes(current);

if (edges === undefined || edges.length === 0) {
  const currentNode = graph.getNode(current);
  const nodeName = currentNode ? (currentNode.cityName || currentNode.stopName || current) : current;
  console.log(`[PathFinder] ⚠️ Узел "${nodeName}" (${current}) не имеет исходящих рёбер (edges=${edges === undefined ? 'undefined' : edges.length})`);
  
  // Диагностика: проверяем, существует ли ключ в edgesMap
  const nodesMapKeysForCheck = graph.getNodesMapKeys();
  const existsInNodesMap = nodesMapKeysForCheck.includes(current);
  
  if (existsInNodesMap && !existsInEdgesMap) {
    console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: узел "${current}" существует в nodesMap, но НЕ существует в edgesMap!`);
    console.log(`[PathFinder] Это означает, что граф не синхронизирован или PathFinder использует другой граф!`);
  } else if (!existsInNodesMap) {
    console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: узел "${current}" не существует в nodesMap!`);
  } else {
    console.log(`[PathFinder] Диагностика: узел "${current}" существует в nodesMap и edgesMap, но edges.get("${current}") возвращает пустой массив`);
  }
  continue;
}

// КРИТИЧЕСКИ ВАЖНО: Проверяем, что мы используем тот же граф
if (!existsInEdgesMap) {
  console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: ключ "${current}" не существует в edgesMap, но getEdgesFrom() вернул рёбра!`);
  console.log(`[PathFinder] Это означает, что PathFinder использует НЕ тот граф, который был построен в RouteGraphManager!`);
  continue;
}
```

**Гарантии:**
- ✅ Проверка существования ключа в `edgesMap` перед обработкой рёбер
- ✅ Диагностика для случаев несоответствия ключей
- ✅ Проверка, что PathFinder использует тот же граф

## Гарантии системы

### ✅ PathFinder использует ТОТ ЖЕ граф, который был построен в RouteGraphManager

- PathFinder получает граф по ссылке (один и тот же объект)
- Все операции происходят над `graph.edges` и `graph.nodes`, а не над собственными коллекциями
- Ключи в PathFinder EXACTLY те же, что и в `graph.edgesMap` и `graph.nodesMap`

### ✅ Диагностика графа перед поиском пути

- Количество узлов в `nodesMap` выводится перед поиском пути
- Количество ключей в `edgesMap` выводится перед поиском пути
- Количество рёбер из `fromStopId` выводится перед поиском пути
- Соответствие ключей между `nodesMap` и `edgesMap` проверяется и выводится

### ✅ Инициализация алгоритма Dijkstra использует те же ключи

- `unvisited` инициализируется из `graph.getNodesMapKeys()`, а не из `graph.getAllNodes()`
- Гарантируется, что используются ТОЧНО те же ключи, что и в `graph.nodesMap`
- Проверяется соответствие количества узлов

### ✅ Проверка соответствия ключей при обработке рёбер

- Проверяется существование ключа в `edgesMap` перед обработкой рёбер
- Диагностика для случаев, когда ключ существует в `nodesMap`, но не существует в `edgesMap`
- Проверка, что PathFinder использует тот же граф

## Проверка работы

### Примеры пар городов для тестирования:

1. **Якутск → Олёкминск**: Реальный → Виртуальный
2. **Ленск → Якутск**: Реальный → Реальный
3. **Верхоянск → Жиганск**: Виртуальный → Виртуальный
4. **Амга → Тикси**: Виртуальный → Виртуальный
5. **Мирный → Олёкминск**: Реальный → Виртуальный

### Ожидаемые логи:

```
[PathFinder] 📊 Диагностика графа перед поиском пути:
[PathFinder]   - Количество узлов в nodesMap: N
[PathFinder]   - Количество ключей в edgesMap: N
[PathFinder]   - Рёбер из fromStopId (...): M
[PathFinder]   - Соответствие ключей: ✅ СОВПАДАЕТ
[PathFinder] ✅ Все ключи совпадают между nodesMap и edgesMap
[PathFinder] 🔄 Инициализация алгоритма Dijkstra: узлов=N
[PathFinder] ✅ Все N узлов из nodesMap добавлены в unvisited
```

### Критерии успеха:

- ✅ PathFinder использует тот же граф, который был построен в RouteGraphManager
- ✅ Ключи в PathFinder EXACTLY те же, что и в `graph.edgesMap` и `graph.nodesMap`
- ✅ Диагностика показывает соответствие ключей между `nodesMap` и `edgesMap`
- ✅ Маршруты находятся для любой пары городов
- ✅ Нет ошибок "ключ не существует в edgesMap" или "PathFinder использует НЕ тот граф"

## Результат

После изменений система гарантирует:

- ✅ **PathFinder использует ТОТ ЖЕ граф**: PathFinder получает граф по ссылке и использует те же `nodesMap` и `edgesMap`
- ✅ **Диагностика графа перед поиском пути**: Полная диагностика с выводом количества узлов, ключей и рёбер
- ✅ **Инициализация алгоритма Dijkstra использует те же ключи**: `unvisited` инициализируется из `graph.getNodesMapKeys()`
- ✅ **Проверка соответствия ключей при обработке рёбер**: Проверяется существование ключа в `edgesMap` перед обработкой рёбер

## Файлы изменены

1. `backend/src/application/route-builder/RouteGraph.ts`:
   - Добавлены методы `getNodesMapSize()`, `getEdgesMapSize()`, `getNodesMapKeys()`, `getEdgesMapKeys()`, `getEdgesCountForNode()` для диагностики

2. `backend/src/application/route-builder/PathFinder.ts`:
   - Добавлена диагностика графа перед поиском пути
   - Исправлена инициализация алгоритма Dijkstra для использования `graph.getNodesMapKeys()`
   - Добавлена проверка соответствия ключей при обработке рёбер

## Критерий готовности

✅ **PathFinder должен:**
- Использовать тот же граф, который был построен в RouteGraphManager
- Выводить диагностику графа перед поиском пути
- Показывать соответствие ключей между `nodesMap` и `edgesMap`
- Инициализировать `unvisited` из `graph.getNodesMapKeys()`
- Проверять существование ключа в `edgesMap` перед обработкой рёбер

✅ **Логи должны показывать:**
- "📊 Диагностика графа перед поиском пути"
- "✅ Все ключи совпадают между nodesMap и edgesMap"
- "✅ Все N узлов из nodesMap добавлены в unvisited"
- "✅ Соответствие ключей: ✅ СОВПАДАЕТ"

✅ **Не должно быть ошибок:**
- "❌ КРИТИЧЕСКАЯ ОШИБКА: fromStopId НЕ существует в nodesMap"
- "❌ КРИТИЧЕСКАЯ ОШИБКА: fromStopId НЕ существует в edgesMap"
- "❌ КРИТИЧЕСКАЯ ОШИБКА: PathFinder использует НЕ тот граф"


