# Комплексный аудит бэкенда Travel App - Отчёт о рисках для продакшена

**Дата аудита:** 2024-12-19  
**Версия проекта:** 1.0.0  
**Стек:** Node.js 18, TypeScript 5.3, Express 4.21, PostgreSQL 15, Redis 7, MinIO

---

## 1. Краткий обзор

### 1.1. Структура проекта
- **Архитектура:** Clean Architecture (Domain, Application, Infrastructure, Presentation)
- **Точка входа:** `src/index.ts`
- **Запуск:** Docker Compose (`docker compose up backend`) или `npm run docker:dev`
- **Конфигурация:** Переменные окружения через `.env` (корневой или `backend/.env`)
- **База данных:** PostgreSQL 15 с миграциями в `src/infrastructure/database/migrations/`
- **Кеш:** Redis 7 для графа маршрутов и кеширования данных
- **Хранилище:** MinIO для бэкапов графов

### 1.2. Общий уровень качества
**Оценка: 7/10**

**Сильные стороны:**
- ✅ Чистая архитектура с разделением слоёв
- ✅ Использование TypeScript с strict mode
- ✅ Структурированное логирование
- ✅ Обработка ошибок с единым форматом ответов
- ✅ Миграции БД с версионированием
- ✅ Graceful shutdown
- ✅ Health checks
- ✅ Retry стратегия для OData API

**Основные риски:**
- ⚠️ Отсутствие rate limiting
- ⚠️ Потенциальные проблемы производительности при больших объёмах данных
- ⚠️ Отсутствие валидации входных данных через Zod/class-validator
- ⚠️ Использование `SELECT *` в запросах
- ⚠️ Отсутствие пагинации в API
- ⚠️ Потенциальные утечки соединений с БД
- ⚠️ Использование `KEYS` в Redis (блокирующая операция)
- ⚠️ Отсутствие middleware для обработки ошибок
- ⚠️ Устаревшие зависимости

---

## 2. Список проблем в порядке приоритета

### Issue 1: Отсутствие Rate Limiting
**Серьёзность:** 🔴 **КРИТИЧНО**  
**Компонент:** `src/index.ts`, `src/presentation/routes/index.ts`  
**Описание:**  
API не защищён от злоупотреблений. Отсутствует ограничение количества запросов на IP/пользователя.

**Риски для продакшена:**
- DDoS атаки могут вывести сервис из строя
- Злоупотребление ресурсами (CPU, память, БД)
- Блокировка соединений с БД при большом количестве запросов
- Исчерпание пула соединений PostgreSQL

**Предлагаемое решение:**
1. Установить `express-rate-limit`:
   ```bash
   npm install express-rate-limit
   ```
2. Добавить middleware в `src/index.ts`:
   ```typescript
   import rateLimit from 'express-rate-limit';
   
   const apiLimiter = rateLimit({
     windowMs: 15 * 60 * 1000, // 15 минут
     max: 100, // 100 запросов с одного IP
     message: 'Слишком много запросов, попробуйте позже',
     standardHeaders: true,
     legacyHeaders: false,
   });
   
   app.use(`/api/${API_VERSION}`, apiLimiter);
   ```
3. Настроить более строгие лимиты для тяжёлых эндпоинтов (route search, risk assessment)
4. Использовать Redis для распределённого rate limiting в кластере

---

### Issue 2: Отсутствие валидации входных данных
**Серьёзность:** 🔴 **КРИТИЧНО**  
**Компонент:** `src/presentation/controllers/*.ts`  
**Описание:**  
Входные данные из `req.query` и `req.body` не валидируются перед использованием. Используется только базовая проверка на наличие обязательных полей.

**Риски для продакшена:**
- SQL injection через параметры запросов (хотя используется parameterized queries)
- XSS атаки через невалидированные строки
- Некорректные данные могут привести к ошибкам в бизнес-логике
- Потенциальные проблемы с безопасностью при работе с данными пользователей

**Предлагаемое решение:**
1. Установить Zod:
   ```bash
   npm install zod
   ```
2. Создать схемы валидации в `src/presentation/validators/`:
   ```typescript
   // route-search.validator.ts
   import { z } from 'zod';
   
   export const RouteSearchSchema = z.object({
     from: z.string().min(1).max(255),
     to: z.string().min(1).max(255),
     date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),
     passengers: z.coerce.number().int().min(1).max(100).optional(),
   });
   ```
3. Создать middleware для валидации:
   ```typescript
   // validation.middleware.ts
   import { Request, Response, NextFunction } from 'express';
   import { ZodSchema } from 'zod';
   
   export function validate(schema: ZodSchema) {
     return (req: Request, res: Response, next: NextFunction) => {
       try {
         schema.parse({ ...req.query, ...req.body });
         next();
       } catch (error) {
         res.status(400).json({
           error: {
             code: 'VALIDATION_ERROR',
             message: 'Ошибка валидации входных данных',
             details: error.errors,
           },
         });
       }
     };
   }
   ```
4. Применить валидацию ко всем контроллерам

---

### Issue 3: Использование `SELECT *` в запросах
**Серьёзность:** 🟠 **ВЫСОКО**  
**Компонент:** `src/infrastructure/repositories/*.ts`  
**Описание:**  
Множество запросов используют `SELECT *`, что приводит к:
- Избыточной передаче данных из БД
- Потенциальным проблемам при изменении схемы БД
- Увеличению нагрузки на сеть и память

**Риски для продакшена:**
- Медленные запросы при большом количестве записей
- Высокое потребление памяти при загрузке всех полей
- Проблемы с производительностью при масштабировании

**Предлагаемое решение:**
1. Заменить все `SELECT *` на явное указание полей:
   ```typescript
   // Было:
   const result = await this.pool.query('SELECT * FROM stops WHERE id = $1', [id]);
   
   // Стало:
   const result = await this.pool.query(
     'SELECT id, name, latitude, longitude, city_id, is_airport, is_railway_station, metadata, created_at, updated_at FROM stops WHERE id = $1',
     [id]
   );
   ```
2. Создать константы с списками полей для переиспользования
3. Использовать TypeScript типы для гарантии соответствия

---

### Issue 4: Отсутствие пагинации в API
**Серьёзность:** 🟠 **ВЫСОКО**  
**Компонент:** `src/presentation/controllers/CitiesController.ts`, `src/infrastructure/repositories/*.ts`  
**Описание:**  
Эндпоинты, возвращающие списки (например, `/api/v1/cities`), не поддерживают пагинацию. Все данные загружаются сразу.

**Риски для продакшена:**
- Очень большие ответы при большом количестве данных
- Высокая нагрузка на БД и сеть
- Медленные ответы API
- Потенциальные таймауты при большом объёме данных

**Предлагаемое решение:**
1. Добавить параметры `page` и `limit` в запросы:
   ```typescript
   const page = parseInt(req.query.page as string || '1', 10);
   const limit = Math.min(parseInt(req.query.limit as string || '50', 10), 100);
   const offset = (page - 1) * limit;
   ```
2. Использовать `LIMIT` и `OFFSET` в SQL запросах
3. Возвращать метаданные пагинации:
   ```typescript
   {
     data: [...],
     pagination: {
       page,
       limit,
       total,
       totalPages: Math.ceil(total / limit),
     },
   }
   ```

---

### Issue 5: Использование `KEYS` в Redis
**Серьёзность:** 🟠 **ВЫСОКО**  
**Компонент:** `src/infrastructure/cache/RedisCacheService.ts:87`  
**Описание:**  
Метод `deleteByPattern` использует `client.keys(pattern)`, который является блокирующей операцией в Redis и может привести к проблемам производительности.

**Риски для продакшена:**
- Блокировка Redis на время выполнения `KEYS` при большом количестве ключей
- Замедление всех операций с Redis
- Потенциальные таймауты при большом объёме данных

**Предлагаемое решение:**
1. Использовать `SCAN` вместо `KEYS`:
   ```typescript
   async deleteByPattern(pattern: string): Promise<void> {
     try {
       const client = this.redis.getClient();
       if (!client || !this.redis.isReady()) {
         console.warn('⚠️ Redis not available, skipping cache delete by pattern');
         return;
       }

       const stream = client.scanStream({
         match: pattern,
         count: 100,
       });

       const keys: string[] = [];
       stream.on('data', (resultKeys: string[]) => {
         keys.push(...resultKeys);
       });

       await new Promise<void>((resolve, reject) => {
         stream.on('end', () => {
           if (keys.length > 0) {
             client.del(...keys).then(() => resolve()).catch(reject);
           } else {
             resolve();
           }
         });
         stream.on('error', reject);
       });
     } catch (error) {
       console.error(`❌ Error deleting cache by pattern ${pattern}:`, error);
     }
   }
   ```
2. Или использовать `ioredis` с поддержкой `scanStream`

---

### Issue 6: Потенциальные утечки соединений с БД
**Серьёзность:** 🟠 **ВЫСОКО**  
**Компонент:** `src/infrastructure/repositories/PostgresRouteRepository.ts:276`, `PostgresStopRepository.ts:106`  
**Описание:**  
В методах `saveVirtualRoutesBatch` и `saveRealStopsBatch` используется `client.connect()` и `client.release()`, но при ошибке между `BEGIN` и `COMMIT` соединение может не освободиться корректно.

**Риски для продакшена:**
- Исчерпание пула соединений PostgreSQL
- Блокировка новых запросов
- Деградация производительности

**Предлагаемое решение:**
1. Убедиться, что `client.release()` всегда вызывается в `finally`:
   ```typescript
   const client = await this.pool.connect();
   try {
     await client.query('BEGIN');
     // ... операции ...
     await client.query('COMMIT');
   } catch (error) {
     await client.query('ROLLBACK');
     throw error;
   } finally {
     client.release(); // ✅ Уже есть, но проверить все места
   }
   ```
2. Использовать `pg` транзакции через `pool.query` с callback или async/await
3. Добавить мониторинг количества активных соединений

---

### Issue 7: Последовательные await в циклах
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** `src/application/route-builder/use-cases/BuildRouteUseCase.optimized.ts:347-366`  
**Описание:**  
В методе `buildRouteFromPath` используется цикл `for` с последовательными `await` для каждого сегмента маршрута, что замедляет выполнение.

**Риски для продакшена:**
- Медленные ответы API при длинных маршрутах
- Высокая нагрузка на Redis при последовательных запросах
- Плохой пользовательский опыт

**Предлагаемое решение:**
1. Использовать `Promise.all` для параллельных запросов:
   ```typescript
   const segmentPromises = [];
   for (let i = 0; i < path.length - 1; i++) {
     const fromStopId = path[i];
     const toStopId = path[i + 1];
     
     segmentPromises.push(
       Promise.all([
         this.graphRepository.getEdgeWeight(fromStopId, toStopId),
         this.graphRepository.getEdgeMetadata(fromStopId, toStopId),
         this.flightRepository.getFlightsBetweenStops(fromStopId, toStopId, date),
       ])
     );
   }
   
   const segmentData = await Promise.all(segmentPromises);
   // Обработать результаты
   ```
2. Ограничить параллелизм через `p-limit` или аналогичную библиотеку

---

### Issue 8: Отсутствие middleware для обработки ошибок
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** `src/presentation/middleware/` (пустая директория)  
**Описание:**  
Отсутствует централизованный обработчик ошибок. Каждый контроллер обрабатывает ошибки самостоятельно, что приводит к дублированию кода.

**Риски для продакшена:**
- Несогласованные форматы ошибок
- Потенциальные утечки информации об ошибках
- Сложность поддержки и отладки

**Предлагаемое решение:**
1. Создать `src/presentation/middleware/error-handler.ts`:
   ```typescript
   import { Request, Response, NextFunction } from 'express';
   import { getLogger } from '../../shared/logger';
   
   export function errorHandler(
     err: Error,
     req: Request,
     res: Response,
     next: NextFunction
   ) {
     const logger = getLogger();
     logger.error('Unhandled error', err, {
       path: req.path,
       method: req.method,
     });
   
     res.status(500).json({
       error: {
         code: 'INTERNAL_ERROR',
         message: process.env.NODE_ENV === 'production'
           ? 'Внутренняя ошибка сервера'
           : err.message,
       },
     });
   }
   ```
2. Добавить в `src/index.ts`:
   ```typescript
   app.use(errorHandler);
   ```

---

### Issue 9: Загрузка всех данных в память
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** `src/application/route-builder/use-cases/BuildRouteUseCase.optimized.ts:212`  
**Описание:**  
Метод `findStopsForCity` загружает все остановки через `getAllRealStops()` и `getAllVirtualStops()`, затем фильтрует в памяти.

**Риски для продакшена:**
- Высокое потребление памяти при большом количестве остановок
- Медленная работа при большом объёме данных
- Потенциальные проблемы с производительностью

**Предлагаемое решение:**
1. Фильтровать на уровне БД:
   ```typescript
   async getStopsByCityName(cityName: string): Promise<RealStop[]> {
     const normalizedCity = this.normalizeCity(cityName);
     const result = await this.pool.query(
       `SELECT * FROM stops 
        WHERE LOWER(name) LIKE $1 
        ORDER BY name`,
       [`%${normalizedCity}%`]
     );
     return result.rows.map(row => this.mapRowToRealStop(row));
   }
   ```
2. Использовать полнотекстовый поиск PostgreSQL (GIN индекс уже есть)

---

### Issue 10: Устаревшие зависимости
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** `package.json`  
**Описание:**  
Множество зависимостей имеют доступные обновления, включая критические:
- `express`: 4.21.2 → 5.1.0 (major update)
- `redis`: 4.7.1 → 5.10.0 (major update)
- `uuid`: 9.0.1 → 13.0.0 (major update)
- `dotenv`: 16.6.1 → 17.2.3 (major update)
- `eslint`: 8.57.1 → 9.39.1 (major update)

**Риски для продакшена:**
- Уязвимости безопасности в старых версиях
- Отсутствие исправлений багов
- Проблемы совместимости с новыми версиями Node.js

**Предлагаемое решение:**
1. Обновить зависимости поэтапно:
   - Сначала patch/minor обновления
   - Затем major обновления с тестированием
2. Использовать `npm outdated` для отслеживания
3. Настроить Dependabot или аналогичный инструмент

---

### Issue 11: Отсутствие индексов для частых запросов
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** `src/infrastructure/database/migrations/003_optimized_storage_schema.sql`  
**Описание:**  
Хотя есть индексы для основных полей, могут отсутствовать составные индексы для частых запросов (например, поиск маршрутов по `from_stop_id` и `to_stop_id`).

**Риски для продакшена:**
- Медленные запросы при большом объёме данных
- Высокая нагрузка на CPU БД
- Плохая производительность поиска маршрутов

**Предлагаемое решение:**
1. Проанализировать частые запросы через `EXPLAIN ANALYZE`
2. Добавить составные индексы:
   ```sql
   CREATE INDEX IF NOT EXISTS idx_routes_from_to 
   ON routes(from_stop_id, to_stop_id);
   ```
3. Мониторить производительность запросов

---

### Issue 12: Отсутствие мониторинга и метрик
**Серьёзность:** 🟡 **СРЕДНЕ**  
**Компонент:** Весь проект  
**Описание:**  
Отсутствует интеграция с системами мониторинга (Prometheus, Grafana, DataDog) и сбор метрик производительности.

**Риски для продакшена:**
- Невозможность отслеживать производительность в реальном времени
- Сложность диагностики проблем
- Отсутствие алертов при деградации сервиса

**Предлагаемое решение:**
1. Установить `prom-client`:
   ```bash
   npm install prom-client
   ```
2. Добавить сбор метрик:
   - Время ответа API
   - Количество запросов
   - Ошибки по типам
   - Использование ресурсов (CPU, память)
3. Экспортировать метрики через `/metrics` endpoint
4. Интегрировать с Prometheus/Grafana

---

### Issue 13: Отсутствие логирования запросов
**Серьёзность:** 🟢 **НИЗКО**  
**Компонент:** `src/presentation/controllers/*.ts`  
**Описание:**  
Запросы к API не логируются, что затрудняет отладку и аудит.

**Предлагаемое решение:**
1. Добавить middleware для логирования:
   ```typescript
   import { Request, Response, NextFunction } from 'express';
   import { getLogger } from '../shared/logger';
   
   export function requestLogger(
     req: Request,
     res: Response,
     next: NextFunction
   ) {
     const logger = getLogger();
     const startTime = Date.now();
     
     res.on('finish', () => {
       const duration = Date.now() - startTime;
       logger.info('HTTP Request', {
         method: req.method,
         path: req.path,
         statusCode: res.statusCode,
         durationMs: duration,
         ip: req.ip,
       });
     });
     
     next();
   }
   ```
2. Добавить в `src/index.ts` перед роутами

---

### Issue 14: Отсутствие CORS конфигурации для продакшена
**Серьёзность:** 🟢 **НИЗКО**  
**Компонент:** `src/index.ts:34-37`  
**Описание:**  
CORS настроен только для одного origin из переменной окружения. Для продакшена может потребоваться более гибкая конфигурация.

**Предлагаемое решение:**
1. Добавить поддержку множественных origins:
   ```typescript
   const allowedOrigins = (process.env.CORS_ORIGINS || 'http://localhost:3000').split(',');
   
   app.use(cors({
     origin: (origin, callback) => {
       if (!origin || allowedOrigins.includes(origin)) {
         callback(null, true);
       } else {
         callback(new Error('Not allowed by CORS'));
       }
     },
     credentials: true,
   }));
   ```

---

### Issue 15: Отсутствие документации API
**Серьёзность:** 🟢 **НИЗКО**  
**Компонент:** Весь проект  
**Описание:**  
Отсутствует Swagger/OpenAPI документация для API.

**Предлагаемое решение:**
1. Установить `swagger-jsdoc` и `swagger-ui-express`
2. Добавить JSDoc комментарии к контроллерам
3. Настроить Swagger UI на `/api-docs`

---

## 3. Быстрые улучшения на 1-2 дня

### Приоритет 1 (Критично)
1. **Добавить Rate Limiting** (2-3 часа)
   - Установить `express-rate-limit`
   - Настроить базовые лимиты
   - Добавить более строгие лимиты для тяжёлых эндпоинтов

2. **Добавить валидацию входных данных** (4-6 часов)
   - Установить Zod
   - Создать схемы валидации для всех эндпоинтов
   - Добавить middleware валидации

3. **Заменить `KEYS` на `SCAN` в Redis** (1-2 часа)
   - Обновить метод `deleteByPattern`
   - Протестировать на больших объёмах данных

### Приоритет 2 (Важно)
4. **Добавить middleware для обработки ошибок** (2-3 часа)
   - Создать централизованный обработчик
   - Интегрировать с логированием

5. **Добавить пагинацию** (4-6 часов)
   - Обновить эндпоинты со списками
   - Добавить метаданные пагинации

6. **Заменить `SELECT *` на явные поля** (3-4 часа)
   - Обновить все репозитории
   - Создать константы с полями

---

## 4. Среднесрочные улучшения на 1-3 недели

### Неделя 1: Безопасность и производительность
1. **Обновить зависимости**
   - Обновить patch/minor версии
   - Протестировать major обновления
   - Обновить `express`, `redis`, `uuid` с тестированием

2. **Оптимизировать запросы к БД**
   - Добавить составные индексы
   - Оптимизировать медленные запросы
   - Добавить кеширование для частых запросов

3. **Параллелизация запросов**
   - Использовать `Promise.all` в циклах
   - Ограничить параллелизм через `p-limit`

### Неделя 2: Мониторинг и наблюдаемость
4. **Добавить метрики**
   - Интегрировать Prometheus
   - Настроить Grafana дашборды
   - Добавить алерты

5. **Улучшить логирование**
   - Добавить request logging middleware
   - Структурировать логи для парсинга
   - Настроить централизованное хранение логов

6. **Добавить трейсинг**
   - Интегрировать OpenTelemetry
   - Добавить correlation IDs
   - Трекинг запросов через все слои

### Неделя 3: Документация и тестирование
7. **Документация API**
   - Настроить Swagger/OpenAPI
   - Добавить примеры запросов
   - Документировать коды ошибок

8. **Улучшить тестовое покрытие**
   - Добавить интеграционные тесты для критичных путей
   - Добавить E2E тесты для основных сценариев
   - Настроить CI/CD для автоматического запуска тестов

9. **Оптимизация производительности**
   - Профилирование под нагрузкой
   - Оптимизация алгоритмов поиска маршрутов
   - Настройка connection pooling

---

## 5. Отсутствующие компоненты

### Критичные
- ❌ **Rate Limiting** - отсутствует полностью
- ❌ **Валидация входных данных** - только базовая проверка
- ❌ **Middleware для обработки ошибок** - директория пустая

### Важные
- ⚠️ **Мониторинг и метрики** - отсутствует
- ⚠️ **Request logging** - отсутствует
- ⚠️ **API документация** - отсутствует

### Желательные
- 💡 **Трейсинг запросов** - отсутствует
- 💡 **Health checks для зависимостей** - только базовый `/health`
- 💡 **Graceful degradation** - частично реализовано

---

## 6. Рекомендации по деплою

### Перед деплоем в продакшен ОБЯЗАТЕЛЬНО:
1. ✅ Добавить Rate Limiting
2. ✅ Добавить валидацию входных данных
3. ✅ Заменить `KEYS` на `SCAN` в Redis
4. ✅ Добавить middleware для обработки ошибок
5. ✅ Настроить мониторинг и алерты
6. ✅ Обновить устаревшие зависимости (хотя бы patch версии)
7. ✅ Настроить правильные значения переменных окружения для продакшена
8. ✅ Протестировать под нагрузкой

### Рекомендуется:
- Настроить CI/CD pipeline
- Добавить автоматические тесты
- Настроить резервное копирование БД
- Настроить логирование в централизованную систему
- Добавить документацию API

---

## 7. Итоговая оценка готовности к продакшену

**Текущая готовность: 60%**

**Блокеры для продакшена:**
- ❌ Отсутствие Rate Limiting
- ❌ Отсутствие валидации входных данных
- ❌ Потенциальные проблемы производительности

**После исправления критичных проблем: 85%**

**Для достижения 100% готовности необходимо:**
- Добавить мониторинг и метрики
- Улучшить тестовое покрытие
- Оптимизировать производительность
- Добавить документацию

---

**Дата создания отчёта:** 2024-12-19  
**Следующий аудит рекомендуется:** После исправления критичных проблем

