/**
 * Экспорт доменного слоя (Domain Layer)
 * 
 * Содержит сущности, интерфейсы и контракты бизнес-логики.
 * Не имеет зависимостей от других слоёв (Application, Infrastructure, Presentation).
 */

// Enums
export * from './enums';

// Entities
export * from './entities';

// Repositories (interfaces)
export * from './repositories';

// Types (utility types and helpers)
export * from './types';

