# Отчёт о разделении healthcheck на live и ready

## Дата: 2024

## 🔍 Анализ проблемы

### Найденная проблема

**Backend становится unhealthy во время выполнения тяжёлого pipeline**

#### Причина
1. **Healthcheck использовал `/health`:** Этот endpoint проверяет DB, Redis и граф, что может занимать время
2. **Pipeline блокирует healthcheck:** Во время выполнения pipeline (ODataSyncWorker, AirRouteGeneratorWorker, VirtualEntitiesGeneratorWorker, GraphBuilderWorker) проверки DB/Redis могут быть медленными или временно недоступными
3. **Контейнер помечается как unhealthy:** Docker помечает контейнер как unhealthy, даже если backend работает нормально, просто загружает данные

### Pipeline компоненты

Backend выполняет следующий pipeline при инициализации:
1. **ODataSyncWorker** — синхронизация данных из OData (mock data)
2. **AirRouteGeneratorWorker** — генерация авиамаршрутов для федеральных городов
3. **VirtualEntitiesGeneratorWorker** — генерация виртуальных остановок и маршрутов
4. **GraphBuilderWorker** — построение графа маршрутов

**Оценка времени выполнения:**
- Первая загрузка (пустая БД): 5-10 минут
- Повторная загрузка (данные есть): 1-3 минуты
- Пересборка графа: 2-5 минут
- **Максимальное время:** 10-15 минут

## 🔧 Выполненные исправления

### Разделение healthcheck на live и ready

#### 1. `/health/live` — Liveness probe
- ✅ **Всегда возвращает успех:** Если backend запущен и работает, возвращает 200
- ✅ **Без проверок:** Не проверяет DB, Redis, граф или данные
- ✅ **Используется для healthcheck:** Docker использует этот endpoint для определения, жив ли контейнер

**Реализация:**
```typescript
export function live(req: Request, res: Response): void {
  // Liveness check: service is running if it can respond
  res.status(200).json({
    status: 'alive',
    timestamp: new Date().toISOString(),
  });
}
```

#### 2. `/health` — Readiness probe (общий статус)
- ✅ **Проверяет готовность:** Проверяет DB, Redis и доступность графа
- ✅ **Возвращает детальный статус:** Показывает состояние всех компонентов
- ✅ **Используется для мониторинга:** Для проверки полной готовности сервиса

**Реализация:**
- Проверяет подключение к PostgreSQL
- Проверяет подключение к Redis
- Проверяет доступность графа (опционально)
- Возвращает 200 если все готово, 503 если деградирован

#### 3. `/health/ready` — Readiness probe (детальный)
- ✅ **Проверяет готовность:** Аналогично `/health`, но с более детальной информацией
- ✅ **Используется для оркестрации:** Для определения, должен ли сервис получать трафик

### Обновление docker-compose.yml

#### Backend healthcheck
**До:**
```yaml
healthcheck:
  test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health"]
  interval: 30s
  timeout: 20s
  retries: 5
  start_period: 600s
```

**После:**
```yaml
healthcheck:
  test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health/live"]
  interval: 30s
  timeout: 20s
  retries: 5
  start_period: 600s
```

**Изменения:**
- ✅ Использует `/health/live` вместо `/health`
- ✅ Контейнер становится healthy сразу после запуска backend
- ✅ Pipeline не блокирует healthcheck
- ✅ Контейнер не становится unhealthy во время загрузки данных

### Проверка зависимостей

#### Frontend → Backend
- ✅ **Зависимость корректна:** Frontend зависит от Backend через `condition: service_healthy`
- ✅ **Порядок запуска:** Frontend не запустится, пока Backend не станет healthy
- ✅ **Новое поведение:** Frontend запустится, когда Backend станет healthy (т.е. запущен), даже если pipeline еще выполняется
- ✅ **Безопасность:** Frontend может работать в limited mode, если данные еще загружаются

#### Backend → Postgres, MinIO, Redis
- ✅ **Все зависимости корректны:** Backend зависит от всех сервисов через `condition: service_healthy`
- ✅ **Порядок запуска:** Backend не запустится, пока все зависимости не станут healthy

## 📊 Проверка всех healthchecks

### Postgres
```yaml
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U travel_user -d travel_app || exit 1"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 10s
```
✅ **Статус:** Корректно настроен

### Redis
```yaml
healthcheck:
  test: ["CMD", "redis-cli", "--raw", "-a", "123456S", "ping"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 10s
```
✅ **Статус:** Корректно настроен

### MinIO
```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  interval: 30s
  timeout: 20s
  retries: 3
  start_period: 30s
```
✅ **Статус:** Корректно настроен

### Backend
```yaml
healthcheck:
  test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health/live"]
  interval: 30s
  timeout: 20s
  retries: 5
  start_period: 600s
```
✅ **Статус:** Оптимизирован для длительных операций
- Использует `/health/live` для проверки живости
- Не блокируется pipeline
- Контейнер остается healthy во время загрузки данных

### Frontend
```yaml
healthcheck:
  test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 60s
```
✅ **Статус:** Корректно настроен

## 📝 Изменённые файлы

1. ✅ `docker-compose.yml` — обновлён healthcheck для backend:
   - `test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health"]`
   - → `test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health/live"]`

### Неизменённые файлы (уже корректны)

2. ✅ `backend/src/presentation/controllers/HealthController.ts` — `/health/live` уже реализован корректно
3. ✅ `backend/src/index.ts` — маршруты `/health/live` уже настроены
4. ✅ `backend/src/presentation/routes/index.ts` — маршруты API уже настроены

## ✅ Статус готовности

### Healthcheck endpoints
- ✅ `/health/live` — Liveness probe (всегда возвращает 200, если backend работает)
- ✅ `/health` — Readiness probe (проверяет DB, Redis, граф)
- ✅ `/health/ready` — Readiness probe (детальная проверка готовности)

### Docker healthcheck
- ✅ Backend использует `/health/live` для healthcheck
- ✅ Контейнер становится healthy сразу после запуска backend
- ✅ Pipeline не блокирует healthcheck
- ✅ Контейнер не становится unhealthy во время загрузки данных

### Зависимости
- ✅ Backend зависит от: postgres, minio, redis (condition: service_healthy)
- ✅ Frontend зависит от: backend (condition: service_healthy)

### Порядок запуска
1. Postgres, Redis, MinIO запускаются первыми
2. Backend ждёт готовности всех зависимостей
3. Backend запускается и становится healthy (через `/health/live`)
4. Backend выполняет pipeline (до 10-15 минут) — контейнер остается healthy
5. Frontend ждёт готовности Backend (healthy через `/health/live`)
6. Frontend запускается после того, как Backend стал healthy

## 🎯 Ожидаемый результат

После оптимизации:

### Стабильность
- ✅ Backend не становится unhealthy во время выполнения pipeline
- ✅ Healthcheck не блокируется pipeline
- ✅ Контейнер остается healthy, даже если данные еще загружаются
- ✅ Pipeline может выполняться 10-15 минут без проблем

### Время запуска
- ✅ Backend становится healthy сразу после запуска (через `/health/live`)
- ✅ Frontend запускается, когда Backend healthy (не ждёт завершения pipeline)
- ✅ Pipeline выполняется в фоне, не блокируя healthcheck

### Надёжность
- ✅ `/health/live` всегда возвращает 200, если backend работает
- ✅ `/health` и `/health/ready` остаются для проверки полной готовности
- ✅ Все healthchecks стабильны и не мешают работе проекта

## 📋 Рекомендации

### Мониторинг
1. **Liveness probe:** Использовать `/health/live` для определения, жив ли контейнер
2. **Readiness probe:** Использовать `/health` или `/health/ready` для определения готовности к трафику
3. **Метрики:** Отслеживать время выполнения pipeline отдельно от healthcheck

### Дальнейшая оптимизация
1. **Асинхронный pipeline:** Pipeline уже выполняется асинхронно, не блокируя healthcheck
2. **Readiness для трафика:** Если нужно, можно использовать `/health/ready` для определения, должен ли сервис получать трафик
3. **Градуальная готовность:** Сервис может работать в limited mode, пока данные загружаются

Все изменения сохраняют работоспособность проекта и не ломают существующую логику.



