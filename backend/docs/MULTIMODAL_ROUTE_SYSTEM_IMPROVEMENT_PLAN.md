# План улучшений системы мультимодального поиска маршрутов

**Дата создания:** 2025-01-XX  
**Статус:** План разработки  
**Версия:** 1.0.0

---

## 📋 Содержание

1. [Выявление ошибок](#1-выявление-ошибок)
2. [Улучшение логики данных](#2-улучшение-логики-данных)
3. [Добавление федеральных городов](#3-добавление-федеральных-городов)
4. [Маршруты Россия → Якутия](#4-маршруты-россия--якутия)
5. [Нормализация переправы](#5-нормализация-переправы)
6. [План рефакторинга поиска](#6-план-рефакторинга-поиска)
7. [Оптимальная архитектура](#7-оптимальная-архитектура)

---

## 1. Выявление ошибок

### 1.1 Проблема: Исчез город "Якутск" из списка доступных городов

**Корневая причина:**
- В `stops.json` есть остановка `stop-001`: "Аэропорт Якутск (Туймаада)"
- Функция `extractCityFromStopName()` в `city-normalizer.ts` (строки 121-180) извлекает последнее слово в скобках как город
- Результат: `city_id = "Туймаада"` вместо `city_id = "Якутск"`
- При поиске по "Якутск" остановка не находится, так как в БД она хранится с `city_id = "Туймаада"`

**Где происходит:**
- `ODataSyncWorker.ts`, строка 267: `cityName = extractCityFromStopName(stopData.name, stopData.address)`
- `RouteGraphBuilder.ts`, строка 72: `const cityName = extractCityFromStopName(stop.name, stop.metadata?.address)`

**Решение:**
- Улучшить `extractCityFromStopName()` для обработки паттерна "Аэропорт [Город] ([Название аэропорта])"
- Правило: если в скобках находится известное название аэропорта (Туймаада, Шереметьево, Домодедово), извлекать город из части ДО скобок
- Создать справочник известных названий аэропортов для фильтрации

---

### 1.2 Проблема: Появился ложный город "Туймада"

**Корневая причина:**
- Та же, что в 1.1: `extractCityFromStopName()` извлекает "Туймаада" как отдельный город
- В БД создается запись с `city_id = "Туймаада"` (или нормализованное "туймаада")
- При генерации виртуальных остановок `VirtualEntitiesGeneratorWorker` может создать виртуальную остановку для "Туймаада", если она не найдена в reference

**Где происходит:**
- `ODataSyncWorker.ts`: сохранение остановки с неправильным `city_id`
- `VirtualEntitiesGeneratorWorker.ts`: генерация виртуальных остановок для городов, не найденных в reference

**Решение:**
- Исправить извлечение `city_id` для остановок с названиями аэропортов
- Добавить валидацию: если извлеченный город не найден в reference Якутии и не является федеральным городом, проверить, не является ли он названием аэропорта
- Исключить "Туймаада" из списка городов при генерации виртуальных сущностей

---

### 1.3 Проблема: Несуществующие города и мусорные записи

**Корневая причина:**
- OData-источник может содержать неполные записи (без координат, без названия, с опечатками)
- `ODataSyncWorker.ts` не фильтрует невалидные записи перед сохранением
- `extractCityFromStopName()` может извлечь мусорные значения (пустые строки, служебные слова, опечатки)

**Где происходит:**
- `ODataSyncWorker.ts`, метод `parseODataResponse()`: создание `RealStop` без валидации
- Отсутствие проверки на пустые/некорректные `city_id`

**Решение:**
- Добавить валидацию в `parseODataResponse()`:
  - Проверка наличия обязательных полей (`name`, `latitude`, `longitude`)
  - Проверка корректности координат (диапазон: lat [-90, 90], lng [-180, 180])
  - Проверка извлеченного `city_id` (не пустой, не служебное слово)
  - Фильтрация записей с некорректными данными с логированием
- Создать whitelist допустимых городов (reference Якутии + федеральные города) для валидации

---

### 1.4 Проблема: Не строятся маршруты из российских городов (Москва → Олёкминск)

**Корневая причина:**
- В `yakutia-cities-reference.json` есть только города Якутии
- Федеральные города (Москва, Новосибирск, Красноярск, Иркутск, Хабаровск) отсутствуют в reference
- `VirtualEntitiesGeneratorWorker` использует только `yakutia-cities-reference.json` для генерации виртуальных остановок
- Если для федерального города нет реальной остановки в OData, виртуальная остановка не создается
- `ensureYakutiaCitiesConnectivity()` проверяет только города Якутии, не создает маршруты из федеральных городов

**Где происходит:**
- `VirtualEntitiesGeneratorWorker.ts`, строка 130: `const missingCities = Object.keys(this.citiesDirectory).filter(...)`
- `citiesDirectory` содержит только города из `yakutia-cities-reference.json`
- `ensureYakutiaCitiesConnectivity()` (строка 501): проверяет только `getAllYakutiaCities()`

**Решение:**
- Расширить reference: создать `russia-federal-cities-reference.json` с федеральными городами
- Объединить reference Якутии и федеральных городов в единый справочник
- Обновить `VirtualEntitiesGeneratorWorker` для работы с объединенным справочником
- Расширить `ensureYakutiaCitiesConnectivity()` для создания маршрутов из федеральных городов в Якутию

---

## 2. Улучшение логики данных

### 2.1 Нормализация остановок, относящихся к Якутску

**Проблема:**
- Остановки "Аэропорт Якутск (Туймаада)", "Автостанция Центральная Якутск", "Нижний Бестях" должны быть привязаны к городу "Якутск"
- Сейчас они могут иметь разные `city_id` или отсутствующий `city_id`

**План исправления:**

#### Шаг 1: Создать справочник аэропортов и их городов
- Файл: `backend/data/reference/airports-reference.json`
- Структура:
  ```json
  {
    "airports": [
      {
        "name": "Туймаада",
        "city": "Якутск",
        "aliases": ["Туймада", "Yakutsk Airport"]
      },
      {
        "name": "Шереметьево",
        "city": "Москва",
        "aliases": ["SVO", "Sheremetyevo"]
      }
    ]
  }
  ```

#### Шаг 2: Улучшить `extractCityFromStopName()`
- Добавить проверку паттерна "Аэропорт [Город] ([Название аэропорта])"
- Если название в скобках найдено в справочнике аэропортов, использовать город из справочника
- Если паттерн не найден, использовать логику извлечения города из части ДО скобок

#### Шаг 3: Создать справочник пригородов и их основных городов
- Файл: `backend/data/reference/suburbs-reference.json`
- Структура:
  ```json
  {
    "suburbs": [
      {
        "name": "Нижний Бестях",
        "mainCity": "Якутск",
        "type": "ferry_terminal"
      },
      {
        "name": "Беркакит",
        "mainCity": "Нерюнгри",
        "type": "railway_station"
      }
    ]
  }
  ```

#### Шаг 4: Обновить `ODataSyncWorker.parseODataResponse()`
- После извлечения `city_id` через `extractCityFromStopName()`:
  - Проверить, не является ли извлеченный город пригородом (через `suburbs-reference.json`)
  - Если да, заменить на `mainCity`
  - Проверить, не является ли извлеченный город названием аэропорта (через `airports-reference.json`)
  - Если да, заменить на город из справочника

---

### 2.2 Исключение мусорных/неполных OData-записей

**План исправления:**

#### Шаг 1: Создать валидатор для остановок
- Файл: `backend/src/shared/validators/stop-validator.ts`
- Функции:
  - `validateStopData(stopData): ValidationResult`
  - Проверки:
    - `name` не пустой, длина >= 3 символов
    - `latitude` в диапазоне [-90, 90], не NaN
    - `longitude` в диапазоне [-180, 180], не NaN
    - `city_id` (если извлечен) не пустой, не служебное слово

#### Шаг 2: Создать валидатор для маршрутов
- Файл: `backend/src/shared/validators/route-validator.ts`
- Функции:
  - `validateRouteData(routeData): ValidationResult`
  - Проверки:
    - `stops` массив, длина >= 2
    - Все `stopId` в массиве `stops` существуют в БД
    - `transportType` валидный (из enum)

#### Шаг 3: Обновить `ODataSyncWorker.parseODataResponse()`
- Применить валидаторы перед созданием сущностей
- Отфильтровать невалидные записи с логированием
- Вернуть статистику: сколько записей отфильтровано и почему

---

### 2.3 Приведение всех городов к единому справочнику

**План исправления:**

#### Шаг 1: Создать объединенный справочник городов
- Файл: `backend/data/reference/unified-cities-reference.json`
- Структура:
  ```json
  {
    "version": "1.0.0",
    "cities": [
      {
        "name": "Якутск",
        "normalizedName": "якутск",
        "latitude": 62.0278,
        "longitude": 129.7042,
        "region": "Yakutia",
        "isKeyCity": true,
        "isFederalCity": false
      },
      {
        "name": "Москва",
        "normalizedName": "москва",
        "latitude": 55.7558,
        "longitude": 37.6173,
        "region": "Central",
        "isKeyCity": true,
        "isFederalCity": true
      }
    ]
  }
  ```

#### Шаг 2: Создать утилиту для загрузки объединенного справочника
- Файл: `backend/src/shared/utils/unified-cities-loader.ts`
- Функции:
  - `loadUnifiedCitiesReference(): Map<string, UnifiedCity>`
  - `getUnifiedCity(cityName): UnifiedCity | undefined`
  - `isCityInReference(cityName): boolean`
  - `getAllFederalCities(): UnifiedCity[]`
  - `getAllYakutiaCities(): UnifiedCity[]`

#### Шаг 3: Обновить все места использования reference
- Заменить `yakutia-cities-loader.ts` на `unified-cities-loader.ts`
- Обновить `VirtualEntitiesGeneratorWorker` для использования объединенного справочника
- Обновить валидацию в `ODataSyncWorker` для проверки против объединенного справочника

---

### 2.4 Синхронизация OData и reference-файлов

**План исправления:**

#### Шаг 1: Создать механизм синхронизации
- Файл: `backend/src/application/workers/ReferenceSyncWorker.ts`
- Логика:
  - Загружает все уникальные `city_id` из БД (stops, routes)
  - Сравнивает с `unified-cities-reference.json`
  - Находит города, которые есть в БД, но отсутствуют в reference
  - Логирует предупреждения о рассинхронизации

#### Шаг 2: Добавить автоматическую валидацию при загрузке данных
- В `ODataSyncWorker.parseODataResponse()`:
  - После извлечения `city_id` проверить наличие в `unified-cities-reference.json`
  - Если город не найден, логировать предупреждение
  - Если это известный город (например, из справочника аэропортов), автоматически добавить в reference (опционально)

#### Шаг 3: Создать admin-endpoint для ручной синхронизации
- Endpoint: `POST /api/v1/admin/sync-reference`
- Действие: запускает `ReferenceSyncWorker` и возвращает отчет о рассинхронизации

---

## 3. Добавление федеральных городов

### 3.1 Список федеральных городов для добавления

**Города для включения в reference:**

1. **Москва**
   - Координаты: 55.7558, 37.6173
   - Аэропорты: Шереметьево, Домодедово, Внуково
   - Вокзалы: Ленинградский, Казанский, Ярославский, Курский, Павелецкий, Киевский, Белорусский, Савеловский, Рижский

2. **Санкт-Петербург**
   - Координаты: 59.9343, 30.3351
   - Аэропорты: Пулково
   - Вокзалы: Московский, Финляндский, Витебский, Балтийский, Ладожский

3. **Новосибирск**
   - Координаты: 55.0084, 82.9357
   - Аэропорты: Толмачёво
   - Вокзалы: Новосибирск-Главный

4. **Красноярск**
   - Координаты: 56.0089, 92.8537
   - Аэропорты: Емельяново
   - Вокзалы: Красноярск-Пассажирский

5. **Иркутск**
   - Координаты: 52.2680, 104.3889
   - Аэропорты: Иркутск
   - Вокзалы: Иркутск-Пассажирский

6. **Хабаровск**
   - Координаты: 48.4802, 135.0719
   - Аэропорты: Хабаровск (Новый)
   - Вокзалы: Хабаровск-1

7. **Владивосток**
   - Координаты: 43.1155, 131.8855
   - Аэропорты: Владивосток (Кневичи)
   - Вокзалы: Владивосток

8. **Екатеринбург**
   - Координаты: 56.8431, 60.6454
   - Аэропорты: Кольцово
   - Вокзалы: Екатеринбург-Пассажирский

9. **Казань**
   - Координаты: 55.8304, 49.0661
   - Аэропорты: Казань
   - Вокзалы: Казань-Пассажирский

10. **Нижний Новгород**
    - Координаты: 56.2965, 43.9361
    - Аэропорты: Стригино
    - Вокзалы: Нижний Новгород-Московский

---

### 3.2 План интеграции федеральных городов

#### Шаг 1: Создать `russia-federal-cities-reference.json`
- Файл: `backend/data/reference/russia-federal-cities-reference.json`
- Структура аналогична `yakutia-cities-reference.json`
- Включить все 10 городов из списка выше

#### Шаг 2: Объединить reference в единый файл
- Создать `unified-cities-reference.json` (см. раздел 2.3)
- Объединить города Якутии и федеральные города
- Добавить поле `region` для различения регионов

#### Шаг 3: Обновить `VirtualEntitiesGeneratorWorker`
- Использовать `unified-cities-reference.json` вместо `yakutia-cities-reference.json`
- Генерировать виртуальные остановки для федеральных городов, если их нет в OData
- Создавать виртуальные маршруты из федеральных городов в Якутию (hub-based, через Якутск)

#### Шаг 4: Обновить `ensureYakutiaCitiesConnectivity()`
- Переименовать в `ensureCitiesConnectivity()`
- Расширить логику:
  - Для пар федеральный город → город Якутии: создавать виртуальные маршруты через Якутск (hub)
  - Для пар федеральный город → федеральный город: создавать прямые виртуальные маршруты (если нет реальных)

---

### 3.3 Избежание конфликтов с OData

**План:**

#### Шаг 1: Приоритет данных
- Реальные данные из OData имеют приоритет над виртуальными
- Если для федерального города есть реальная остановка в OData, виртуальная остановка не создается
- Если для пары городов есть реальный маршрут, виртуальный маршрут не создается

#### Шаг 2: Проверка перед генерацией
- В `VirtualEntitiesGeneratorWorker.generateVirtualStops()`:
  - Проверить наличие реальных остановок для города
  - Если есть, пропустить генерацию виртуальной остановки
- В `VirtualEntitiesGeneratorWorker.generateVirtualRoutes()`:
  - Проверить наличие реальных маршрутов между городами
  - Если есть, пропустить генерацию виртуального маршрута

#### Шаг 3: Логирование конфликтов
- При обнаружении конфликта (реальная остановка/маршрут существует) логировать предупреждение
- Не перетирать реальные данные виртуальными

---

## 4. Маршруты Россия → Якутия

### 4.1 Примеры смешанных маршрутов

**Типы маршрутов:**

1. **Прямые авиамаршруты:**
   - Москва → Якутск (самолёт)
   - Новосибирск → Якутск (самолёт)
   - Иркутск → Якутск (самолёт)

2. **Многосегментные маршруты (air + ground):**
   - Москва → Якутск → Чурапча (самолёт + такси/автобус)
   - Москва → Якутск → Олёкминск (самолёт + такси/автобус)
   - Новосибирск → Якутск → Мирный (самолёт + автобус)

3. **Многосегментные маршруты (air + ferry + ground):**
   - Москва → Якутск → Нижний Бестях (самолёт → переправа → автобус)
   - Иркутск → Якутск → Нижний Бестях (самолёт → переправа → автобус)

---

### 4.2 Mock-данные для смешанных маршрутов

**План:**

#### Шаг 1: Расширить `stops.json`
- Добавить остановки для федеральных городов (если их нет):
  - Аэропорт Москва Шереметьево (уже есть: `stop-002`)
  - Аэропорт Новосибирск Толмачёво (новый)
  - Аэропорт Красноярск Емельяново (новый)
  - Аэропорт Иркутск (уже есть: `stop-003`)
  - Аэропорт Хабаровск (новый)
- Добавить остановку "Нижний Бестях" (ferry terminal):
  ```json
  {
    "id": "stop-027",
    "name": "Паромная переправа Нижний Бестях",
    "coordinates": {
      "latitude": 61.9500,
      "longitude": 129.6000
    },
    "type": "ferry_terminal"
  }
  ```

#### Шаг 2: Расширить `routes.json`
- Добавить авиамаршруты из федеральных городов в Якутск:
  ```json
  {
    "id": "route-030",
    "name": "Новосибирск - Якутск (Авиа)",
    "routeNumber": "S7-2001",
    "transportType": "PLANE",
    "stops": ["stop-novosibirsk-airport", "stop-001"],
    "baseFare": 15000
  }
  ```
- Добавить маршруты Якутск → Нижний Бестях (ferry):
  ```json
  {
    "id": "route-031",
    "name": "Якутск - Нижний Бестях (Паром)",
    "routeNumber": "FERRY-001",
    "transportType": "FERRY",
    "stops": ["stop-011", "stop-027"],
    "baseFare": 500
  }
  ```

#### Шаг 3: Расширить `flights.json`
- Добавить рейсы для новых маршрутов
- Для ferry-маршрутов: создать "рейсы" с расписанием переправы

---

### 4.3 Формирование авиамаршрутов

**План:**

#### Шаг 1: Создать генератор авиамаршрутов
- Файл: `backend/src/application/workers/AirRouteGeneratorWorker.ts`
- Логика:
  - Загружает все федеральные города из reference
  - Для каждого федерального города создает авиамаршрут в Якутск (если его нет)
  - Создает обратные маршруты (Якутск → федеральный город)
  - Генерирует рейсы для маршрутов (ежедневно, 2-3 рейса в день)

#### Шаг 2: Интеграция с существующей системой
- Запускать `AirRouteGeneratorWorker` после `ODataSyncWorker`
- Перед `VirtualEntitiesGeneratorWorker`
- Созданные маршруты сохранять как реальные (не виртуальные), если они основаны на реальных данных авиакомпаний

---

### 4.4 Соединение air + ground

**План:**

#### Шаг 1: Обновить `GraphBuilderWorker`
- При построении графа учитывать возможность пересадок:
  - Если есть маршрут A → B (air) и маршрут B → C (ground), создавать ребро A → C через B
  - Вес ребра = сумма весов сегментов + время пересадки (например, 60 минут)

#### Шаг 2: Обновить `PathFinder` (Dijkstra)
- При поиске пути учитывать возможность пересадок между разными типами транспорта
- Минимизировать общее время в пути, учитывая время пересадки

#### Шаг 3: Обновить `BuildRouteUseCase`
- При построении маршрута из сегментов:
  - Если сегмент заканчивается в аэропорту, а следующий начинается в том же городе (но другой остановке), добавить сегмент пересадки
  - Сегмент пересадки: тип = "TRANSFER", длительность = время пересадки (60-120 минут)

---

### 4.5 Подключение маршрутов к graph-builder

**План:**

#### Шаг 1: Обновить `GraphBuilderWorker.buildGraphStructure()`
- При создании рёбер из маршрутов:
  - Если маршрут содержит несколько сегментов (например, air + ground), создавать рёбра для каждого сегмента
  - Создавать рёбра пересадки между сегментами в одном городе

#### Шаг 2: Обновить `RouteGraphBuilder.buildFromDataset()`
- При построении графа из датасета:
  - Группировать маршруты по типам транспорта
  - Создавать рёбра для каждого сегмента маршрута
  - Создавать рёбра пересадки между остановками в одном городе (если они связаны маршрутом)

---

## 5. Нормализация переправы

### 5.1 Логика: Якутск → Нижний Бестях как ferry-транспорт

**План:**

#### Шаг 1: Добавить остановки для переправы
- В `stops.json`:
  - `stop-011`: "Автостанция Центральная Якутск" (уже есть)
  - `stop-027`: "Паромная переправа Нижний Бестях" (новый)
  - `stop-028`: "Пристань Якутск" (новый, для отправления парома)

#### Шаг 2: Создать маршрут переправы
- В `routes.json`:
  ```json
  {
    "id": "route-031",
    "name": "Якутск - Нижний Бестях (Паром)",
    "routeNumber": "FERRY-001",
    "transportType": "FERRY",
    "stops": ["stop-028", "stop-027"],
    "baseFare": 500,
    "metadata": {
      "ferrySchedule": {
        "summer": "06:00-23:00",
        "winter": "07:00-22:00",
        "frequency": "every 30 minutes"
      }
    }
  }
  ```

#### Шаг 3: Создать рейсы для переправы
- В `flights.json`:
  - Генерировать рейсы каждые 30 минут в рабочее время
  - Длительность: 15-20 минут
  - Цена: 500 рублей

---

### 5.2 Интеграция в общий граф

**План:**

#### Шаг 1: Обновить `GraphBuilderWorker`
- При построении графа учитывать ferry-маршруты как обычные маршруты
- Тип транспорта: `FERRY` (уже есть в enum `TransportType`)

#### Шаг 2: Обновить `RouteGraphBuilder`
- При построении графа из датасета:
  - Определять тип транспорта для ferry-маршрутов как `TransportType.FERRY`
  - Создавать рёбра для ferry-маршрутов с корректным весом (длительность переправы)

---

### 5.3 Расчет веса маршрута для переправы

**План:**

#### Шаг 1: Определить длительность переправы
- Якутск → Нижний Бестях: ~15-20 минут
- Учесть время ожидания парома (если расписание не совпадает): +30 минут максимум

#### Шаг 2: Обновить `calculateWeightWithValidation()`
- Для ferry-маршрутов:
  - Если есть реальное расписание, использовать его для расчета веса
  - Если расписания нет, использовать фиксированное значение: 20 минут (переправа) + 30 минут (ожидание) = 50 минут

#### Шаг 3: Учесть сезонность
- Летом: переправа работает чаще, время ожидания меньше
- Зимой: переправа работает реже, время ожидания больше
- Использовать `metadata.ferrySchedule` для расчета веса в зависимости от сезона

---

## 6. План рефакторинга поиска

### 6.1 Улучшения в VirtualEntitiesGeneratorWorker

**Проблемы:**
- Использует только reference Якутии, не учитывает федеральные города
- Генерирует виртуальные остановки для всех городов из reference, даже если они не нужны
- Не проверяет наличие реальных остановок перед генерацией виртуальных

**План улучшений:**

#### Шаг 1: Расширить поддержку федеральных городов
- Использовать `unified-cities-reference.json` вместо `yakutia-cities-reference.json`
- Генерировать виртуальные остановки для федеральных городов, если их нет в OData

#### Шаг 2: Оптимизировать генерацию виртуальных остановок
- Генерировать виртуальные остановки только для городов, которые:
  - Есть в reference (Якутия или федеральные)
  - Нет реальных остановок в БД
  - Являются ключевыми городами (`isKeyCity: true`) или запрошены пользователем

#### Шаг 3: Улучшить генерацию виртуальных маршрутов
- Для федеральных городов → Якутия: создавать hub-based маршруты через Якутск
- Для городов Якутии: создавать прямые маршруты или hub-based через Якутск
- Проверять наличие реальных маршрутов перед генерацией виртуальных

---

### 6.2 Улучшения в GraphBuilderWorker

**Проблемы:**
- Не учитывает возможность пересадок между разными типами транспорта
- Не создает рёбра пересадки в одном городе
- Не оптимизирует вес рёбер для смешанных маршрутов

**План улучшений:**

#### Шаг 1: Добавить поддержку пересадок
- При построении графа:
  - Группировать остановки по городам
  - Для каждого города создавать рёбра пересадки между остановками (вес = время пересадки, например, 60 минут)
  - Рёбра пересадки: тип = "TRANSFER"

#### Шаг 2: Оптимизировать вес рёбер
- Для авиамаршрутов: использовать реальную длительность рейса
- Для автобусных маршрутов: использовать расстояние / средняя скорость (60 км/ч)
- Для ferry-маршрутов: использовать длительность переправы + время ожидания

#### Шаг 3: Улучшить валидацию графа
- Проверять, что все рёбра имеют корректный вес (> 0, не NaN, не Infinity)
- Проверять, что все узлы имеют хотя бы одно входящее или исходящее ребро
- Проверять связность графа (все узлы достижимы из hub-узла)

---

### 6.3 Архитектура данных

**Текущая архитектура:**
- OData → PostgreSQL (stops, routes, flights)
- PostgreSQL → Graph (nodes, edges) → Redis
- Поиск маршрутов: чтение из Redis (граф)

**Предлагаемая архитектура:**

#### Уровень 1: Источники данных
- **OData API**: реальные данные о транспорте
- **Reference файлы**: справочники городов, аэропортов, пригородов
- **Mock-данные**: демо-данные для разработки

#### Уровень 2: Нормализация и валидация
- **ODataSyncWorker**: загрузка и нормализация данных из OData
- **Validators**: валидация остановок, маршрутов, рейсов
- **CityNormalizer**: нормализация названий городов, извлечение city_id

#### Уровень 3: Генерация виртуальных сущностей
- **VirtualEntitiesGeneratorWorker**: генерация виртуальных остановок и маршрутов
- **AirRouteGeneratorWorker**: генерация авиамаршрутов из федеральных городов
- **ReferenceSyncWorker**: синхронизация reference с данными в БД

#### Уровень 4: Построение графа
- **GraphBuilderWorker**: построение графа из реальных и виртуальных данных
- **RouteGraphBuilder**: построение графа маршрутов с учетом пересадок
- **GraphValidator**: валидация графа (связность, корректность весов)

#### Уровень 5: Поиск маршрутов
- **OptimizedBuildRouteUseCase**: поиск маршрутов в графе (Dijkstra)
- **PathFinder**: алгоритм поиска кратчайшего пути
- **RouteBuilder**: построение маршрута из найденного пути

---

### 6.4 Избежание повторных ошибок

**План:**

#### Шаг 1: Создать инварианты системы
- Файл: `backend/src/shared/invariants/route-system-invariants.ts`
- Инварианты:
  - Каждая остановка должна иметь корректный `city_id` (не пустой, не служебное слово)
  - Каждый город в БД должен быть в `unified-cities-reference.json`
  - Каждый маршрут должен иметь минимум 2 остановки
  - Каждое ребро в графе должно иметь корректный вес (> 0, не NaN)

#### Шаг 2: Добавить автоматические проверки
- В `ODataSyncWorker`: проверка инвариантов перед сохранением
- В `GraphBuilderWorker`: проверка инвариантов после построения графа
- В `BuildRouteUseCase`: проверка инвариантов перед поиском маршрута

#### Шаг 3: Создать тесты для инвариантов
- Unit-тесты для каждого инварианта
- Integration-тесты для проверки инвариантов в полном пайплайне
- E2E-тесты для проверки инвариантов через API

---

## 7. Оптимальная архитектура

### 7.1 Архитектура для региональных маршрутов

**Региональные маршруты:** маршруты внутри Якутии (например, Якутск → Олёкминск)

**Архитектура:**
- Использовать `yakutia-cities-reference.json` для валидации городов
- Генерировать виртуальные маршруты для всех пар городов Якутии через `ensureYakutiaCitiesConnectivity()`
- Hub-based маршруты через Якутск для удаленных городов
- Прямые маршруты для близких городов

---

### 7.2 Архитектура для межрегиональных маршрутов

**Межрегиональные маршруты:** маршруты между федеральными городами (например, Москва → Новосибирск)

**Архитектура:**
- Использовать `russia-federal-cities-reference.json` для валидации городов
- Генерировать виртуальные авиамаршруты между федеральными городами (если нет реальных)
- Использовать реальные данные из OData, если они есть

---

### 7.3 Архитектура для смешанных маршрутов (air + ground + ferry)

**Смешанные маршруты:** маршруты, использующие несколько типов транспорта (например, Москва → Якутск → Нижний Бестях)

**Архитектура:**
- Разбивать маршрут на сегменты по типам транспорта
- Создавать рёбра пересадки между сегментами в одном городе
- Минимизировать общее время в пути, учитывая время пересадки
- Учитывать расписание переправ (ferry) для расчета времени ожидания

---

## 📊 Итоговый план реализации

### Этап 1: Исправление критических ошибок (Приоритет: ВЫСОКИЙ)
1. Исправить `extractCityFromStopName()` для обработки аэропортов
2. Добавить валидацию в `ODataSyncWorker`
3. Создать справочники аэропортов и пригородов
4. Обновить нормализацию городов

**Ожидаемый результат:** Якутск появляется в списке городов, "Туймада" исчезает, мусорные записи фильтруются

---

### Этап 2: Добавление федеральных городов (Приоритет: ВЫСОКИЙ)
1. Создать `russia-federal-cities-reference.json`
2. Создать `unified-cities-reference.json`
3. Обновить `VirtualEntitiesGeneratorWorker` для работы с объединенным справочником
4. Обновить `ensureYakutiaCitiesConnectivity()` для создания маршрутов из федеральных городов

**Ожидаемый результат:** Федеральные города доступны в системе, маршруты из них в Якутию строятся

---

### Этап 3: Добавление смешанных маршрутов (Приоритет: СРЕДНИЙ)
1. Расширить `stops.json` и `routes.json` для федеральных городов
2. Добавить остановки и маршруты для переправы (Якутск → Нижний Бестях)
3. Создать `AirRouteGeneratorWorker` для генерации авиамаршрутов
4. Обновить `GraphBuilderWorker` для поддержки пересадок

**Ожидаемый результат:** Смешанные маршруты (air + ground + ferry) строятся корректно

---

### Этап 4: Рефакторинг и оптимизация (Приоритет: СРЕДНИЙ)
1. Улучшить `VirtualEntitiesGeneratorWorker`
2. Улучшить `GraphBuilderWorker`
3. Создать инварианты системы
4. Добавить автоматические проверки

**Ожидаемый результат:** Система устойчива к ошибкам, повторные ошибки не возникают

---

## 🎯 Критерии успеха

1. ✅ Якутск появляется в списке доступных городов
2. ✅ "Туймада" не появляется как отдельный город
3. ✅ Мусорные записи фильтруются при загрузке данных
4. ✅ Федеральные города доступны в системе
5. ✅ Маршруты из федеральных городов в Якутию строятся
6. ✅ Смешанные маршруты (air + ground + ferry) строятся корректно
7. ✅ Переправа Якутск → Нижний Бестях работает как ferry-транспорт
8. ✅ Система устойчива к ошибкам, повторные ошибки не возникают

---

## 📝 Примечания

- Все изменения должны быть обратно совместимы с существующими данными
- Тесты должны покрывать все новые функции
- Документация должна быть обновлена после каждого этапа
- Изменения должны быть протестированы в dev-окружении перед деплоем в production

---

**Конец документа**






