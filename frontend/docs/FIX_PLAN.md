# Технический план исправления проблем Frontend проекта

**Дата создания:** 2024-01-XX  
**Основан на:** AUDIT_REPORT.md  
**Всего проблем:** 30 (5 критических, 9 важных, 16 улучшений)

---

## 📋 СОДЕРЖАНИЕ

1. [Критические проблемы (Блок 1-5)](#критические-проблемы)
2. [Важные проблемы (Блок 6-14)](#важные-проблемы)
3. [Улучшения (Блок 15-30)](#улучшения)
4. [Приоритизация и зависимости](#приоритизация-и-зависимости)

---

# КРИТИЧЕСКИЕ ПРОБЛЕМЫ

## БЛОК 1: Управление состоянием (State Management)

### Шаг 1.1: Установка и настройка React Query

**Цель:** Внедрить TanStack React Query для управления server state и кеширования API запросов.

**Файлы для изменения:**
- `package.json` - добавить зависимости
- `src/app/layout.tsx` - обернуть приложение в QueryClientProvider
- `src/shared/providers/query-provider.tsx` - создать новый файл провайдера
- `src/shared/utils/api.ts` - интегрировать с React Query

**Структуры и подходы:**
1. Установить `@tanstack/react-query` версии 5.x (последняя стабильная)
2. Создать `QueryClient` с настройками:
   - `defaultOptions.queries.staleTime: 5 * 60 * 1000` (5 минут)
   - `defaultOptions.queries.cacheTime: 10 * 60 * 1000` (10 минут)
   - `defaultOptions.queries.retry: 2` (2 попытки при ошибке)
   - `defaultOptions.queries.refetchOnWindowFocus: false` (не обновлять при фокусе)
3. Создать провайдер `QueryProvider` как Client Component
4. Обернуть `children` в `layout.tsx` в `QueryProvider`
5. Настроить DevTools для разработки (опционально, через `@tanstack/react-query-devtools`)

**Риски, которые устраняются:**
- Дублирование API запросов
- Отсутствие кеширования
- Нет автоматической инвалидации кеша
- Плохая производительность при частых запросах
- Сложность синхронизации состояния между компонентами

**Ожидаемый эффект:**
- Автоматическое кеширование всех API запросов
- Снижение нагрузки на backend
- Улучшение производительности UI
- Единая точка управления server state
- Возможность оптимистичных обновлений

**Детальные инструкции:**
1. В `package.json` добавить в `dependencies`: `"@tanstack/react-query": "^5.0.0"`
2. Создать файл `src/shared/providers/query-provider.tsx`:
   - Импортировать `QueryClient`, `QueryClientProvider` из `@tanstack/react-query`
   - Создать функцию `makeQueryClient()` для создания нового клиента
   - Создать компонент `QueryProvider` с использованием `useState` для хранения клиента
   - Экспортировать компонент
3. В `src/app/layout.tsx`:
   - Импортировать `QueryProvider`
   - Обернуть `{children}` в `<QueryProvider>`
   - Убедиться, что это Client Component (добавить `'use client'` если нужно)
4. В `src/shared/utils/api.ts`:
   - Оставить функцию `fetchApi` как есть (она будет использоваться в query functions)
   - Добавить комментарий о том, что функция используется с React Query

---

### Шаг 1.2: Миграция компонентов на React Query

**Цель:** Заменить все `useEffect + fetch` паттерны на `useQuery` hooks.

**Файлы для изменения:**
- `src/app/routes/page.tsx` - миграция поиска маршрутов
- `src/components/search-form/search-form.tsx` - миграция загрузки городов
- `src/components/city-autocomplete/city-autocomplete.tsx` - миграция загрузки городов
- `src/shared/utils/cities-api.ts` - адаптировать под React Query

**Структуры и подходы:**
1. Создать custom hooks для каждого типа запроса:
   - `src/shared/hooks/use-routes-search.ts` - для поиска маршрутов
   - `src/shared/hooks/use-cities.ts` - для загрузки городов
2. Использовать `useQuery` с правильными `queryKey`:
   - Для маршрутов: `['routes', 'search', from, to, date, passengers]`
   - Для городов: `['cities']` (статический ключ, так как данные общие)
3. Обработать состояния: `isLoading`, `isError`, `error`, `data`
4. Использовать `enabled` опцию для условных запросов
5. Настроить `refetchOnMount`, `refetchOnWindowFocus` по необходимости

**Риски, которые устраняются:**
- Дублирование запросов при ре-рендерах
- Отсутствие кеширования между компонентами
- Ручное управление loading/error состояниями
- Нет автоматической повторной загрузки при ошибках

**Ожидаемый эффект:**
- Единый источник данных для всех компонентов
- Автоматическое кеширование и синхронизация
- Упрощение кода компонентов
- Лучшая производительность

**Детальные инструкции:**

**Для `src/app/routes/page.tsx`:**
1. Создать файл `src/shared/hooks/use-routes-search.ts`:
   - Импортировать `useQuery` из `@tanstack/react-query`
   - Импортировать `fetchApi` и типы
   - Создать hook `useRoutesSearch` с параметрами: `from`, `to`, `date`, `passengers`
   - Использовать `useQuery` с:
     - `queryKey: ['routes', 'search', from, to, date, passengers]`
     - `queryFn: () => fetchApi<RouteSearchResult>(...)`
     - `enabled: Boolean(from && to)` - запрос только если есть обязательные параметры
     - `staleTime: 2 * 60 * 1000` - данные актуальны 2 минуты
   - Вернуть объект с `data`, `isLoading`, `error`, `refetch`
2. В `src/app/routes/page.tsx`:
   - Удалить все `useState` для `routes`, `alternatives`, `loading`, `error`
   - Удалить `useEffect` с запросом
   - Использовать `useRoutesSearch` hook
   - Заменить логику отображения на использование данных из hook
   - Обработать состояния `isLoading` и `error` из hook

**Для `src/components/city-autocomplete/city-autocomplete.tsx` и `src/components/search-form/search-form.tsx`:**
1. Создать файл `src/shared/hooks/use-cities.ts`:
   - Импортировать `useQuery` из `@tanstack/react-query`
   - Импортировать `fetchCities` из `cities-api.ts`
   - Создать hook `useCities`:
     - `queryKey: ['cities']`
     - `queryFn: () => fetchCities()`
     - `staleTime: 5 * 60 * 1000` - города меняются редко
     - `cacheTime: 10 * 60 * 1000` - долгое кеширование
   - Вернуть `{ cities: string[], isLoading: boolean, error: Error | null }`
2. В обоих компонентах:
   - Удалить `useState` для `availableCities` и `loading`
   - Удалить `useEffect` с загрузкой городов
   - Использовать `useCities` hook
   - Заменить все обращения к `availableCities` на `cities` из hook

**Для `src/shared/utils/cities-api.ts`:**
1. Удалить внутреннее кеширование (React Query будет кешировать)
2. Упростить функцию `fetchCities` - она должна только делать запрос
3. Оставить fallback логику, но упростить её
4. Функция `clearCitiesCache` больше не нужна (React Query управляет кешем)

---

### Шаг 1.3: Добавление Zustand для UI состояния (опционально)

**Цель:** Внедрить Zustand для управления UI состоянием (не server state).

**Файлы для изменения:**
- `package.json` - добавить зависимость
- `src/shared/stores/ui-store.ts` - создать новый файл
- `src/app/page.tsx` - использовать store для `activeSection`

**Структуры и подходы:**
1. Установить `zustand` версии 4.x
2. Создать store для UI состояния:
   - `activeSection` - текущая активная секция
   - `isTransitioning` - состояние перехода
3. Использовать store вместо локального `useState` в компонентах
4. Настроить persist middleware для сохранения состояния (опционально)

**Риски, которые устраняются:**
- Потеря состояния при навигации
- Дублирование состояния между компонентами
- Сложность синхронизации UI состояния

**Ожидаемый эффект:**
- Централизованное управление UI состоянием
- Сохранение состояния при навигации
- Упрощение логики компонентов

**Детальные инструкции:**
1. В `package.json` добавить: `"zustand": "^4.4.0"`
2. Создать `src/shared/stores/ui-store.ts`:
   - Импортировать `create` из `zustand`
   - Определить интерфейс состояния: `{ activeSection: ActiveSection, setActiveSection: (section: ActiveSection) => void }`
   - Создать store с помощью `create`
   - Экспортировать hook `useUIStore`
3. В `src/app/page.tsx`:
   - Заменить `useState` для `activeSection` на `useUIStore`
   - Использовать `setActiveSection` из store

**Примечание:** Этот шаг опционален и может быть выполнен позже, если React Query решит большинство проблем с состоянием.

---

## БЛОК 2: Обработка ошибок на уровне приложения

### Шаг 2.1: Создание Error Boundary компонента

**Цель:** Создать Error Boundary для перехвата и обработки ошибок React компонентов.

**Файлы для создания:**
- `src/components/error-boundary/error-boundary.tsx` - основной компонент
- `src/components/error-boundary/error-fallback.tsx` - компонент отображения ошибки
- `src/components/error-boundary/index.ts` - экспорты

**Структуры и подходы:**
1. Использовать классовый компонент (Error Boundary требует класс)
2. Реализовать методы `componentDidCatch` и `getDerivedStateFromError`
3. Создать fallback UI с информацией об ошибке
4. Добавить кнопку "Попробовать снова" для перезагрузки компонента
5. Логировать ошибки в консоль (в development) или отправлять в сервис логирования (в production)

**Риски, которые устраняются:**
- Полный краш приложения при ошибке в компоненте
- Белый экран вместо понятного сообщения
- Невозможность восстановления после ошибки
- Отсутствие отслеживания ошибок в production

**Ожидаемый эффект:**
- Приложение не падает полностью при ошибке
- Пользователь видит понятное сообщение
- Возможность восстановления без перезагрузки страницы
- Централизованное логирование ошибок

**Детальные инструкции:**
1. Создать директорию `src/components/error-boundary/`
2. Создать `error-boundary.tsx`:
   - Создать классовый компонент `ErrorBoundary` extends `React.Component`
   - Определить state: `{ hasError: boolean, error: Error | null, errorInfo: React.ErrorInfo | null }`
   - Реализовать `static getDerivedStateFromError(error: Error)`:
     - Вернуть `{ hasError: true, error }`
   - Реализовать `componentDidCatch(error: Error, errorInfo: React.ErrorInfo)`:
     - Сохранить `errorInfo` в state
     - Логировать ошибку (через logger utility)
   - В `render()`:
     - Если `hasError`, отрендерить `ErrorFallback`
     - Иначе отрендерить `this.props.children`
   - Добавить prop `fallback?: React.ComponentType<ErrorFallbackProps>` для кастомного fallback
3. Создать `error-fallback.tsx`:
   - Создать функциональный компонент `ErrorFallback`
   - Принять props: `error: Error, resetError: () => void`
   - Отобразить:
     - Заголовок "Что-то пошло не так"
     - Описание ошибки (только в development)
     - Кнопку "Попробовать снова" (вызывает `resetError`)
     - Кнопку "Вернуться на главную" (навигация через `useRouter`)
   - Использовать стили проекта (yakutia-card, yakutia-transition)
4. Создать `index.ts`:
   - Экспортировать `ErrorBoundary` и `ErrorFallback`

---

### Шаг 2.2: Интеграция Error Boundary в приложение

**Цель:** Обернуть приложение в Error Boundary для перехвата всех ошибок.

**Файлы для изменения:**
- `src/app/layout.tsx` - добавить ErrorBoundary
- `src/app/routes/page.tsx` - добавить ErrorBoundary для страницы маршрутов
- `src/app/routes/details/page.tsx` - добавить ErrorBoundary для деталей маршрута

**Структуры и подходы:**
1. Обернуть корневой `layout.tsx` в ErrorBoundary
2. Добавить ErrorBoundary на уровне страниц для более гранулярной обработки
3. Настроить разные fallback UI для разных типов ошибок (опционально)
4. Добавить логирование ошибок через утилиту logger

**Риски, которые устраняются:**
- Ошибки в любом компоненте приводят к падению всего приложения
- Нет возможности восстановления после ошибки
- Плохой UX при ошибках

**Ожидаемый эффект:**
- Изоляция ошибок на уровне страниц/компонентов
- Возможность восстановления без перезагрузки
- Лучший UX при ошибках

**Детальные инструкции:**
1. В `src/app/layout.tsx`:
   - Импортировать `ErrorBoundary` и `ErrorFallback`
   - Обернуть `{children}` в `<ErrorBoundary fallback={ErrorFallback}>`
   - Разместить ErrorBoundary внутри QueryProvider (чтобы ошибки React Query тоже перехватывались)
2. В `src/app/routes/page.tsx`:
   - Импортировать `ErrorBoundary`
   - Обернуть содержимое `RoutesContent` в ErrorBoundary
   - Это позволит изолировать ошибки на странице маршрутов
3. В `src/app/routes/details/page.tsx`:
   - Аналогично обернуть `RouteDetailsContent` в ErrorBoundary

---

### Шаг 2.3: Создание утилиты для логирования ошибок

**Цель:** Создать централизованную систему логирования ошибок.

**Файлы для создания:**
- `src/shared/utils/logger.ts` - утилита логирования

**Структуры и подходы:**
1. Создать объект `logger` с методами: `error`, `warn`, `info`, `debug`
2. В development: логировать в консоль
3. В production: отправлять в сервис логирования (Sentry, LogRocket и т.д.)
4. Добавить контекст к логам (URL, user agent, timestamp)
5. Заменить все `console.error` на `logger.error`

**Риски, которые устраняются:**
- Логи в production коде
- Нет централизованного логирования
- Сложность отслеживания ошибок в production

**Ожидаемый эффект:**
- Централизованное логирование
- Возможность отслеживания ошибок в production
- Чистый production код без console.log

**Детальные инструкции:**
1. Создать `src/shared/utils/logger.ts`:
   - Определить интерфейс `LogLevel` и `LogContext`
   - Создать функцию `createLogger()`:
     - Проверять `process.env.NODE_ENV`
     - В development: использовать `console.*`
     - В production: отправлять в сервис (пока можно оставить пустую реализацию)
   - Экспортировать объект `logger` с методами
2. Заменить все `console.error` в проекте:
   - `src/components/search-form/search-form.tsx:34`
   - `src/components/city-autocomplete/city-autocomplete.tsx:42`
   - `src/shared/utils/cities-api.ts:33`
3. Использовать `logger.error(message, error)` вместо `console.error`

---

## БЛОК 3: SSR-безопасность localStorage

### Шаг 3.1: Создание безопасной утилиты для localStorage

**Цель:** Создать SSR-безопасную обертку над localStorage.

**Файлы для создания:**
- `src/shared/utils/storage.ts` - утилита для работы с localStorage

**Структуры и подходы:**
1. Создать объект `safeLocalStorage` с методами: `getItem`, `setItem`, `removeItem`, `clear`
2. Проверять `typeof window !== 'undefined'` перед использованием
3. Обрабатывать ошибки (QuotaExceededError, SecurityError)
4. Возвращать `null` при ошибках вместо выбрасывания исключений
5. Добавить TypeScript типы для безопасности

**Риски, которые устраняются:**
- Ошибки при SSR (window не определен)
- Проблемы в приватном режиме браузера
- Ошибки при переполнении localStorage
- Проблемы с безопасностью (CORS, блокировка)

**Ожидаемый эффект:**
- Безопасная работа с localStorage в SSR
- Обработка всех edge cases
- Нет ошибок в консоли

**Детальные инструкции:**
1. Создать `src/shared/utils/storage.ts`:
   - Определить интерфейс `StorageAPI` с методами
   - Создать объект `safeLocalStorage`:
     - `getItem(key: string): string | null`:
       - Проверить `typeof window === 'undefined'` → вернуть `null`
       - Обернуть в try-catch
       - При ошибке вернуть `null`
     - `setItem(key: string, value: string): void`:
       - Проверить `typeof window === 'undefined'` → ничего не делать
       - Обернуть в try-catch
       - При ошибке игнорировать (можно логировать через logger)
     - `removeItem(key: string): void`:
       - Аналогично `setItem`
     - `clear(): void`:
       - Аналогично `setItem`
   - Экспортировать `safeLocalStorage`

---

### Шаг 3.2: Замена прямого использования localStorage

**Цель:** Заменить все прямые обращения к localStorage на безопасную утилиту.

**Файлы для изменения:**
- `src/app/routes/page.tsx` - заменить `localStorage.setItem`
- `src/app/routes/details/page.tsx` - заменить `localStorage.getItem`

**Структуры и подходы:**
1. Импортировать `safeLocalStorage` в компоненты
2. Заменить `localStorage.getItem` на `safeLocalStorage.getItem`
3. Заменить `localStorage.setItem` на `safeLocalStorage.setItem`
4. Убедиться, что обработка `null` значений корректна

**Риски, которые устраняются:**
- Ошибки при SSR
- Проблемы в приватном режиме
- Некорректная обработка отсутствующих данных

**Ожидаемый эффект:**
- SSR-безопасный код
- Корректная работа во всех браузерах
- Нет runtime ошибок

**Детальные инструкции:**
1. В `src/app/routes/page.tsx`:
   - Импортировать `safeLocalStorage` из `@/shared/utils/storage`
   - Найти строку `localStorage.setItem(...)` (строка 136)
   - Заменить на `safeLocalStorage.setItem(...)`
   - Убедиться, что JSON.stringify не вызовет ошибку
2. В `src/app/routes/details/page.tsx`:
   - Импортировать `safeLocalStorage`
   - Найти строку `localStorage.getItem(...)` (строка 36)
   - Заменить на `safeLocalStorage.getItem(...)`
   - Проверить обработку `null` значения (уже есть проверка `if (!storedData)`)

---

## БЛОК 4: Тестирование

### Шаг 4.1: Настройка тестовой инфраструктуры

**Цель:** Установить и настроить все необходимые инструменты для тестирования.

**Файлы для изменения:**
- `package.json` - добавить dev dependencies и scripts
- `jest.config.js` - создать конфигурацию Jest
- `playwright.config.ts` - создать конфигурацию Playwright
- `.github/workflows/test.yml` - создать CI workflow (опционально)

**Структуры и подходы:**
1. Установить Jest и React Testing Library для unit/integration тестов
2. Установить Playwright для E2E тестов
3. Настроить Jest для работы с TypeScript и Next.js
4. Настроить Playwright для тестирования Next.js приложения
5. Создать структуру директорий для тестов

**Риски, которые устраняются:**
- Невозможность гарантировать работоспособность кода
- Высокий риск регрессий
- Сложность рефакторинга без тестов

**Ожидаемый эффект:**
- Возможность писать и запускать тесты
- Автоматическая проверка кода
- Уверенность в работоспособности после изменений

**Детальные инструкции:**
1. В `package.json` добавить в `devDependencies`:
   - `"jest": "^29.7.0"`
   - `"jest-environment-jsdom": "^29.7.0"`
   - `"@testing-library/react": "^14.1.2"`
   - `"@testing-library/jest-dom": "^6.1.5"`
   - `"@testing-library/user-event": "^14.5.1"`
   - `"@playwright/test": "^1.40.0"`
   - `"@types/jest": "^29.5.11"`
2. В `package.json` добавить scripts:
   - `"test": "jest"`
   - `"test:watch": "jest --watch"`
   - `"test:coverage": "jest --coverage"`
   - `"test:e2e": "playwright test"`
   - `"test:e2e:ui": "playwright test --ui"`
3. Создать `jest.config.js`:
   - Настроить `testEnvironment: 'jsdom'`
   - Настроить `moduleNameMapper` для алиасов (`@/*`)
   - Настроить `transform` для TypeScript
   - Настроить `setupFilesAfterEnv` для `@testing-library/jest-dom`
   - Настроить `testMatch` для поиска тестов
   - Настроить `collectCoverageFrom` для сбора покрытия
4. Создать `playwright.config.ts`:
   - Настроить `use.baseURL` на `http://localhost:3000`
   - Настроить `projects` для разных браузеров
   - Настроить `webServer` для запуска Next.js dev server
5. Создать структуру директорий:
   - `src/__tests__/components/` - unit тесты компонентов
   - `src/__tests__/utils/` - unit тесты утилит
   - `src/__tests__/integration/` - integration тесты
   - `e2e/` - E2E тесты Playwright

---

### Шаг 4.2: Написание unit тестов для утилит

**Цель:** Покрыть тестами критически важные утилиты.

**Файлы для создания:**
- `src/__tests__/utils/storage.test.ts` - тесты для safeLocalStorage
- `src/__tests__/utils/api.test.ts` - тесты для fetchApi
- `src/__tests__/utils/format.test.ts` - тесты для функций форматирования (после их создания)

**Структуры и подходы:**
1. Написать тесты для `safeLocalStorage`:
   - Тест на SSR безопасность (window undefined)
   - Тест на нормальную работу (getItem, setItem)
   - Тест на обработку ошибок (QuotaExceededError)
   - Тест на приватный режим (SecurityError)
2. Написать тесты для `fetchApi`:
   - Мокировать `fetch`
   - Тест на успешный запрос
   - Тест на ошибку сети
   - Тест на ошибку API (4xx, 5xx)
   - Тест на парсинг ошибок из ответа

**Риски, которые устраняются:**
- Ошибки в утилитах не обнаруживаются
- Регрессии при изменении утилит
- Нет документации через тесты

**Ожидаемый эффект:**
- Уверенность в работоспособности утилит
- Защита от регрессий
- Примеры использования через тесты

**Детальные инструкции:**
1. Создать `src/__tests__/utils/storage.test.ts`:
   - Импортировать `safeLocalStorage`
   - Описать блок `describe('safeLocalStorage')`
   - Тест `'should return null when window is undefined'`:
     - Мокировать `window` как `undefined`
     - Проверить, что `getItem` возвращает `null`
   - Тест `'should get and set items correctly'`:
     - Использовать реальный localStorage
     - Установить значение, получить, проверить
   - Тест `'should handle QuotaExceededError'`:
     - Мокировать `localStorage.setItem` чтобы выбрасывать ошибку
     - Проверить, что ошибка не пробрасывается
2. Создать `src/__tests__/utils/api.test.ts`:
   - Импортировать `fetchApi`
   - Мокировать глобальный `fetch`
   - Тест `'should return data on successful request'`:
     - Мокировать успешный ответ
     - Проверить, что данные возвращаются
   - Тест `'should throw error on network failure'`:
     - Мокировать ошибку сети
     - Проверить, что выбрасывается правильная ошибка
   - Тест `'should parse error from API response'`:
     - Мокировать ответ с ошибкой (4xx)
     - Проверить, что ошибка парсится из JSON

---

### Шаг 4.3: Написание тестов для компонентов

**Цель:** Покрыть тестами критически важные компоненты.

**Файлы для создания:**
- `src/__tests__/components/search-form.test.tsx` - тесты SearchForm
- `src/__tests__/components/city-autocomplete.test.tsx` - тесты CityAutocomplete
- `src/__tests__/components/error-boundary.test.tsx` - тесты ErrorBoundary

**Структуры и подходы:**
1. Использовать React Testing Library для рендеринга компонентов
2. Использовать `user-event` для симуляции пользовательских действий
3. Мокировать зависимости (API, hooks)
4. Тестировать:
   - Рендеринг компонента
   - Взаимодействие пользователя
   - Обработку ошибок
   - Валидацию форм

**Риски, которые устраняются:**
- Ошибки в компонентах не обнаруживаются
- Регрессии при изменении компонентов
- Нет документации поведения компонентов

**Ожидаемый эффект:**
- Уверенность в работоспособности компонентов
- Защита от регрессий
- Примеры использования компонентов

**Детальные инструкции:**
1. Создать `src/__tests__/components/search-form.test.tsx`:
   - Импортировать `SearchForm`, `render`, `screen`, `userEvent`
   - Мокировать `useRouter` из `next/navigation`
   - Мокировать `useCities` hook
   - Тест `'should render search form'`:
     - Рендерить компонент
     - Проверить наличие полей формы
   - Тест `'should validate required fields'`:
     - Попытаться отправить пустую форму
     - Проверить наличие ошибок валидации
   - Тест `'should submit form with valid data'`:
     - Заполнить форму
     - Отправить форму
     - Проверить вызов router.push с правильными параметрами
2. Создать `src/__tests__/components/city-autocomplete.test.tsx`:
   - Аналогично, но тестировать автокомплит функциональность
3. Создать `src/__tests__/components/error-boundary.test.tsx`:
   - Создать компонент, который выбрасывает ошибку
   - Обернуть в ErrorBoundary
   - Проверить, что ErrorFallback отображается

---

### Шаг 4.4: Написание E2E тестов

**Цель:** Покрыть критически важные пользовательские сценарии E2E тестами.

**Файлы для создания:**
- `e2e/routes-search.spec.ts` - E2E тест поиска маршрутов
- `e2e/navigation.spec.ts` - E2E тест навигации

**Структуры и подходы:**
1. Использовать Playwright для E2E тестов
2. Тестировать полные пользовательские сценарии:
   - Поиск маршрутов
   - Просмотр деталей маршрута
   - Навигация между секциями
3. Мокировать API ответы или использовать тестовый backend
4. Использовать `page.getByRole`, `page.getByLabel` для селекторов

**Риски, которые устраняются:**
- Интеграционные проблемы не обнаруживаются
- Регрессии в пользовательских сценариях
- Проблемы с навигацией

**Ожидаемый эффект:**
- Уверенность в работоспособности полных сценариев
- Защита от регрессий на уровне приложения
- Автоматическая проверка перед деплоем

**Детальные инструкции:**
1. Создать `e2e/routes-search.spec.ts`:
   - Импортировать `test`, `expect` из `@playwright/test`
   - Тест `'should search for routes'`:
     - Перейти на главную страницу
     - Заполнить форму поиска (from, to)
     - Нажать кнопку поиска
     - Проверить переход на страницу результатов
     - Проверить наличие результатов
   - Тест `'should display error on invalid search'`:
     - Попытаться искать с одинаковыми городами
     - Проверить наличие ошибки валидации
2. Создать `e2e/navigation.spec.ts`:
   - Тест `'should navigate between sections'`:
     - Перейти на главную
     - Кликнуть на таб "Отели"
     - Проверить отображение секции отелей
     - Аналогично для других секций

---

## БЛОК 5: Типизация

### Шаг 5.1: Замена `any` на конкретные типы

**Цель:** Устранить использование `any` типа для строгой типизации.

**Файлы для изменения:**
- `src/shared/types/index.ts` - заменить `any` в `ApiResponse`

**Структуры и подходы:**
1. Определить конкретный тип для `details` в `ApiResponse`
2. Использовать union type вместо `any`
3. Добавить JSDoc комментарии для типов

**Риски, которые устраняются:**
- Потеря безопасности типов
- Потенциальные runtime ошибки
- Плохое автодополнение в IDE

**Ожидаемый эффект:**
- Строгая типизация
- Лучшее автодополнение
- Раннее обнаружение ошибок

**Детальные инструкции:**
1. В `src/shared/types/index.ts`:
   - Найти строку `details?: Record<string, any>`
   - Заменить на `details?: Record<string, string | number | boolean | null>`
   - Или создать отдельный тип:
     ```typescript
     type ApiErrorDetails = string | number | boolean | null | ApiErrorDetails[] | { [key: string]: ApiErrorDetails }
     details?: Record<string, ApiErrorDetails>
     ```
   - Добавить JSDoc комментарий к типу

---

### Шаг 5.2: Замена `unknown` на конкретные типы

**Цель:** Определить конкретные типы для `unknown` значений.

**Файлы для изменения:**
- `src/shared/types/route-adapter.ts` - заменить `unknown` в `IRouteBuilderResult` и `RouteDetailsData`

**Структуры и подходы:**
1. Определить интерфейс `MLData` для машинного обучения данных
2. Определить интерфейс `OccupancyData` для данных занятости
3. Заменить `unknown` на конкретные типы
4. Добавить JSDoc комментарии

**Риски, которые устраняются:**
- Потеря безопасности типов
- Потенциальные runtime ошибки
- Неясная структура данных

**Ожидаемый эффект:**
- Строгая типизация
- Понятная структура данных
- Лучшее автодополнение

**Детальные инструкции:**
1. В `src/shared/types/route-adapter.ts`:
   - Определить интерфейс `MLData`:
     ```typescript
     interface MLData {
       predictions?: {
         delay?: number
         price?: number
       }
       confidence?: number
       modelVersion?: string
     }
     ```
   - Определить интерфейс `OccupancyData`:
     ```typescript
     interface OccupancyData {
       segmentId: string
       occupied: number
       total: number
       percentage: number
     }
     ```
   - Заменить `mlData?: unknown` на `mlData?: MLData`
   - Заменить `occupancy: Array<unknown>` на `occupancy: Array<OccupancyData>`
   - Добавить JSDoc комментарии к интерфейсам
2. В `src/components/route-details/route-details-view.tsx`:
   - Обновить локальный интерфейс `RouteDetailsData` (если он дублируется)
   - Импортировать типы из `route-adapter.ts`

---

# ВАЖНЫЕ ПРОБЛЕМЫ

## БЛОК 6: Валидация через Zod

### Шаг 6.1: Установка и настройка Zod

**Цель:** Установить Zod и создать базовые схемы валидации.

**Файлы для изменения:**
- `package.json` - добавить зависимость
- `src/shared/schemas/route.schema.ts` - создать схемы для маршрутов
- `src/shared/schemas/cities.schema.ts` - создать схемы для городов

**Детальные инструкции:**
1. В `package.json` добавить: `"zod": "^3.22.4"`
2. Создать `src/shared/schemas/route.schema.ts`:
   - Импортировать `z` из `zod`
   - Создать `RouteSearchParamsSchema`:
     - `from: z.string().min(1, 'Город отправления обязателен')`
     - `to: z.string().min(1, 'Город назначения обязателен')`
     - `date: z.string().optional()`
     - `passengers: z.string().regex(/^[1-9]$/, 'От 1 до 9 пассажиров').optional()`
   - Экспортировать схемы
3. Использовать схемы в `SearchForm` для валидации перед отправкой

---

## БЛОК 7: Реорганизация структуры (Feature-Based)

### Шаг 7.1: Создание структуры модулей

**Цель:** Создать правильную структуру модулей согласно Feature-Based архитектуре.

**Детальные инструкции:**
1. Создать структуру директорий:
   - `src/modules/routes/features/route-search/`
   - `src/modules/routes/features/route-list/`
   - `src/modules/routes/features/route-details/`
   - `src/modules/routes/domain/`
   - Аналогично для `hotels`, `transport`, `services`
2. Переместить компоненты из `components/` в соответствующие модули
3. Обновить все импорты
4. Оставить в `components/` только общие UI компоненты (Header, Footer, Button и т.д.)

---

## БЛОК 8: React Context для глобального состояния

### Шаг 8.1: Создание контекстов

**Цель:** Создать React Context для глобального состояния приложения.

**Детальные инструкции:**
1. Создать `src/shared/contexts/auth-context.tsx`:
   - Создать `AuthContext` с `createContext`
   - Создать `AuthProvider` компонент
   - Управлять состоянием авторизации
2. Создать `src/shared/contexts/theme-context.tsx`:
   - Аналогично для темы
3. Обернуть приложение в провайдеры в `layout.tsx`

---

## БЛОК 9: Оптимизация изображений Next.js

### Шаг 9.1: Настройка next/image

**Цель:** Включить оптимизацию изображений Next.js.

**Детальные инструкции:**
1. В `next.config.js`:
   - Удалить `images: { unoptimized: true }`
   - Добавить `remotePatterns` для MinIO
2. Заменить все `<img>` на `<Image>` из `next/image`
3. Добавить `width`, `height` или `fill` prop

---

## БЛОК 10: Улучшение ESLint конфигурации

### Шаг 10.1: Расширение ESLint правил

**Детальные инструкции:**
1. В `package.json` добавить dev dependencies:
   - `"@typescript-eslint/parser"`
   - `"@typescript-eslint/eslint-plugin"`
   - `"eslint-plugin-react-hooks"`
   - `"eslint-plugin-jsx-a11y"`
2. В `.eslintrc.json` добавить extends и rules
3. Создать `.eslintignore` для исключения файлов

---

# УЛУЧШЕНИЯ

## БЛОК 11-30: Мелкие улучшения

**Краткие инструкции для каждого:**

### 11. Обновление зависимостей
- Запустить `npm outdated`
- Обновить Next.js, React до последних стабильных версий
- Проверить breaking changes

### 12. Создание .env.example
- Создать файл с примерами переменных окружения
- Добавить комментарии

### 13. Добавление Prettier
- Установить Prettier
- Создать `.prettierrc`
- Добавить в scripts

### 14. Добавление Husky
- Установить Husky
- Настроить pre-commit hook
- Добавить lint-staged

### 15. Вынос функций форматирования
- Создать `src/shared/utils/format.ts`
- Переместить `formatDuration`, `formatTime`, `formatDate`
- Обновить импорты

### 16. Мемоизация компонентов
- Обернуть presentational компоненты в `React.memo`
- Добавить `useMemo` для дорогих вычислений
- Добавить `useCallback` для функций

### 17. Упрощение обработки клавиатуры
- Упростить логику в `search-form.tsx`
- Использовать стандартные паттерны React

### 18. Проверка индексации экспортов
- Убедиться, что все компоненты имеют `index.ts`
- Использовать barrel exports

### 19. Добавление JSDoc
- Добавить JSDoc ко всем публичным функциям и компонентам
- Следовать TypeDoc формату

### 20. Улучшение accessibility
- Добавить `aria-label` ко всем интерактивным элементам
- Проверить клавиатурную навигацию
- Использовать семантические HTML теги

### 21. Оптимизация производительности
- Использовать `dynamic` imports для больших компонентов
- Добавить lazy loading
- Рассмотреть виртуализацию для списков

### 22. Добавление SEO мета-тегов
- Расширить `metadata` в `layout.tsx`
- Добавить Open Graph теги
- Добавить Twitter Card теги

### 23. Оптимизация CSS переменных
- Настроить Tailwind для использования CSS переменных
- Заменить inline styles на классы

### 24. Обработка offline состояния
- Добавить проверку `navigator.onLine`
- Показывать уведомление при offline
- Рассмотреть Service Worker

### 25. Мониторинг производительности
- Настроить `next/web-vitals`
- Отправлять метрики в аналитику

---

# ПРИОРИТИЗАЦИЯ И ЗАВИСИМОСТИ

## Критический путь (выполнить строго последовательно):

1. **БЛОК 1.1** - Установка React Query (основа для всего)
2. **БЛОК 1.2** - Миграция на React Query (зависит от 1.1)
3. **БЛОК 2.1** - Создание Error Boundary (независимо, но важно)
4. **БЛОК 2.2** - Интеграция Error Boundary (зависит от 2.1)
5. **БЛОК 3.1** - Создание safeLocalStorage (независимо)
6. **БЛОК 3.2** - Замена localStorage (зависит от 3.1)
7. **БЛОК 5.1-5.2** - Типизация (можно параллельно с другими)

## Можно выполнять параллельно:

- **БЛОК 2.3** (logger) - параллельно с 2.1-2.2
- **БЛОК 4.1-4.4** (тесты) - параллельно со всеми блоками
- **БЛОК 6** (Zod) - параллельно с блоками 1-3
- **БЛОК 8** (Context) - параллельно с блоком 1

## Можно отложить (низкий приоритет):

- **БЛОК 7** (реорганизация структуры) - большой рефакторинг, лучше после стабилизации
- **БЛОК 9** (изображения) - не критично для функциональности
- **БЛОК 10-30** (улучшения) - можно делать постепенно

## Рекомендуемый порядок выполнения:

### Фаза 1 (Критично - 1-2 недели):
1. БЛОК 1: Управление состоянием (1.1, 1.2)
2. БЛОК 2: Обработка ошибок (2.1, 2.2, 2.3)
3. БЛОК 3: SSR-безопасность (3.1, 3.2)
4. БЛОК 5: Типизация (5.1, 5.2)

### Фаза 2 (Важно - 1-2 недели):
5. БЛОК 4: Тестирование (4.1, 4.2, 4.3, 4.4)
6. БЛОК 6: Валидация Zod
7. БЛОК 8: React Context
8. БЛОК 9: Оптимизация изображений
9. БЛОК 10: ESLint

### Фаза 3 (Улучшения - постепенно):
10. БЛОК 7: Реорганизация структуры
11. БЛОК 11-30: Мелкие улучшения

---

## Итоговая оценка времени:

- **Фаза 1:** 1-2 недели (критично)
- **Фаза 2:** 1-2 недели (важно)
- **Фаза 3:** 2-4 недели (улучшения, можно делать постепенно)

**Общее время:** 4-8 недель при работе одного разработчика полный рабочий день.

---

## Зависимости между блоками:

```
БЛОК 1.1 → БЛОК 1.2
БЛОК 2.1 → БЛОК 2.2
БЛОК 3.1 → БЛОК 3.2
БЛОК 4.1 → БЛОК 4.2 → БЛОК 4.3 → БЛОК 4.4
БЛОК 1.2 → БЛОК 6 (можно использовать Zod для валидации query params)
БЛОК 2.3 → используется в БЛОК 2.1, 2.2
```

Все остальные блоки независимы и могут выполняться параллельно.

