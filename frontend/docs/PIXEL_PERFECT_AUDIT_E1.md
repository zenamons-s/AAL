# Pixel-Perfect Визуальный Аудит (Этап E1)

## Дата: 2025-01-27

## Цель
Полный детальный визуальный аудит всего фронтенд-проекта для выявления всех визуальных несоответствий, перекосов, проблем в spacing, нарушений baseline и недочётов в UI-KIT и Motion System.

---

## 🔵 1. Сетки, выравнивание, вертикальный ритм

### Проблема 1.1: Несоответствие высоты header и footer
**Описание**: Header использует `h-12` (48px), но нет единого стандарта для высоты header/footer элементов.

**Файл**: `frontend/src/shared/ui/header/header.tsx`  
**Локация**: Строка 18: `<div className="flex items-center justify-between h-12">`

**Причина**: Хардкодное значение `h-12` не использует токен Theme System v2. Должно быть `h-lg` или создан токен `--height-header`.

**Критичность**: Средняя

**Рекомендуемое исправление**: Заменить `h-12` на токен из Theme System v2 или создать семантический токен `--height-header: 48px` и использовать его.

---

### Проблема 1.2: Несоответствие размеров логотипа между header и footer
**Описание**: Логотип в header имеет размер `w-8 h-8` (32px), а в footer — `w-7 h-7` (28px). Это создает визуальную несогласованность.

**Файл**: 
- `frontend/src/shared/ui/header/header.tsx` (строка 21)
- `frontend/src/shared/ui/footer/footer.tsx` (строка 21)

**Локация**: 
- Header: `<div className="w-8 h-8 bg-header-hover...">`
- Footer: `<div className="w-7 h-7 bg-header-hover...">`

**Причина**: Разные размеры для одного и того же элемента в разных местах нарушают визуальную консистентность.

**Критичность**: Высокая

**Рекомендуемое исправление**: Унифицировать размер логотипа. Использовать один размер (например, `w-8 h-8`) в обоих местах или создать токен `--size-logo`.

---

### Проблема 1.3: Синтаксическая ошибка в routes/page.tsx
**Описание**: Отсутствует закрывающий тег `>` в строке 165, что может привести к ошибке рендеринга.

**Файл**: `frontend/src/app/routes/page.tsx`  
**Локация**: Строка 165: `<div key={route.routeId} className="card card-hover p-lg transition-fast"`

**Причина**: Синтаксическая ошибка в JSX - отсутствует `>` после `transition-fast"`.

**Критичность**: Критичная

**Рекомендуемое исправление**: Добавить закрывающий тег `>` после `transition-fast"`. Должно быть: `<div key={route.routeId} className="card card-hover p-lg transition-fast">`

---

## 🔵 2. Расстояния между элементами (spacing consistency)

### Проблема 2.1: Несоответствие padding в карточках
**Описание**: Большинство карточек используют `p-lg`, но некоторые используют `p-2xl` (routes/page.tsx, строка 269) и `p-xl` (error-fallback.tsx, строка 21).

**Файл**: 
- `frontend/src/app/routes/page.tsx` (строка 269)
- `frontend/src/shared/ui/error-boundary/error-fallback.tsx` (строка 21)

**Локация**: 
- `className="card p-2xl text-center"`
- `className="card p-xl max-w-2xl w-full text-center"`

**Причина**: Несогласованное использование spacing-токенов нарушает визуальную ритмику.

**Критичность**: Средняя

**Рекомендуемое исправление**: Унифицировать padding карточек. Использовать `p-lg` для стандартных карточек, `p-xl` только для специальных случаев (например, error-fallback), и убрать `p-2xl`.

---

### Проблема 2.2: Хардкодные spacing значения в search-form
**Описание**: Используются хардкодные значения `pr-3xl`, `px-2xl`, `py-sm` вместо системных токенов.

**Файл**: `frontend/src/modules/routes/features/route-search/ui/search-form.tsx`  
**Локация**: 
- Строка 246: `className="input pr-3xl"`
- Строка 310: `className="btn-primary w-full md:w-auto px-2xl py-sm"`

**Причина**: Хардкодные значения не соответствуют Theme System v2 spacing-токенам.

**Критичность**: Низкая

**Рекомендуемое исправление**: Заменить на системные токены: `pr-3xl` → `pr-3xl` (если токен существует) или создать токен, `px-2xl` → `px-2xl` (если токен существует), `py-sm` → `py-sm` (корректно).

---

## 🔵 3. Иконки

### Проблема 3.1: Несоответствие размеров иконок
**Описание**: Иконки в header и footer используют `w-3.5 h-3.5` (14px), а в navigation-tabs — `w-4 h-4` (16px). Это создает визуальную несогласованность.

**Файл**: 
- `frontend/src/shared/ui/header/header.tsx` (строки 36, 44)
- `frontend/src/shared/ui/footer/footer.tsx` (строки 64, 73, 82)
- `frontend/src/shared/ui/navigation-tabs/navigation-tabs.tsx` (строка 47)

**Локация**: 
- Header/Footer: `<ProfileIcon className="w-3.5 h-3.5" ... />`
- Navigation: `<Icon className="w-4 h-4 transition-fast" ... />`

**Причина**: Разные размеры для иконок одного типа нарушают визуальную консистентность.

**Критичность**: Высокая

**Рекомендуемое исправление**: Унифицировать размеры иконок. Использовать один размер (например, `w-4 h-4`) для всех иконок навигации или создать токен `--size-icon-nav`.

---

### Проблема 3.2: Несоответствие размеров иконок в assistant-button
**Описание**: Assistant button использует `w-14 h-14` (56px) для контейнера и `w-8 h-8` (32px) для иконки внутри.

**Файл**: `frontend/src/shared/ui/assistant-button/assistant-button.tsx`  
**Локация**: 
- Строка 8: `className="... w-14 h-14 ..."`
- Строка 12: `className="w-8 h-8"`

**Причина**: Хардкодные значения не используют токены Theme System v2.

**Критичность**: Низкая

**Рекомендуемое исправление**: Заменить на токены или создать семантические токены `--size-assistant-button` и `--size-assistant-icon`.

---

### Проблема 3.3: Несоответствие размеров кнопок пассажиров
**Описание**: Кнопки увеличения/уменьшения пассажиров используют `w-6 h-4` (24px × 16px), что не соответствует стандартным размерам кнопок.

**Файл**: `frontend/src/modules/routes/features/route-search/ui/search-form.tsx`  
**Локация**: Строки 260, 277: `className="w-6 h-4 flex items-center justify-center ..."`

**Причина**: Нестандартные размеры кнопок нарушают визуальную консистентность.

**Критичность**: Средняя

**Рекомендуемое исправление**: Использовать стандартные размеры кнопок из UI-KIT или создать токен для кнопок-стрелок.

---

## 🔵 4. Типографика (вся система)

### Проблема 4.1: Использование text-base вместо токенов
**Описание**: В header используется `text-base` для логотипа, что не соответствует системе токенов.

**Файл**: `frontend/src/shared/ui/header/header.tsx`  
**Локация**: Строка 22: `<span className="text-header-text text-base font-medium">Т</span>`

**Причина**: `text-base` не является токеном Theme System v2. Должен использоваться `text-md` или `text-sm`.

**Критичность**: Низкая

**Рекомендуемое исправление**: Заменить `text-base` на `text-md` или `text-sm` в зависимости от дизайна.

---

### Проблема 4.2: Использование tracking-tight без токена
**Описание**: В header и footer используется `tracking-tight`, но нет проверки соответствия токену.

**Файл**: 
- `frontend/src/shared/ui/header/header.tsx` (строки 24, 37, 45)
- `frontend/src/shared/ui/footer/footer.tsx` (строки 24, 33, 42, 49, 84)

**Локация**: Множественные использования `tracking-tight`

**Причина**: `tracking-tight` может не соответствовать системе типографики Theme System v2.

**Критичность**: Низкая

**Рекомендуемое исправление**: Проверить соответствие `tracking-tight` системе типографики или заменить на токен.

---

## 🔵 5. Карточки и контейнеры

### Проблема 5.1: Несоответствие padding в карточках (повтор)
**Описание**: См. Проблема 2.1

---

### Проблема 5.2: Отсутствие единого стиля для card-hover
**Описание**: Не все карточки используют `card-hover` класс для hover-эффектов.

**Файл**: Множественные файлы с карточками

**Локация**: Некоторые карточки используют `card-hover`, другие — нет.

**Причина**: Несогласованное применение hover-эффектов.

**Критичность**: Низкая

**Рекомендуемое исправление**: Унифицировать использование `card-hover` для всех интерактивных карточек.

---

## 🔵 6. Кнопки (UI-KIT)

### Проблема 6.1: Дополнительные классы поверх btn-primary
**Описание**: В некоторых местах к `btn-primary` добавляются дополнительные классы (`rounded-sm`, `font-medium`), которые могут конфликтовать с UI-KIT.

**Файл**: 
- `frontend/src/app/routes/page.tsx` (строки 258, 388)
- `frontend/src/modules/routes/features/route-search/ui/search-form.tsx` (строка 310)

**Локация**: 
- `className="btn-primary px-xl py-sm rounded-sm font-medium transition-fast"`
- `className="btn-primary w-full md:w-auto px-2xl py-sm"`

**Причина**: Дополнительные классы могут переопределять стили UI-KIT.

**Критичность**: Средняя

**Рекомендуемое исправление**: Убрать конфликтующие классы (`rounded-sm`, `font-medium`) из `btn-primary`, так как они уже определены в UI-KIT.

---

## 🔵 7. Формы, инпуты, dropdown и autocomplete

### Проблема 7.1: Хардкодный padding-right в input
**Описание**: В search-form используется `pr-3xl` для input с кнопками пассажиров.

**Файл**: `frontend/src/modules/routes/features/route-search/ui/search-form.tsx`  
**Локация**: Строка 246: `className="input pr-3xl"`

**Причина**: Хардкодное значение не использует токен Theme System v2.

**Критичность**: Низкая

**Рекомендуемое исправление**: Заменить `pr-3xl` на токен или создать семантический токен для input с иконками/кнопками.

---

### Проблема 7.2: Несоответствие высоты input
**Описание**: В globals.css определена высота `height: 38px` для всех input, но это хардкодное значение.

**Файл**: `frontend/src/app/globals.css`  
**Локация**: Строка 462: `height: 38px;`

**Причина**: Хардкодное значение не использует токен Theme System v2.

**Критичность**: Низкая

**Рекомендуемое исправление**: Создать токен `--height-input: 38px` и использовать его.

---

## 🔵 8. Табы

### Проблема 8.1: Несоответствие размеров иконок в табах
**Описание**: См. Проблема 3.1

---

## 🔵 9. Header / Footer / Layout

### Проблема 9.1: Несоответствие размеров логотипа
**Описание**: См. Проблема 1.2

---

### Проблема 9.2: Несоответствие размеров иконок
**Описание**: См. Проблема 3.1

---

## 🔵 10. Motion System (визуальная часть)

### Проблема 10.1: Смешение transition классов
**Описание**: В некоторых местах используется `transition-fast`, в других — `transition-opacity`, что может создавать несогласованные анимации.

**Файл**: Множественные файлы

**Локация**: Различные использования transition классов

**Причина**: Несогласованное применение Motion System классов.

**Критичность**: Низкая

**Рекомендуемое исправление**: Унифицировать использование transition классов согласно Motion System.

---

## Дополнительные находки

### Положительные моменты:
1. ✅ Корректное использование токенов `h-lg`, `w-lg` для spinner в routes/page.tsx
2. ✅ Единообразное использование `border-l-primary` и `border-l-accent` утилит
3. ✅ Правильное применение Motion System классов (`transition-fast`, `transition-opacity`)
4. ✅ Консистентное использование `card` и `card-hover` классов

---

## Итоговая статистика

### Критичность проблем:
- **Критичные**: 1
- **Высокие**: 3
- **Средние**: 4
- **Низкие**: 8

### Всего найдено проблем: 16

### Категории проблем:
- **Сетки и выравнивание**: 3
- **Spacing**: 2
- **Иконки**: 3
- **Типографика**: 2
- **Карточки**: 2
- **Кнопки**: 1
- **Формы**: 2
- **Табы**: 1 (дубликат)
- **Header/Footer**: 2 (дубликаты)
- **Motion System**: 1

---

## Рекомендации

1. **Унифицировать размеры**: Создать семантические токены для всех размеров (логотип, иконки, кнопки, input).
2. **Исправить синтаксическую ошибку**: Немедленно исправить отсутствующий закрывающий тег в routes/page.tsx.
3. **Унифицировать spacing**: Привести все padding/margin к системным токенам Theme System v2.
4. **Проверить UI-KIT**: Убедиться, что все компоненты используют только UI-KIT классы без дополнительных переопределений.
5. **Создать токены для размеров**: Добавить в Theme System v2 токены для всех размеров элементов.

---

## Подпись

**Не исправлял, только зафиксировал**

Аудит выполнен в режиме только проверки, без автоматических исправлений. Все найденные проблемы требуют ручного исправления с учетом контекста и дизайн-системы.

