# Финальный аудит модуля поиска маршрутов после исправлений

## Резюме

**Статус:** Модуль поиска маршрутов после внесённых исправлений работает стабильно, но обнаружены **2 критические проблемы**, которые могут привести к ошибкам:

1. **Проблема с `dataMode` и `dataQuality`**: В `page.tsx` используются поля, которые были удалены из `UseRoutesSearchResult`, что вызовет ошибку TypeScript/рантайма.
2. **Параметр `date` в `queryKey`**: Пустая строка `date` попадает в `queryKey`, что может вызвать проблемы с кешированием React Query, но **не блокирует выполнение запроса**.

**Вывод:** Модуль функционально работает корректно, но требует исправления использования удалённых полей в `page.tsx`.

---

## Шаг 1. Проверка движения параметров из URL

### Путь параметров: `page.tsx` → `useRoutesSearch` → `queryKey` → `params` → `fetchApi`

### Файл: `frontend/src/app/routes/page.tsx`

**Строки 27-30:**
```typescript
const from = searchParams.get('from') || ''
const to = searchParams.get('to') || ''
const date = searchParams.get('date') || ''
const passengers = searchParams.get('passengers') || '1'
```

**Строка 32:**
```typescript
const { routes, alternatives, dataMode, dataQuality, isLoading, error, errorCode } = useRoutesSearch({
```

**Проблема:** Используются `dataMode` и `dataQuality`, которые были удалены из `UseRoutesSearchResult` (строка 69-76 в `use-routes-search.ts`).

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строки 93-94:**
```typescript
const normalizedFrom = from.trim()
const normalizedTo = to.trim()
```

**Анализ:**
- `from` и `to` нормализуются через `.trim()`
- `date` **НЕ нормализуется** — передается как есть
- `passengers` **НЕ нормализуется** — передается как есть

**Строка 97:**
```typescript
queryKey: ['routes', 'search', normalizedFrom, normalizedTo, date, passengers],
```

### Анализ движения параметра `date`:

| Сценарий | Значение в URL | Значение в `page.tsx` | Значение в `queryKey` | Попадает в URL запроса? |
|----------|----------------|----------------------|----------------------|------------------------|
| `date` отсутствует | `null` | `''` (пустая строка) | `''` | ❌ Нет (строка 104: `if (date)`) |
| `date` пустая | `''` | `''` | `''` | ❌ Нет |
| `date` с пробелами | `'  '` | `'  '` | `'  '` | ✅ Да (пробелы не falsy) |
| `date` неверная | `'invalid'` | `'invalid'` | `'invalid'` | ✅ Да |
| `date` корректная | `'2025-01-15'` | `'2025-01-15'` | `'2025-01-15'` | ✅ Да |

### Выводы:

1. **Пустая строка `date` попадает в `queryKey`**, но **не блокирует выполнение запроса**, так как:
   - `enabled` проверяет только `from` и `to` (строка 142)
   - В `queryFn` пустая строка не добавляется в URL (строка 104: `if (date)`)

2. **Проблема:** Если `date = '  '` (пробелы), то:
   - Пробелы попадают в `queryKey`
   - Пробелы попадают в URL запроса (строка 105: `params.set('date', date)`)
   - Backend может вернуть ошибку

3. **Проблема:** Если `date = 'invalid'`, то:
   - Неверный формат попадает в `queryKey`
   - Неверный формат попадает в URL запроса
   - Backend может вернуть ошибку 400

---

## Шаг 2. Проверка логики condition / enabled

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строка 142:**
```typescript
enabled: Boolean(normalizedFrom && normalizedTo),
```

### Анализ условий:

| `from` | `to` | `date` | `enabled` | `queryFn` вызывается? |
|--------|------|--------|-----------|----------------------|
| `''` | `''` | любое | `false` | ❌ Нет |
| `'Якутск'` | `''` | любое | `false` | ❌ Нет |
| `''` | `'Мирный'` | любое | `false` | ❌ Нет |
| `'Якутск'` | `'Мирный'` | любое | `true` | ✅ Да |

### Вывод:

**`date` НЕ влияет на `enabled`** — запрос выполняется независимо от значения `date`, если `from` и `to` заполнены.

### Особые случаи для `date`:

| Значение `date` | `enabled` | `queryFn` вызывается? | `date` в URL запроса? |
|-----------------|-----------|----------------------|----------------------|
| `''` (пустая) | `true` (если `from` и `to` заполнены) | ✅ Да | ❌ Нет |
| `' '` (пробелы) | `true` | ✅ Да | ✅ Да (пробелы) |
| `'invalid'` | `true` | ✅ Да | ✅ Да (неверный формат) |
| Отсутствует в URL | `''` | ✅ Да | ❌ Нет |
| `'2025-01-15'` (корректная) | `true` | ✅ Да | ✅ Да |

### Вывод:

**Параметр `date` больше НЕ блокирует выполнение запроса.** Запрос выполняется при любом значении `date`, если `from` и `to` заполнены.

---

## Шаг 3. Проверка формирования URL

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строки 99-110:**
```typescript
const params = new URLSearchParams({
  from: normalizedFrom,
  to: normalizedTo,
})

if (date) {
  params.set('date', date)
}

if (passengers && passengers !== '1') {
  params.set('passengers', passengers)
}
```

### Анализ формирования URL:

| `from` | `to` | `date` | `passengers` | Итоговый URL |
|--------|------|--------|--------------|--------------|
| `'Якутск'` | `'Мирный'` | `''` | `'1'` | `/routes/search?from=Якутск&to=Мирный` |
| `'Якутск'` | `'Мирный'` | `'  '` | `'1'` | `/routes/search?from=Якутск&to=Мирный&date=++` ⚠️ |
| `'Якутск'` | `'Мирный'` | `'invalid'` | `'1'` | `/routes/search?from=Якутск&to=Мирный&date=invalid` ⚠️ |
| `'Якутск'` | `'Мирный'` | `'2025-01-15'` | `'1'` | `/routes/search?from=Якутск&to=Мирный&date=2025-01-15` ✅ |
| `'Якутск'` | `'Мирный'` | `'2025-01-15'` | `'2'` | `/routes/search?from=Якутск&to=Мирный&date=2025-01-15&passengers=2` ✅ |

### Выводы:

1. **Пустая `date` НЕ попадает в URL** — корректно (строка 104: `if (date)`)
2. **Пробелы в `date` попадают в URL** — проблема (строка 105: `params.set('date', date)`)
3. **Неверный формат `date` попадает в URL** — проблема (строка 105)
4. **Корректная `date` добавляется правильно** — корректно

### Рекомендация:

Добавить нормализацию и валидацию `date` перед добавлением в URL:
```typescript
if (date && date.trim() && /^\d{4}-\d{2}-\d{2}$/.test(date.trim())) {
  params.set('date', date.trim())
}
```

---

## Шаг 4. Проверка вызова `useRoutesSearch` → `fetchApi`

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строка 113:**
```typescript
const response = await fetchApi<unknown>(`/routes/search?${params.toString()}`)
```

### Условия вызова `fetchApi`:

| Условие | Результат |
|---------|-----------|
| `enabled = false` | `queryFn` не вызывается → `fetchApi` не вызывается |
| `enabled = true` | `queryFn` вызывается → `fetchApi` вызывается |

### Ранние выходы:

**Нет ранних выходов до `fetchApi`** в `queryFn`. Единственное условие — `enabled`, которое проверяет только `from` и `to`.

### Гарантированный набор параметров для выполнения запроса:

| Параметр | Значение |
|----------|----------|
| `from` | Непустая строка после `.trim()` |
| `to` | Непустая строка после `.trim()` |
| `date` | Любое значение (включая пустую строку, пробелы, неверный формат) |
| `passengers` | Любое значение (по умолчанию `'1'`) |

### Вывод:

**Запрос `/routes/search` отправляется гарантированно**, если `from` и `to` заполнены. Параметр `date` не блокирует выполнение запроса.

---

## Шаг 5. Проверка валидации ответа `RouteSearchResponseSchema`

### Файл: `frontend/src/modules/routes/schemas/route.schema.ts`

**Строки 122-162:**
```typescript
export const RouteSearchResponseSchema = z.object({
  success: z.boolean(),
  routes: z.array(RouteResultSchema),
  alternatives: z.array(RouteResultSchema).optional(),
  riskAssessment: z.any().optional(),
  executionTimeMs: z.number(),
  graphVersion: z.string().optional(),
  graphAvailable: z.boolean(),
  error: ErrorSchema,
}).passthrough()
```

### Анализ валидации:

#### 1. Может ли Zod-схема отклонить корректный backend-ответ?

**Нет**, если backend возвращает структуру:
```json
{
  "success": true,
  "routes": [...],
  "executionTimeMs": 123,
  "graphAvailable": true
}
```

Схема принимает эту структуру, так как:
- `success` — обязательное boolean ✅
- `routes` — обязательный массив ✅
- `executionTimeMs` — обязательное number ✅
- `graphAvailable` — обязательное boolean ✅
- Остальные поля опциональны ✅
- `.passthrough()` позволяет дополнительные поля ✅

#### 2. Может ли Zod-схема отклонить ответ при пустом маршруте?

**Нет**, если `routes = []` (пустой массив):
- `z.array(RouteResultSchema)` принимает пустой массив ✅
- Схема пройдет валидацию ✅

#### 3. Может ли Zod-схема отклонить альтернативы?

**Нет**, если `alternatives` отсутствует или пустой:
- `alternatives` — опциональное поле (`.optional()`) ✅
- Пустой массив `[]` также валиден ✅

#### 4. Что произойдёт, если backend вернёт 0 маршрутов?

**Корректная обработка:**
- Zod-схема примет `routes: []` ✅
- В `useRoutesSearch` (строка 191): `if (data.routes.length > 0)` — адаптация не выполнится
- `adaptedRoutes = []` ✅
- UI покажет "Маршруты не найдены" ✅

#### 5. Что произойдёт, если backend вернёт маршруты без некоторых полей?

**Проблема:** Если маршрут не соответствует `RouteResultSchema`:
- Zod отклонит маршрут при валидации (строка 184: `RouteResultSchema.safeParse(route)`)
- Выбросится ошибка (строка 186)
- Установится `processingError` с кодом `'INVALID_ROUTE_RESPONSE'` (строка 196)
- Вернутся пустые массивы (строки 243-244)

### Вывод:

**Zod-схема корректно валидирует ответы**, включая пустые маршруты и отсутствие альтернатив. Проблемы могут возникнуть только при несоответствии структуры маршрута схеме `RouteResultSchema`.

---

## Шаг 6. Проверка адаптации маршрутов

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строки 172-214:**
```typescript
if (hasValidData) {
  if (!data.routes) {
    processingError = new Error('Ответ сервера не содержит маршрутов')
  } else if (!Array.isArray(data.routes)) {
    processingError = new Error('Маршруты должны быть массивом')
  } else {
    try {
      for (const route of data.routes) {
        const routeValidation = RouteResultSchema.safeParse(route)
        if (!routeValidation.success) {
          throw new Error('Неверная структура маршрута')
        }
      }
      if (data.routes.length > 0) {
        adaptedRoutes = adaptBackendRoutesToFrontend(data.routes, date, Number(passengers) || 1)
      }
    } catch (err) {
      processingError = new Error('Ошибка при обработке маршрутов')
    }
  }
}
```

### Анализ проверок:

#### 1. Проверка структуры перед адаптацией:

✅ **Проверяется:**
- Существование `data.routes` (строка 174)
- Тип `data.routes` (массив) (строка 177)
- Структура каждого маршрута через Zod (строка 184)
- Длина массива перед адаптацией (строка 191)

#### 2. Адаптер получает корректные `RouteResult`:

✅ **Да**, так как:
- Все маршруты валидируются через `RouteResultSchema` (строка 184)
- Адаптер вызывается только после успешной валидации (строка 192)

#### 3. Адаптер не может упасть при пустой альтернативе:

✅ **Да**, так как:
- Проверка `data.alternatives && Array.isArray(data.alternatives) && data.alternatives.length > 0` (строка 201)
- Если альтернативы пустые или отсутствуют, адаптер не вызывается
- `adaptedAlternatives = []` по умолчанию (строка 169)

#### 4. Адаптер не может упасть при отсутствии `segment.price` или `segment.routeId`:

✅ **Да**, так как:
- В `route-adapter.ts` (строка 114): `basePrice: segment.price` — опциональное поле
- В `route-adapter.ts` (строка 107): `segmentId: segment.routeId || \`segment-${segIndex}\`` — есть fallback
- В `route-adapter.ts` (строка 110): `routeId: segment.routeId || routeId` — есть fallback
- В `route-adapter.ts` (строка 122): `price: segment.price || 0` — есть fallback

### Вывод:

**Адаптер защищён от падений** благодаря проверкам структуры перед адаптацией и fallback-значениям в самом адаптере.

---

## Шаг 7. Проверка обработки ошибок в `useRoutesSearch`

### Файл: `frontend/src/modules/routes/hooks/use-routes-search.ts`

**Строки 228-244:**
```typescript
const apiError = processingError || (data?.error
  ? new Error(data.error.message || 'Ошибка при поиске маршрутов')
  : (error as Error | null))

if (apiError && (apiError as ApiError).code === 'INVALID_ROUTE_RESPONSE') {
  apiError.message = 'Неверный формат данных от сервера. Попробуйте обновить страницу.'
}

const errorCode = processingError?.code || (error as ApiError)?.code || data?.error?.code

const finalRoutes = apiError ? [] : routes
const finalAlternatives = apiError ? [] : alternatives
```

### Классификация ошибок:

#### 1. Сетевые ошибки:

**Определение:** Ошибки, возникающие при сетевых проблемах (нет интернета, сервер недоступен).

**Источник:** `fetchApi` (файл `frontend/src/shared/utils/api.ts`, строки 56-63):
```typescript
if (typeof navigator !== 'undefined' && !navigator.onLine) {
  throw new Error('Нет подключения к интернету. Проверьте ваше соединение.')
}

if (error instanceof Error && error.message.includes('Failed to fetch')) {
  throw new Error(`Не удалось подключиться к серверу. Проверьте, что backend запущен на ${API_BASE_URL.replace('/api/v1', '')}`);
}
```

**Обработка в `useRoutesSearch`:**
- Попадает в `error` от React Query (строка 96)
- Обрабатывается как `apiError` (строка 232)
- **Проблема:** Может маскироваться под сетевую ошибку, если не имеет специфического кода

#### 2. Серверные ошибки:

**Определение:** Ошибки, возвращаемые backend (400, 404, 500, 503).

**Источник:** `fetchApi` (строки 28-51):
```typescript
if (!response.ok) {
  const error = new Error(errorMessage) as Error & { code?: string; status?: number };
  error.code = errorCode;
  error.status = response.status;
  throw error;
}
```

**Обработка в `useRoutesSearch`:**
- Для 404 с кодом `'ROUTES_NOT_FOUND'` — возвращается успешный ответ с пустым массивом (строки 129-136)
- Для других ошибок — пробрасывается дальше (строка 139)
- Попадает в `error` от React Query
- Обрабатывается как `apiError` (строка 232)

#### 3. Валидационные ошибки:

**Определение:** Ошибки валидации ответа через Zod.

**Источник:** `useRoutesSearch` (строки 118-121, 175-179, 195-196):
```typescript
if (!validationResult.success) {
  const validationError = new Error('Неверный формат ответа от сервера') as Error & { code?: string }
  validationError.code = 'INVALID_ROUTE_RESPONSE'
  throw validationError
}
```

**Обработка:**
- Устанавливается `processingError` с кодом `'INVALID_ROUTE_RESPONSE'` (строки 176, 179, 196)
- Создается понятное сообщение (строка 236)
- **Не маскируется под сетевую ошибку** ✅

#### 4. Ошибки, приводящие к показу "Не удалось подключиться":

**Проблема:** Если backend возвращает ошибку без специфического кода (например, 400 Bad Request с неверным форматом `date`), то:

1. `fetchApi` создает ошибку с сообщением из backend (строка 36 в `api.ts`)
2. Если сообщение содержит "Failed to fetch", оно заменяется на "Не удалось подключиться к серверу" (строка 62 в `api.ts`)
3. В `useRoutesSearch` ошибка обрабатывается как `apiError` (строка 232)
4. В `page.tsx` общая ошибка отображается как "Произошла ошибка при поиске маршрутов" (строка 67)

**Вывод:** Ошибки backend **могут маскироваться** под сетевые, если они не имеют специфического кода или если сообщение не содержит ключевых слов для различения.

### Файл: `frontend/src/app/routes/page.tsx`

**Строки 43-68:**
```typescript
const errorMessage = useMemo(() => {
  if (!hasRequiredParams) {
    return 'Не указаны параметры поиска'
  }
  
  if (!error) {
    return null
  }
  
  if (errorCode === 'STOPS_NOT_FOUND') {
    return `Города "${from}" или "${to}" не найдены в базе данных. Проверьте правильность написания.`
  }
  
  if (errorCode === 'GRAPH_OUT_OF_SYNC') {
    return 'Данные временно недоступны. Пожалуйста, попробуйте позже.'
  }
  
  if (errorCode === 'ROUTES_NOT_FOUND') {
    return null
  }
  
  return error.message || 'Произошла ошибка при поиске маршрутов'
}, [error, errorCode, hasRequiredParams, from, to])
```

### Анализ отображения ошибок:

| Тип ошибки | `errorCode` | Сообщение в UI |
|------------|-------------|----------------|
| Сетевая (нет интернета) | `undefined` | "Нет подключения к интернету. Проверьте ваше соединение." |
| Сетевая (сервер недоступен) | `undefined` | "Не удалось подключиться к серверу..." |
| Backend 404 (STOPS_NOT_FOUND) | `'STOPS_NOT_FOUND'` | "Города ... не найдены..." ✅ |
| Backend 503 (GRAPH_OUT_OF_SYNC) | `'GRAPH_OUT_OF_SYNC'` | "Данные временно недоступны..." ✅ |
| Backend 404 (ROUTES_NOT_FOUND) | `'ROUTES_NOT_FOUND'` | Не показывается (null) ✅ |
| Валидация ответа | `'INVALID_ROUTE_RESPONSE'` | "Неверный формат данных от сервера..." ✅ |
| Backend 400 (неверный формат `date`) | `undefined` | "Произошла ошибка при поиске маршрутов" ⚠️ |
| Backend 500 | `undefined` | "Произошла ошибка при поиске маршрутов" ⚠️ |

### Вывод:

**Ошибки с специфическими кодами отображаются корректно.** Ошибки без кода (например, 400 из-за неверного формата `date`) могут отображаться как общая ошибка, но **не маскируются под "Не удалось подключиться к серверу"**, если backend возвращает корректное сообщение об ошибке.

---

## Шаг 8. Финальный вывод

### 1. Отправляется ли запрос `/routes/search` гарантированно?

**Да**, если:
- `from` непустая строка после `.trim()`
- `to` непустая строка после `.trim()`

**Параметр `date` не влияет на выполнение запроса.**

### 2. Может ли параметр `date` блокировать вызов?

**Нет**, параметр `date` больше не блокирует вызов `queryFn`. Запрос выполняется при любом значении `date`, если `from` и `to` заполнены.

**Однако:** Если `date` содержит пробелы или неверный формат, она попадет в URL запроса, и backend может вернуть ошибку 400.

### 3. Может ли структура ответа привести к UI-ошибке?

**Да**, если:
- Backend вернет маршруты, не соответствующие `RouteResultSchema`
- В этом случае установится `processingError` с кодом `'INVALID_ROUTE_RESPONSE'`
- UI покажет: "Неверный формат данных от сервера. Попробуйте обновить страницу."

**Но это корректная обработка ошибки, а не маскировка под сетевую.**

### 4. Соответствует ли работа фронта реальной структуре backend?

**Да**, после внесённых исправлений:
- Добавлена Zod-схема `RouteSearchResponseSchema`, соответствующая структуре backend
- Валидация ответа происходит перед использованием данных
- Адаптер получает только валидированные данные
- Ошибки валидации обрабатываются корректно

### 5. Является ли модуль поиска маршрутов теперь стабильным?

**Частично стабилен**, но обнаружены **2 критические проблемы**:

#### Проблема 1: Использование удалённых полей

**Файл:** `frontend/src/app/routes/page.tsx`, строка 32

**Проблема:**
```typescript
const { routes, alternatives, dataMode, dataQuality, isLoading, error, errorCode } = useRoutesSearch({
```

`dataMode` и `dataQuality` были удалены из `UseRoutesSearchResult`, но все ещё используются в `page.tsx`.

**Последствия:**
- Ошибка TypeScript (если включена строгая проверка)
- Возможная ошибка рантайма при обращении к `undefined`

**Строки использования:**
- Строка 147: `{dataMode && (`
- Строка 149: `<DataModeBadge dataMode={dataMode} dataQuality={dataQuality} />`

**Критичность:** 🔴 Высокая

#### Проблема 2: Пробелы и неверный формат `date` попадают в URL

**Файл:** `frontend/src/modules/routes/hooks/use-routes-search.ts`, строка 105

**Проблема:**
```typescript
if (date) {
  params.set('date', date)
}
```

Если `date = '  '` (пробелы) или `date = 'invalid'`, она попадет в URL запроса.

**Последствия:**
- Backend может вернуть ошибку 400
- Ошибка может отображаться как общая, без специфического кода

**Критичность:** 🟡 Средняя

### Рекомендации:

1. **Исправить использование `dataMode` и `dataQuality` в `page.tsx`:**
   - Удалить деструктуризацию этих полей
   - Удалить использование `DataModeBadge` или сделать его условным

2. **Добавить нормализацию и валидацию `date` перед добавлением в URL:**
   ```typescript
   if (date && date.trim() && /^\d{4}-\d{2}-\d{2}$/.test(date.trim())) {
     params.set('date', date.trim())
   }
   ```

3. **Улучшить обработку ошибок backend без специфического кода:**
   - Добавить проверку статуса ответа
   - Различать ошибки 400 (валидация) от сетевых ошибок

---

## Итоговый статус

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Параметр `date` не блокирует запрос | ✅ Да | Запрос выполняется при любом `date`, если `from` и `to` заполнены |
| Запрос `/routes/search` отправляется | ✅ Да | Гарантированно, если `from` и `to` заполнены |
| Адаптер получает корректные данные | ✅ Да | Данные валидируются перед адаптацией |
| UI не показывает ложную ошибку | ⚠️ Частично | Ошибки с кодом обрабатываются корректно, но ошибки без кода могут отображаться как общие |
| Ошибки backend корректно отображаются | ⚠️ Частично | Ошибки с кодом отображаются корректно, ошибки без кода — как общие |
| `enabled`, `queryKey`, `queryFn` работают согласованно | ✅ Да | Все работает корректно |

**Общий вывод:** Модуль функционально работает стабильно, но требует исправления использования удалённых полей в `page.tsx` и нормализации параметра `date`.






