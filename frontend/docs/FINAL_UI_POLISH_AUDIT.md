# Финальный UI-аудит проекта (Final UI Polish Audit)

**Дата:** 2025-01-27  
**Версия:** Theme System v2  
**Статус:** Аудит завершён

---

## 📋 Сводка

**Всего найдено проблем:** 18  
**Критичных:** 2  
**Важных:** 10  
**Рекомендованных:** 6

---

## 🔴 КРИТИЧНЫЕ ПРОБЛЕМЫ

### 1. Хардкод padding в CSS классах кнопок

**Где находится:**  
`frontend/src/app/globals.css` (строки 261, 289)

**Проблема:**  
Классы `.btn-primary` и `.btn-secondary` используют хардкод padding (`10px 20px`, `12px 24px`) вместо токенов spacing из Theme System v2.

**Почему проблема:**  
Нарушает единообразие системы токенов. При изменении spacing-токенов кнопки не обновятся автоматически.

**Степень важности:** Критично

**Как исправить:**  
Заменить хардкод на токены:
```css
.btn-primary {
  padding: var(--spacing-sm) var(--spacing-lg);
  /* вместо: padding: 10px 20px; */
}

.btn-secondary {
  padding: var(--spacing-md) var(--spacing-xl);
  /* вместо: padding: 12px 24px; */
}
```

---

### 2. Хардкод padding и font-size в CSS классе .input

**Где находится:**  
`frontend/src/app/globals.css` (строки 303, 308)

**Проблема:**  
Класс `.input` использует хардкод `padding: 8px 12px;` и `font-size: 14px;` вместо токенов.

**Почему проблема:**  
Нарушает единообразие системы токенов. При изменении spacing или typography токенов инпуты не обновятся автоматически.

**Степень важности:** Критично

**Как исправить:**  
Заменить хардкод на токены:
```css
.input {
  padding: var(--spacing-xs) var(--spacing-md);
  font-size: var(--font-sm);
  /* вместо: padding: 8px 12px; font-size: 14px; */
}
```

---

## ⚠️ ВАЖНЫЕ ПРОБЛЕМЫ

### 3. Нерегулярные spacing значения в search-form.tsx

**Где находится:**  
`frontend/src/modules/routes/features/route-search/ui/search-form.tsx` (строки 190, 211)

**Проблема:**  
Используется `mt-1` вместо токена `mt-xs` для отступов сообщений об ошибках.

**Почему проблема:**  
Нарушает единообразие spacing-системы. `mt-1` не является частью Theme System v2.

**Степень важности:** Важно

**Как исправить:**  
Заменить `mt-1` на `mt-xs`:
```tsx
<p className="text-sm mt-xs text-error">
```

---

### 4. Нерегулярные spacing значения в hotels-sort-dropdown.tsx

**Где находится:**  
`frontend/src/modules/hotels/features/hotel-search/ui/hotels-sort-dropdown.tsx` (строки 46, 53)

**Проблема:**  
Используются `gap-2`, `mt-2`, `p-2` вместо токенов `gap-sm`, `mt-sm`, `p-sm`.

**Почему проблема:**  
Нарушает единообразие spacing-системы.

**Степень важности:** Важно

**Как исправить:**  
Заменить:
- `gap-2` → `gap-sm`
- `mt-2` → `mt-sm`
- `p-2` → `p-sm`

---

### 5. Нерегулярные spacing значения в assistant-button.tsx

**Где находится:**  
`frontend/src/shared/ui/assistant-button/assistant-button.tsx` (строка 8)

**Проблема:**  
Используются `bottom-6`, `right-6` вместо токенов spacing.

**Почему проблема:**  
Нарушает единообразие spacing-системы. `6` не является частью Theme System v2.

**Степень важности:** Важно

**Как исправить:**  
Заменить на токены:
```tsx
className="fixed bottom-lg right-lg ..."
```

---

### 6. Arbitrary значения в page.tsx

**Где находится:**  
`frontend/src/app/page.tsx` (строка 154)

**Проблема:**  
Используется `min-h-[400px]` вместо токена.

**Почему проблема:**  
Нарушает единообразие системы токенов.

**Степень важности:** Важно

**Как исправить:**  
Создать токен для минимальной высоты секции или использовать существующий:
```tsx
className="relative min-h-screen" // или создать токен --min-height-section
```

---

### 7. Arbitrary значения в hotels-sort-dropdown.tsx

**Где находится:**  
`frontend/src/modules/hotels/features/hotel-search/ui/hotels-sort-dropdown.tsx` (строка 53)

**Проблема:**  
Используется `min-w-[200px]` вместо токена.

**Почему проблема:**  
Нарушает единообразие системы токенов.

**Степень важности:** Важно

**Как исправить:**  
Использовать токен или стандартный класс:
```tsx
className="... min-w-xs ..." // или создать токен --min-width-dropdown
```

---

### 8. Нерегулярные spacing значения в routes/page.tsx

**Где находится:**  
`frontend/src/app/routes/page.tsx` (строки 130, 415)

**Проблема:**  
Используются `h-10`, `w-10` вместо токенов `h-lg`, `w-lg`.

**Почему проблема:**  
Нарушает единообразие spacing-системы.

**Степень важности:** Важно

**Как исправить:**  
Заменить `h-10 w-10` на `h-lg w-lg`.

---

### 9. Нерегулярные spacing значения в route-details-view.tsx

**Где находится:**  
`frontend/src/modules/routes/features/route-details/ui/route-details-view.tsx` (строка 120)

**Проблема:**  
Используется `py-10` вместо токена `py-2xl`.

**Почему проблема:**  
Нарушает единообразие spacing-системы.

**Степень важности:** Важно

**Как исправить:**  
Заменить `py-10` на `py-2xl`.

---

### 10. Нерегулярные spacing значения в transport/hotels/services компонентах

**Где находится:**  
Множественные файлы:
- `rent-tab.tsx`, `taxi-tab.tsx`, `bus-tab.tsx` (строки с `gap-4`, `mb-4`, `p-5`)
- `hotels-filters.tsx`, `rent-filters.tsx` (строки с `mb-2`, `px-6`, `py-2`, `gap-4`)
- `packages-tab.tsx` (строки с `gap-5`, `p-5`, `mb-3`, `mt-4`)
- `tours-tab.tsx` (строки с `mb-3`, `mb-2`, `mb-4`)
- `hotel-card.tsx` (строки с `gap-4`, `mb-2`, `mb-3`, `gap-1`)
- `hotels-search-form.tsx` (строки с `p-5`, `mb-4`, `gap-4`)
- `individual-services-tab.tsx` (строки с `gap-4`, `mb-2`, `mb-4`)
- `offline-notification.tsx` (строки с `px-6`, `py-4`, `gap-3`)

**Проблема:**  
Используются нерегулярные spacing значения (`gap-4`, `gap-5`, `mb-2`, `mb-3`, `mb-4`, `p-5`, `px-6`, `py-2`, `py-4`, `gap-1`, `gap-3`) вместо токенов Theme System v2.

**Почему проблема:**  
Нарушает единообразие spacing-системы по всему проекту.

**Степень важности:** Важно

**Как исправить:**  
Заменить все нерегулярные значения на токены:
- `gap-4` → `gap-md`
- `gap-5` → `gap-lg`
- `gap-1` → `gap-xs`
- `gap-3` → `gap-md`
- `mb-2` → `mb-sm`
- `mb-3` → `mb-md`
- `mb-4` → `mb-lg`
- `p-5` → `p-lg`
- `px-6` → `px-xl`
- `py-2` → `py-sm`
- `py-4` → `py-md`

---

### 11. Использование border-l-4 вместо токенов

**Где находится:**  
- `route-summary.tsx` (строки 52, 60)
- `route-segments.tsx` (строка 47)
- `route-alternatives.tsx` (строки 94, 110)

**Проблема:**  
Используется `border-l-4` (хардкод толщины границы) вместо стандартизированного подхода.

**Почему проблема:**  
Нарушает единообразие системы. Толщина границы должна быть частью дизайн-системы.

**Степень важности:** Важно

**Как исправить:**  
Создать утилитарный класс или использовать токен:
```css
.border-l-accent {
  border-left-width: 4px;
  border-left-color: var(--color-accent);
}
```
Или использовать стандартный Tailwind класс с токеном цвета.

---

### 12. Использование shadow-none в header и footer

**Где находится:**  
- `frontend/src/shared/ui/header/header.tsx` (строка 21)
- `frontend/src/shared/ui/footer/footer.tsx` (строка 21)

**Проблема:**  
Используется `shadow-none` для логотипов в header и footer.

**Почему проблема:**  
Может быть намеренно, но стоит проверить, соответствует ли это дизайн-системе. Если тени не нужны, это нормально, но нужно убедиться в консистентности.

**Степень важности:** Важно

**Как исправить:**  
Если тени действительно не нужны, оставить как есть. Если нужны, заменить на `shadow-xs` или `shadow-sm`.

---

## 📝 РЕКОМЕНДОВАННЫЕ ПРОБЛЕМЫ

### 13. Несогласованность размеров шрифтов в кнопках

**Где находится:**  
`frontend/src/app/globals.css` (строка 266)

**Проблема:**  
Класс `.btn-primary` использует хардкод `font-size: 15px;` вместо токена `var(--font-md)`.

**Почему проблема:**  
Нарушает единообразие типографической системы, хотя значение близко к `--font-md` (15px).

**Степень важности:** Рекомендовано

**Как исправить:**  
Заменить на токен:
```css
.btn-primary {
  font-size: var(--font-md);
}
```

---

### 14. Несогласованность в использовании rounded-full

**Где находится:**  
- `assistant-button.tsx` (строка 8)
- `routes/page.tsx` (строки 130, 224, 354, 415)
- `route-segments.tsx` (строка 50)

**Проблема:**  
Использование `rounded-full` корректно, но стоит убедиться, что это соответствует дизайн-системе для всех случаев.

**Почему проблема:**  
Не критично, но стоит проверить консистентность использования.

**Степень важности:** Рекомендовано

**Как исправить:**  
Проверить, что `rounded-full` используется только для круглых элементов (кнопки, аватары, индикаторы). Если это так, оставить как есть.

---

### 15. Длинные цепочки классов в некоторых компонентах

**Где находится:**  
Множественные файлы, например:
- `search-form.tsx` (строка 310)
- `routes/page.tsx` (строки 258, 388)

**Проблема:**  
Некоторые элементы имеют длинные цепочки классов, что может затруднять читаемость.

**Почему проблема:**  
Не критично, но может усложнить поддержку кода.

**Степень важности:** Рекомендовано

**Как исправить:**  
Рассмотреть возможность вынесения часто используемых комбинаций классов в отдельные утилитарные классы или компоненты.

---

### 16. Использование gap-2 в hotels-sort-dropdown.tsx

**Где находится:**  
`frontend/src/modules/hotels/features/hotel-search/ui/hotels-sort-dropdown.tsx` (строка 61)

**Проблема:**  
Используется `px-4 py-2` вместо токенов `px-md py-sm`.

**Почему проблема:**  
Нарушает единообразие spacing-системы.

**Степень важности:** Рекомендовано

**Как исправить:**  
Заменить на токены:
```tsx
className="... px-md py-sm ..."
```

---

### 17. Проверка консистентности использования text-inverse

**Где находится:**  
Множественные файлы, где используется `text-inverse` или `bg-primary text-inverse`.

**Проблема:**  
Нужно убедиться, что `text-inverse` используется только на тёмных фонах (primary, dark-zone).

**Почему проблема:**  
Не критично, но стоит проверить для обеспечения правильного контраста.

**Степень важности:** Рекомендовано

**Как исправить:**  
Провести визуальную проверку всех мест, где используется `text-inverse`, и убедиться в правильности контраста.

---

### 18. Проверка консистентности hover-эффектов

**Где находится:**  
Множественные файлы с hover-эффектами.

**Проблема:**  
Нужно убедиться, что все hover-эффекты используют токены transition из Theme System v2.

**Почему проблема:**  
Не критично, но стоит проверить для единообразия анимаций.

**Степень важности:** Рекомендовано

**Как исправить:**  
Проверить, что все hover-эффекты используют классы `transition-fast`, `transition-base`, или `transition-slow` из Theme System v2.

---

## 📊 Статистика

### По категориям:

- **Spacing проблемы:** 10
- **CSS хардкоды:** 2
- **Радиусы и границы:** 2
- **Тени:** 1
- **Типографика:** 1
- **Общие рекомендации:** 2

### По приоритетам:

- **Критично:** 2
- **Важно:** 10
- **Рекомендовано:** 6

---

## ✅ Положительные моменты

1. ✅ Нет хардкодных цветов (HEX, rgba) в компонентах
2. ✅ Все цвета используют токены Theme System v2
3. ✅ Нет конфликтов между `surface`, `surface-alt`, `dark-zone`
4. ✅ Правильная иерархия текстовых цветов (`text-primary`, `text-secondary`, `text-heading`)
5. ✅ Все инпуты используют классы `input` или `input-dark`
6. ✅ Все кнопки используют классы `btn-primary` или `btn-secondary`
7. ✅ Все карточки используют класс `card`
8. ✅ Все тени используют системные токены (`shadow-xs`, `shadow-sm`, `shadow-md`)
9. ✅ Все радиусы используют системные токены (`rounded-sm`, `rounded-md`, `rounded-lg`, `rounded-full`)
10. ✅ Иконки используют `currentColor` для правильной адаптации к фону

---

## 🎯 Рекомендации по приоритетам исправления

1. **Сначала исправить критичные проблемы** (проблемы #1, #2) — они влияют на всю систему токенов.
2. **Затем исправить важные проблемы** (проблемы #3–#12) — они влияют на визуальную консистентность.
3. **В конце исправить рекомендованные проблемы** (проблемы #13–#18) — они улучшают общее качество кода.

---

## 📝 Заключение

Проект в целом хорошо структурирован и использует Theme System v2. Основные проблемы связаны с:
1. Хардкодами в CSS классах (кнопки, инпуты)
2. Нерегулярными spacing значениями в некоторых компонентах
3. Arbitrary значениями в некоторых местах

После исправления всех проблем проект будет полностью соответствовать Theme System v2 и иметь единообразный визуальный стиль.






