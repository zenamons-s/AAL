# LenaLink Brand Identity — Premium Travel Blue Theme

**Версия:** 1.0  
**Дата создания:** 2024  
**Статус:** ✅ Активная бренд-тема

---

## 🎨 Обзор бренда

LenaLink — это премиальная travel-платформа с уникальной фирменной идентичностью, вдохновлённой лучшими travel-продуктами (Skyscanner, Google Flights). Бренд-тема построена на премиальной синей палитре, которая передаёт ощущение чистоты, воздушности, технологичности и премиальности.

### Ключевые принципы бренда

- **Чистота** — минималистичный, чистый дизайн без лишних элементов
- **Воздушность** — лёгкие оттенки, просторные интерфейсы
- **Технологичность** — современные градиенты и плавные переходы
- **Премиальность** — качественные цвета, продуманная типографика
- **Доступность** — соответствие WCAG AA стандартам

---

## 🎨 Фирменная палитра

### Основные фирменные цвета

#### Primary (Премиальный синий)
```css
--brand-primary: #0066cc;                    /* Основной акцент */
--brand-primary-hover: #0052a3;              /* Насыщенный тёмный синий */
--brand-primary-active: #004080;              /* Активный премиальный синий */
--brand-primary-light: #e6f2ff;               /* Airy light blue */
--brand-primary-dark: #003d7a;                /* Глубокий синий */
```

**Использование:**
- Основные кнопки (`.btn-primary`)
- Активные табы (`.tab-light-active`)
- Акцентные ссылки
- Заголовки (`.brand-heading`)
- Фокус-состояния интерактивных элементов

#### Secondary (Бирюзовый акцент)
```css
--brand-secondary: #00a8b5;                  /* Бирюзовый акцент */
--brand-secondary-hover: #008a94;             /* Бирюзовый hover */
--brand-secondary-light: rgba(0, 168, 181, 0.1); /* Бирюзовый светлый */
```

**Использование:**
- Вторичные акценты
- Акцентный текст (`.brand-accent-text`)
- Дополнительные интерактивные элементы

#### Accent (Soft Aqua)
```css
--brand-accent: #4dd0e1;                     /* Soft aqua */
--brand-accent-hover: #26c6da;               /* Aqua hover */
--brand-accent-light: rgba(77, 208, 225, 0.1); /* Aqua светлый */
```

**Использование:**
- Тонкие акценты
- Декоративные элементы
- Подсветка важной информации

### Градиенты

#### Primary Gradient (blue → aqua)
```css
--brand-gradient-primary: linear-gradient(135deg, #0066cc 0%, #00a8b5 100%);
```

**Использование:**
- Класс `.brand-gradient` для премиальных элементов
- Фоновые акценты
- Hero-секции

#### Surface Gradient (subtle blue tint)
```css
--brand-gradient-surface: linear-gradient(180deg, rgba(230, 242, 255, 0.4) 0%, rgba(255, 255, 255, 1) 100%);
```

**Использование:**
- Класс `.brand-gradient-surface` для фонов карточек
- Декоративные фоны

### Нейтральные цвета

```css
--brand-neutral-light: #f5f8fa;              /* Мягкий светлый нейтральный */
--brand-neutral-dark: #1a2332;                /* Глубокий тёмный нейтральный */
--brand-neutral-medium: #6b7c93;              /* Средний нейтральный */
```

**Использование:**
- Фоновые поверхности
- Вторичный текст
- Границы

---

## 🎨 Обновлённые токены Theme System v2

### Поверхности (Surface)

```css
--color-surface: #fafcfe;                    /* Лёгкий оттенок синевато-белого */
--color-surface-alt: #f0f5f9;                /* Синий оттенок 2-3% */
--color-surface-hover: #e6f2ff;              /* Airy blue hover */
--color-background-subtle: #f5f8fa;          /* Мягкий синеватый оттенок */
```

### Текст

```css
--color-text-primary: #1a2332;               /* Глубокий синий */
--color-text-secondary: #6b7c93;             /* Нейтральный тёмный */
--color-text-heading: var(--brand-primary);   /* Фирменный синий для заголовков */
```

### Границы

```css
--color-border-light: #d6e8f5;               /* Потемневший воздушный синий */
--color-border: #e0eef7;                      /* Воздушный синий */
--color-border-hover: var(--brand-primary);    /* Акцентированный синий */
--color-divider: #d6e8f5;                     /* Потемневший воздушный синий */
```

### Инпуты

```css
--color-input-border: #d6e8f5;                /* Светло-синий оттенок */
--color-input-border-focus: var(--brand-primary); /* Брендовый акцент */
--color-input-placeholder: #9bb0c4;          /* Мягкий gray-blue */
--color-input-text: #1a2332;                 /* Глубокий синий */
```

### Карточки

```css
--color-card-bg: #fafcfe;                    /* Airy-blue surface */
--color-card-border: #d6e8f5;                 /* Blue-neutral */
```

### Dark Zone

```css
--color-dark-zone: #003d7a;                   /* Глубокий синий (не чёрный) */
--color-dark-zone-alt: #002d5c;              /* Ещё более глубокий синий */
```

---

## 🎨 UI-KIT компоненты

### Кнопки

#### Primary Button (`.btn-primary`)
- **Фон:** `--brand-primary` (#0066cc)
- **Hover:** `--brand-primary-hover` (#0052a3)
- **Активный:** `--brand-primary-active` (#004080)
- **Цвет текста:** Белый

#### Secondary Button (`.btn-secondary`)
- **Фон:** `--color-surface` (airy-blue)
- **Граница:** `--color-border`
- **Hover:** `--color-surface-hover` + граница `--brand-primary`

#### Subtle Button (`.btn-subtle`)
- **Фон:** `--brand-primary-light` (#e6f2ff)
- **Hover:** `--color-surface-hover` + цвет `--brand-primary`

#### Icon Button (`.btn-icon`)
- **Фон:** Прозрачный
- **Hover:** `--brand-primary-light` + цвет `--brand-primary`

### Инпуты

- **Рамка:** `--color-input-border` (#d6e8f5) — светло-синий оттенок
- **Focus:** `--brand-primary` — брендовый акцент
- **Placeholder:** `--color-input-placeholder` (#9bb0c4) — мягкий gray-blue

### Карточки

- **Фон:** `--color-card-bg` (#fafcfe) — airy-blue surface
- **Граница:** `--color-card-border` (#d6e8f5) — blue-neutral
- **Hover:** Граница меняется на `--brand-primary`

### Табы

#### Active Tab (`.tab-light-active`)
- **Фон:** `--brand-primary`
- **Цвет текста:** Белый

#### Hover Tab (`.tab-light:hover`)
- **Фон:** `--brand-primary-light`
- **Цвет текста:** `--brand-primary`
- **Граница:** `--brand-primary`

#### Inactive Tab (`.tab-light`)
- **Фон:** `--color-input-bg`
- **Цвет текста:** `--color-text-secondary`
- **Граница:** `--color-border-light`

---

## 🎨 Brand Components (Семантические утилиты)

### Заголовки

#### `.brand-heading`
```css
.brand-heading {
  color: var(--brand-primary);
  font-weight: var(--font-weight-semibold);
  letter-spacing: 0.01em;
}
```

**Использование:** Основные заголовки секций, важные заголовки

#### `.brand-subtitle`
```css
.brand-subtitle {
  color: var(--brand-neutral-medium);
  font-weight: var(--font-weight-normal);
  letter-spacing: 0.005em;
}
```

**Использование:** Подзаголовки, вторичные заголовки

#### `.brand-accent-text`
```css
.brand-accent-text {
  color: var(--brand-secondary);
  font-weight: var(--font-weight-medium);
}
```

**Использование:** Акцентный текст, важная информация

### Поверхности

#### `.brand-surface-card`
```css
.brand-surface-card {
  background: var(--color-card-bg);
  border: 1px solid var(--color-card-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}
```

**Использование:** Брендовые карточки, контентные блоки

### Градиенты

#### `.brand-gradient`
```css
.brand-gradient {
  background: var(--brand-gradient-primary);
  color: var(--color-text-inverse);
}
```

**Использование:** Премиальные элементы, hero-секции, акцентные блоки

#### `.brand-gradient-surface`
```css
.brand-gradient-surface {
  background: var(--brand-gradient-surface);
}
```

**Использование:** Декоративные фоны, фоны карточек

### Разделители

#### `.brand-divider`
```css
.brand-divider {
  border-top: 1px solid var(--color-divider);
  margin: var(--spacing-lg) 0;
}
```

**Использование:** Разделители между секциями, визуальное разделение контента

---

## 📝 Типографика

### Заголовки

#### H1
- **Размер:** 1.5rem
- **Вес:** `--font-weight-medium` (500)
- **Letter-spacing:** 0.01em (увеличенный для премиальности)
- **Цвет:** `--brand-primary` (#0066cc)

#### H2
- **Размер:** 1.25rem
- **Вес:** `--font-weight-medium` (500)
- **Letter-spacing:** 0.005em (увеличенный для премиальности)
- **Цвет:** `--brand-primary` (#0066cc)

#### H3
- **Размер:** 1.125rem
- **Вес:** `--font-weight-medium` (500)
- **Цвет:** `--brand-primary` (#0066cc)

### Body текст

- **Primary:** `--color-text-primary` (#1a2332) — глубокий синий
- **Secondary:** `--color-text-secondary` (#6b7c93) — нейтральный тёмный
- **Line-height:** `--line-height-normal` (1.5)

### Принципы типографики

- **Чистота:** Минималистичная, без лишних декоративных элементов
- **Честность:** Прямолинейная, понятная иерархия
- **Премиальность:** Увеличенный letter-spacing для заголовков
- **Читаемость:** Оптимальные размеры и контрастность

---

## 🎨 Примеры использования

### Пример карточки

```html
<div class="card brand-surface-card card-hover">
  <h2 class="brand-heading">Заголовок карточки</h2>
  <p class="brand-subtitle">Подзаголовок</p>
  <p>Основной контент карточки...</p>
  <button class="btn-primary">Действие</button>
</div>
```

### Пример кнопок

```html
<!-- Primary -->
<button class="btn-primary">Основное действие</button>

<!-- Secondary -->
<button class="btn-secondary">Вторичное действие</button>

<!-- Subtle -->
<button class="btn-subtle">Тонкое действие</button>

<!-- Icon -->
<button class="btn-icon" aria-label="Иконка">
  <Icon />
</button>
```

### Пример типографики

```html
<h1 class="brand-heading">Главный заголовок</h1>
<h2 class="brand-subtitle">Подзаголовок секции</h2>
<p>Основной текст с <span class="brand-accent-text">акцентом</span></p>
```

### Пример градиента

```html
<div class="brand-gradient">
  <h2>Премиальный блок с градиентом</h2>
  <p>Контент на градиентном фоне</p>
</div>
```

### Пример табов

```html
<div role="tablist">
  <button class="tab tab-light tab-light-active" role="tab">Активная вкладка</button>
  <button class="tab tab-light" role="tab">Неактивная вкладка</button>
</div>
```

---

## 🎨 Правила контраста

### WCAG AA Compliance

Все цвета соответствуют стандартам WCAG AA:

- **Текст на светлом фоне:**
  - Primary text (#1a2332) на white: **12.5:1** ✅
  - Secondary text (#6b7c93) на white: **4.8:1** ✅

- **Текст на тёмном фоне:**
  - White на dark-zone (#003d7a): **8.2:1** ✅

- **Интерактивные элементы:**
  - Primary button (#0066cc) с white text: **4.5:1** ✅
  - Focus states: **3:1** ✅

### Рекомендации

- Всегда используйте токены вместо хардкода цветов
- Проверяйте контраст при добавлении новых элементов
- Используйте `--color-text-primary` для основного текста
- Используйте `--color-text-secondary` для вторичного текста

---

## 🎨 Правила иконок

### Размеры

- **Малые:** `--size-icon-md` (16px)
- **Средние:** 20px
- **Крупные:** 24px

### Цвета

- **Primary:** `--brand-primary` (#0066cc)
- **Secondary:** `--color-text-secondary` (#6b7c93)
- **Inverse:** `--color-text-inverse` (#ffffff)

### Использование

- Иконки должны соответствовать контексту
- Используйте `--brand-primary` для активных иконок
- Используйте `--color-text-secondary` для неактивных иконок

---

## 🎨 Правила Motion

### Motion System (не изменён)

Все анимации используют существующий Motion System:

```css
--motion-duration-xs: 80ms;
--motion-duration-sm: 120ms;
--motion-duration-md: 180ms;
--motion-duration-lg: 240ms;

--motion-ease-default: cubic-bezier(0.4, 0, 0.2, 1);
```

### Принципы

- **Плавность:** Все переходы плавные и естественные
- **Скорость:** Быстрые переходы для интерактивных элементов
- **Reduced Motion:** Поддержка `prefers-reduced-motion`

### Примеры

```css
/* Быстрый переход */
.transition-fast {
  transition: all var(--motion-duration-xs) var(--motion-ease-default);
}

/* Нормальный переход */
.transition-normal {
  transition: all var(--motion-duration-sm) var(--motion-ease-default);
}
```

---

## 🎨 Описание фирменного UI стиля

### Общие принципы

1. **Чистота и минимализм**
   - Минимальное количество визуальных элементов
   - Чёткая иерархия информации
   - Просторные интерфейсы

2. **Воздушность**
   - Лёгкие оттенки синего
   - Мягкие границы
   - Прозрачные фоны

3. **Технологичность**
   - Современные градиенты
   - Плавные переходы
   - Чёткие фокус-состояния

4. **Премиальность**
   - Качественные цвета
   - Продуманная типографика
   - Внимание к деталям

### Цветовая схема

- **Основной:** Премиальный синий (#0066cc)
- **Вторичный:** Бирюзовый (#00a8b5)
- **Акцент:** Soft aqua (#4dd0e1)
- **Нейтральный:** Серо-синие оттенки

### Типографика

- **Заголовки:** Премиальный синий, увеличенный letter-spacing
- **Body:** Глубокий синий для primary, нейтральный для secondary
- **Акценты:** Бирюзовый для важной информации

### Компоненты

- **Кнопки:** Чёткие, с брендовыми цветами
- **Инпуты:** Воздушные границы, брендовый фокус
- **Карточки:** Лёгкие фоны, синие границы
- **Табы:** Брендовый синий для активных

---

## 📋 Чеклист использования

### ✅ Обязательные правила

- [ ] Использовать только токены, не хардкодить цвета
- [ ] Проверять контрастность при добавлении новых элементов
- [ ] Использовать Brand Components для брендовых элементов
- [ ] Следовать принципам типографики
- [ ] Использовать Motion System для анимаций

### ✅ Рекомендации

- [ ] Использовать `.brand-heading` для основных заголовков
- [ ] Использовать `.brand-gradient` для премиальных элементов
- [ ] Использовать `.brand-surface-card` для брендовых карточек
- [ ] Проверять визуальное соответствие бренду

---

## 🎯 Итог

LenaLink Brand Identity — это полностью интегрированная бренд-тема, которая:

- ✅ Соответствует премиальному travel-стилю
- ✅ Использует уникальную синюю палитру
- ✅ Интегрирована с Theme System v2
- ✅ Не нарушает UI-KIT и Motion System
- ✅ Готова к продакшену

**Статус:** ✅ **ЗАВЕРШЕНО**

---

## 📚 Дополнительные ресурсы

- **Theme System v2:** `frontend/src/app/globals.css`
- **UI-KIT:** Компоненты в `frontend/src/shared/ui/`
- **Motion System:** Токены в `globals.css`

---

**Версия документа:** 1.0  
**Последнее обновление:** 2024





