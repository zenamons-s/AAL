# Оставшиеся проблемы после исправлений

**Дата проверки:** 2025-01-XX  
**Статус:** Требуют исправления

---

## 🟡 Важные проблемы (остались)

### 1. Использование `rounded-md` вместо `rounded-sm` (19 мест)

**Файлы, требующие исправления:**

1. **`frontend/src/modules/transport/features/transport-section/ui/rent-tab.tsx:192`**
   - `rounded-md` для изображения транспорта

2. **`frontend/src/shared/ui/error-boundary/error-fallback.tsx:33`**
   - `rounded-md` для блока ошибки

3. **`frontend/src/modules/routes/features/route-search/ui/trip-class-select.tsx:129`**
   - `rounded-md` для выпадающего списка

4. **`frontend/src/modules/hotels/features/hotel-search/ui/hotels-sort-dropdown.tsx:61`**
   - `rounded-md` для элементов выпадающего списка

5. **`frontend/src/modules/transport/features/transport-section/ui/transport-section.tsx:62`**
   - `rounded-md` для кнопок фильтров

6. **`frontend/src/modules/hotels/features/hotel-search/ui/hotels-filters.tsx:26`**
   - `rounded-md` для кнопок фильтров

7. **`frontend/src/modules/hotels/features/hotel-search/ui/hotel-card-skeleton.tsx:8`**
   - `rounded-md` для скелетона изображения

8. **`frontend/src/modules/transport/features/transport-section/ui/rent-filters.tsx:28`**
   - `rounded-md` для кнопок фильтров

9. **`frontend/src/modules/services/features/services-section/ui/services-section.tsx:62`**
   - `rounded-md` для кнопок фильтров

10. **`frontend/src/modules/routes/features/route-details/ui/route-details-skeleton.tsx`** (7 мест)
    - Строки 7, 8, 9, 13, 16, 22, 25 - `rounded-md` для элементов скелетона

11. **`frontend/src/modules/routes/features/route-details/ui/route-alternatives.tsx:94, 110`**
    - `rounded-md` для блоков альтернативных маршрутов (2 места)

12. **`frontend/src/shared/ui/data-mode-badge/data-mode-badge.tsx:68`**
    - `rounded-md` для бейджа режима данных

**Рекомендация:**  
Заменить все `rounded-md` на `rounded-sm` для соответствия минималистичному стилю Skyscanner.

---

### 2. Использование `shadow-md` вместо `shadow-sm` (1 место)

**Файл:**
- **`frontend/src/modules/routes/features/route-search/ui/trip-class-select.tsx:129`**
  - `shadow-md` для выпадающего списка

**Рекомендация:**  
Заменить `shadow-md` на `shadow-sm` для более деликатной тени.

---

### 3. Неиспользуемые CSS переменные

**Проблема:**  
В `globals.css` определены переменные `--color-card-shadow` и `--color-card-shadow-hover`, но они не используются в классе `.card`.

**Местоположение:**  
- `frontend/src/app/globals.css:43-44`

**Текущее состояние:**
```css
--color-card-shadow: rgba(0, 0, 0, 0.04);
--color-card-shadow-hover: rgba(0, 0, 0, 0.08);
```

**Класс `.card` использует:**
```css
box-shadow: var(--shadow-sm);  /* вместо var(--color-card-shadow) */
```

**Рекомендация:**  
Либо использовать эти переменные в `.card`, либо удалить их для чистоты кода. Рекомендуется удалить, так как `--shadow-sm` и `--shadow-md` уже используются и более универсальны.

---

## 🟢 Нормальные inline styles (не требуют исправления)

Следующие inline styles являются нормальными и не требуют исправления:

1. **`letterSpacing: '-0.01em'`** - специфичное свойство для типографики, допустимо
2. **`boxShadow: 'none'`** - для логотипов, допустимо
3. **`backgroundColor: 'var(--color-primary)'`** - использование CSS переменных, допустимо
4. **`min-h-[400px]`** и **`min-w-[200px]`** - адаптивные размеры, допустимо

---

## 📊 Итоговая статистика

- **Осталось исправить:** 21 место
  - `rounded-md` → `rounded-sm`: 19 мест
  - `shadow-md` → `shadow-sm`: 1 место
  - Неиспользуемые переменные: 1 место

- **Приоритет:** Средний (не критично, но важно для полной консистентности)

---

## ✅ Рекомендации

1. Заменить все оставшиеся `rounded-md` на `rounded-sm`
2. Заменить `shadow-md` в trip-class-select на `shadow-sm`
3. Удалить неиспользуемые переменные `--color-card-shadow` и `--color-card-shadow-hover`

**После этих исправлений стиль будет полностью консистентным!** 🎯

