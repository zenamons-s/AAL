# 📋 ПОЛНЫЙ АУДИТ ФРОНТЕНДА ПОСЛЕ ИЗМЕНЕНИЯ ВИЗУАЛЬНОГО СТИЛЯ

**Дата:** 2025-01-XX  
**Статус:** 🔴 Критические проблемы обнаружены  
**Приоритет:** Высокий

---

## 📊 СВОДКА

- **Всего проблем:** 47
- **Критические:** 18
- **Высокие:** 15
- **Средние:** 10
- **Низкие:** 4

---

## 🔴 1. КОНФЛИКТЫ СТИЛЕЙ

### 1.1. Старые CSS-классы `yakutia-*` (КРИТИЧНО)

**Проблема:** Обнаружено **235+ использований** старых классов `yakutia-*`, которые не определены в новой системе.

**Файлы:**
- `routes/page.tsx` (2 использования: `rounded-yakutia`, `yakutia-transition`)
- `taxi-tab.tsx` (множественные: `yakutia-card`, `rounded-yakutia`, `yakutia-transition`)
- `rent-tab.tsx` (множественные: `yakutia-card`, `rounded-yakutia`, `yakutia-transition`)
- `bus-tab.tsx` (множественные: `yakutia-card`, `rounded-yakutia`, `yakutia-transition`)
- `hotels-search-form.tsx` (множественные)
- `hotel-card.tsx` (множественные)
- `favorites-section.tsx` (множественные)
- `tours-tab.tsx` (множественные)
- `error-fallback.tsx` (`yakutia-card`, `rounded-yakutia`, `yakutia-transition`)
- `routes/details/page.tsx` (`yakutia-pattern`)

**Конфликт:** Эти классы не определены в `globals.css`, что приведет к отсутствию стилей.

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Заменить все `yakutia-*` классы на новые:
- `yakutia-card` → `card`
- `rounded-yakutia` → `rounded-md`
- `yakutia-transition` → `transition-fast`
- `yakutia-pattern` → `bg-background`

---

### 1.2. Устаревшие CSS-переменные (КРИТИЧНО)

**Проблема:** Использование несуществующих переменных `--color-text-dark` и `--color-text-light`.

**Найдено:** **148+ использований** в inline-стилях.

**Файлы:**
- Все компоненты в `route-details/` (6 файлов)
- `taxi-tab.tsx`
- `rent-tab.tsx`
- `bus-tab.tsx`
- `hotels-search-form.tsx`
- `hotel-card.tsx`
- `favorites-section.tsx`
- `tours-tab.tsx`
- `error-fallback.tsx`

**Конфликт:** 
- `--color-text-dark` не существует → должно быть `--color-text-primary` или `--color-text-heading`
- `--color-text-light` не существует → должно быть `--color-text-secondary`

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Заменить:
- `var(--color-text-dark)` → `var(--color-text-primary)` или `var(--color-text-heading)`
- `var(--color-text-light)` → `var(--color-text-secondary)`

---

### 1.3. Несуществующие переменные фона (КРИТИЧНО)

**Проблема:** Использование `--color-background-mid` и `--color-background-end` в градиентах.

**Найдено:** 4 использования.

**Файлы:**
- `rent-tab.tsx:225`
- `tours-tab.tsx:17`
- `favorites-section.tsx:133`
- `hotel-card.tsx:21`

**Конфликт:** Эти переменные не определены в `globals.css`.

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Заменить на:
```css
linear-gradient(135deg, var(--color-background-subtle), var(--color-surface-hover))
```

---

### 1.4. Хардкоженные Tailwind цвета (ВЫСОКО)

**Проблема:** Использование хардкоженных цветов Tailwind вместо переменных темы.

**Найдено:** **78+ использований**.

**Примеры:**
- `bg-white` → должно быть `bg-card-bg` или `bg-surface`
- `text-gray-600` → должно быть `text-secondary`
- `bg-blue-50`, `text-blue-800` → должно быть через переменные
- `text-green-600` → должно быть через переменные
- `bg-gray-200`, `bg-gray-300` → должно быть через переменные

**Файлы:**
- Все компоненты `route-details/` (6 файлов)
- `route-risk-badge.tsx`
- `route-details-skeleton.tsx`
- `route-details-error.tsx`

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Заменить все хардкоженные цвета на переменные темы или классы из дизайн-системы.

---

### 1.5. Хардкоженные радиусы и тени (СРЕДНЕ)

**Проблема:** Использование `rounded-lg`, `shadow-md` вместо переменных.

**Найдено:** **21+ использование**.

**Файлы:**
- Все компоненты `route-details/`

**Конфликт:** Не соответствует новой системе, где используются `rounded-md` и `shadow-sm`.

**Приоритет:** 🟡 СРЕДНЕ

**Рекомендация:** Заменить:
- `rounded-lg` → `rounded-md` (если нужен больший радиус, использовать `rounded-lg` из переменных)
- `shadow-md` → `shadow-sm` или `shadow-md` (проверить соответствие новым значениям)

---

## 🔴 2. СТРУКТУРА ГЛОБАЛЬНЫХ СТИЛЕЙ

### 2.1. Отсутствие определения старых классов (КРИТИЧНО)

**Проблема:** Классы `yakutia-*` используются, но не определены в `globals.css`.

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Либо удалить все использования, либо добавить определения (не рекомендуется).

---

### 2.2. Несоответствие отступов в карточках (ВЫСОКО)

**Проблема:** Разные отступы в карточках:
- Новые компоненты: `p-5`
- Старые компоненты: `p-6`, `p-[18px]`, `p-8`

**Найдено:** Множественные использования.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Унифицировать:
- Стандартные карточки: `p-5`
- Компактные: `p-4`
- Просторные: `p-6`

---

### 2.3. Несоответствие отступов заголовков (ВЫСОКО)

**Проблема:** Разные отступы заголовков:
- Новые: `mb-2`, `mb-3`
- Старые: `mb-4`, `mb-6`

**Найдено:** Во всех компонентах `route-details/`.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Унифицировать:
- Заголовки H1: `mb-3`
- Заголовки H2: `mb-3`
- Подзаголовки: `mb-2`

---

## 🔴 3. ПРОВЕРКА КОМПОНЕНТОВ

### 3.1. Route Details компоненты (КРИТИЧНО)

**Проблема:** Все 6 компонентов используют старый стиль.

**Файлы:**
1. `route-summary.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → должно быть `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ `text-gray-600` → `text-secondary`
   - ❌ `border-blue-500`, `border-green-500` → через переменные

2. `route-pricing.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ `bg-blue-50`, `text-green-600` → через переменные
   - ❌ `text-gray-600` → `text-secondary`

3. `route-risk-assessment.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ Хардкоженные цвета риска → через переменные

4. `route-schedule.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ `text-gray-600` → `text-secondary`
   - ❌ `bg-green-100`, `bg-gray-100` → через переменные

5. `route-segments.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ `bg-blue-500`, `text-blue-800`, `bg-blue-100` → через переменные
   - ❌ `bg-gray-300`, `text-gray-600` → через переменные

6. `route-alternatives.tsx`
   - ❌ `bg-white rounded-lg shadow-md p-6` → `card p-5`
   - ❌ `var(--color-text-dark)` → `var(--color-text-heading)`
   - ❌ `text-gray-600` → `text-secondary`
   - ❌ `border-blue-500`, `bg-blue-50`, `text-blue-800` → через переменные

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Полностью переписать все компоненты на новый стиль.

---

### 3.2. Transport модули (КРИТИЧНО)

**Проблема:** Все компоненты используют старые классы и переменные.

**Файлы:**
1. `taxi-tab.tsx`
   - ❌ `yakutia-card` → `card`
   - ❌ `rounded-yakutia` → `rounded-md`
   - ❌ `yakutia-transition` → `transition-fast`
   - ❌ `var(--color-text-light)` → `var(--color-text-secondary)`
   - ❌ `p-[18px]` → `p-5`

2. `rent-tab.tsx`
   - ❌ Те же проблемы + `--color-background-mid`, `--color-background-end`

3. `bus-tab.tsx`
   - ❌ Те же проблемы

**Приоритет:** 🔴 КРИТИЧНО

**Рекомендация:** Полностью обновить все компоненты.

---

### 3.3. Hotels модули (КРИТИЧНО)

**Проблема:** Использование старых классов и переменных.

**Файлы:**
- `hotels-section.tsx` (использует `yakutia-card`)
- `hotels-search-form.tsx` (множественные проблемы)
- `hotel-card.tsx` (множественные проблемы)

**Приоритет:** 🔴 КРИТИЧНО

---

### 3.4. Services модули (ВЫСОКО)

**Проблема:** Использование старых классов.

**Файлы:**
- `tours-tab.tsx`
- `packages-tab.tsx`
- `individual-services-tab.tsx`

**Приоритет:** 🟠 ВЫСОКО

---

### 3.5. Favorites модули (ВЫСОКО)

**Проблема:** Использование старых классов и переменных.

**Файл:** `favorites-section.tsx`

**Приоритет:** 🟠 ВЫСОКО

---

### 3.6. Error Boundary (ВЫСОКО)

**Проблема:** Использование старых классов.

**Файл:** `error-fallback.tsx`
- ❌ `yakutia-card` → `card`
- ❌ `rounded-yakutia` → `rounded-md`
- ❌ `yakutia-transition` → `transition-fast`
- ❌ `yakutia-pattern` → `bg-background`

**Приоритет:** 🟠 ВЫСОКО

---

### 3.7. Route Risk Badge (СРЕДНЕ)

**Проблема:** Хардкоженные цвета Tailwind.

**Файл:** `route-risk-badge.tsx`
- ❌ `text-green-600 bg-green-50` → через переменные
- ❌ `text-blue-600 bg-blue-50` → через переменные
- ❌ И т.д.

**Приоритет:** 🟡 СРЕДНЕ

---

## 🔴 4. ПРОВЕРКА LAYOUT

### 4.1. Несоответствие контейнеров (ВЫСОКО)

**Проблема:** Разные контейнеры:
- Новые: `container-main` (max-width: 1200px)
- Старые: `container mx-auto px-4` + `max-w-[1300px]`

**Найдено:**
- `routes/details/page.tsx` (использует старый контейнер)
- `error-fallback.tsx` (использует старый контейнер)

**Конфликт:** Разная ширина и отступы.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Заменить на `container-main`.

---

### 4.2. Несоответствие отступов секций (ВЫСОКО)

**Проблема:** Разные отступы:
- Новые: `section-spacing-compact` (16px)
- Старые: `py-6 md:py-8`, `py-12`

**Найдено:** В `routes/details/page.tsx`.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Унифицировать на `section-spacing-compact`.

---

### 4.3. Старый фон страницы (ВЫСОКО)

**Проблема:** Использование `yakutia-pattern` вместо `bg-background`.

**Найдено:**
- `routes/details/page.tsx` (4 использования)
- `error-fallback.tsx` (1 использование)

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Заменить на `bg-background`.

---

## 🔴 5. ПРОВЕРКА ТЕМИЗАЦИИ

### 5.1. Inline-стили вместо классов (ВЫСОКО)

**Проблема:** Множественные inline-стили вместо использования классов.

**Найдено:** **350+ использований** `style={{}}`.

**Примеры:**
- `style={{ color: 'var(--color-text-dark)' }}` → должно быть класс `text-primary` или `text-heading`
- `style={{ backgroundColor: 'var(--color-primary)' }}` → должно быть класс `bg-primary`

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Заменить inline-стили на классы Tailwind или утилитарные классы из дизайн-системы.

---

### 5.2. Хардкоженные значения в inline-стилях (СРЕДНЕ)

**Проблема:** Хардкоженные цвета в inline-стилях.

**Примеры:**
- `color: '#FFFFFF'` → `text-inverse`
- `color: '#ff6b6b'` → через переменную ошибки
- `backgroundColor: 'rgba(255, 0, 0, 0.1)'` → через переменную

**Приоритет:** 🟡 СРЕДНЕ

---

### 5.3. Отсутствие переменных для состояний (СРЕДНЕ)

**Проблема:** Нет переменных для цветов состояний (success, warning, error, info) в некоторых компонентах.

**Найдено:** В `route-risk-badge.tsx` используются хардкоженные цвета.

**Приоритет:** 🟡 СРЕДНЕ

**Рекомендация:** Использовать переменные `--color-success`, `--color-warning`, `--color-error`, `--color-info`.

---

## 🔴 6. ПРОВЕРКА АДАПТИВНОСТИ

### 6.1. Конфликтующие breakpoints (НИЗКО)

**Проблема:** Разные подходы к адаптивности, но не критично.

**Приоритет:** 🟢 НИЗКО

---

### 6.2. Несоответствие отступов на мобильных (НИЗКО)

**Проблема:** Некоторые компоненты могут иметь слишком большие отступы на мобильных.

**Приоритет:** 🟢 НИЗКО

---

## 🔴 7. ПРОВЕРКА ВИЗУАЛЬНОЙ ЦЕЛОСТНОСТИ

### 7.1. Смешение старых и новых стилей (КРИТИЧНО)

**Проблема:** На одной странице могут быть элементы в старом и новом стиле.

**Пример:** Страница `routes/details` использует новые компоненты (Header, Footer), но старые компоненты деталей маршрута.

**Приоритет:** 🔴 КРИТИЧНО

---

### 7.2. Несоответствие размеров заголовков (ВЫСОКО)

**Проблема:** Разные размеры заголовков:
- Новые: `text-3xl/4xl/5xl` с `font-semibold`
- Старые: `text-2xl/3xl` с `font-bold`

**Найдено:** Во всех компонентах `route-details/`.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Унифицировать:
- H1: `text-3xl md:text-4xl lg:text-5xl font-semibold`
- H2: `text-xl font-medium`

---

### 7.3. Несоответствие кнопок (ВЫСОКО)

**Проблема:** Разные стили кнопок:
- Новые: класс `btn-primary` (компактные, 10px 20px)
- Старые: inline-стили с `px-6 py-2` или `px-6 py-3`

**Найдено:** Во многих компонентах.

**Приоритет:** 🟠 ВЫСОКО

**Рекомендация:** Использовать класс `btn-primary` везде.

---

## 📋 ПРИОРИТЕТНЫЙ ПЛАН ИСПРАВЛЕНИЙ

### Фаза 1: Критические проблемы (День 1-2)
1. ✅ Заменить все `yakutia-*` классы
2. ✅ Заменить все `--color-text-dark` и `--color-text-light`
3. ✅ Заменить `--color-background-mid/end`
4. ✅ Обновить все Route Details компоненты (6 файлов)
5. ✅ Обновить контейнеры в `routes/details/page.tsx` и `error-fallback.tsx`

### Фаза 2: Высокие проблемы (День 3-4)
6. ✅ Обновить Transport модули (3 файла)
7. ✅ Обновить Hotels модули (3 файла)
8. ✅ Обновить Services модули (3 файла)
9. ✅ Обновить Favorites модули (1 файл)
10. ✅ Заменить хардкоженные Tailwind цвета на переменные

### Фаза 3: Средние проблемы (День 5)
11. ✅ Унифицировать отступы
12. ✅ Унифицировать заголовки
13. ✅ Унифицировать кнопки
14. ✅ Заменить inline-стили на классы где возможно

### Фаза 4: Низкие проблемы (День 6)
15. ✅ Проверить адаптивность
16. ✅ Финальная визуальная проверка

---

## 📊 СТАТИСТИКА

### По типам проблем:
- **Старые классы:** 235+ использований
- **Устаревшие переменные:** 148+ использований
- **Хардкоженные цвета:** 78+ использований
- **Inline-стили:** 350+ использований
- **Несоответствие отступов:** 50+ мест

### По модулям:
- **Route Details:** 6 файлов, все требуют обновления
- **Transport:** 3 файла, все требуют обновления
- **Hotels:** 3 файла, все требуют обновления
- **Services:** 3 файла, требуют обновления
- **Favorites:** 1 файл, требует обновления
- **Shared UI:** 2 файла, требуют обновления

---

## ✅ РЕКОМЕНДАЦИИ

1. **Немедленно:** Исправить критические проблемы (Фаза 1)
2. **В течение недели:** Исправить высокие проблемы (Фаза 2-3)
3. **Долгосрочно:** Создать систему токенов для всех цветов состояний
4. **Долгосрочно:** Минимизировать использование inline-стилей
5. **Долгосрочно:** Создать Storybook для визуального тестирования компонентов

---

**Отчет подготовлен:** AI Assistant  
**Дата:** 2025-01-XX

