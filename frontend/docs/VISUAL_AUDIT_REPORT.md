# Полный аудит визуальных изменений под стиль Skyscanner

**Дата проведения:** 2025-01-XX  
**Цель:** Выявление всех конфликтов, несостыковок, визуальных артефактов и несовместимостей после обновления стиля

---

## 1. Конфликты в глобальных стилях

### 🔴 Критично: Несоответствие цвета header текста

**Проблема:**  
В `globals.css` переменная `--color-header-text: #212529` использует старый цвет, который не соответствует новому `--color-text-primary: #1a1a1a`.

**Местоположение:**  
- `frontend/src/app/globals.css:49`

**Объяснение:**  
Это создаёт визуальную несогласованность: header использует более тёмный оттенок (#212529), чем основной текст (#1a1a1a), что нарушает единообразие палитры.

**Рекомендация:**  
Изменить `--color-header-text` на `#1a1a1a` для соответствия `--color-text-primary`.

---

### 🟡 Важно: Неиспользуемые переменные теней карточек

**Проблема:**  
В `globals.css` определены `--color-card-shadow` и `--color-card-shadow-hover`, но они не используются в классе `.card`.

**Местоположение:**  
- `frontend/src/app/globals.css:43-44`
- `frontend/src/app/globals.css:170-185`

**Объяснение:**  
Класс `.card` использует `var(--shadow-sm)` напрямую, а не переменные `--color-card-shadow`. Это создаёт дублирование и потенциальную несогласованность.

**Рекомендация:**  
Либо использовать `--color-card-shadow` в `.card`, либо удалить неиспользуемые переменные для чистоты кода.

---

### 🟡 Важно: Конфликт в определении h1

**Проблема:**  
В `globals.css` для `h1` установлен `font-size: 2rem` и `font-weight: var(--font-weight-semibold)`, но во всех компонентах используется `font-medium` и меньшие размеры.

**Местоположение:**  
- `frontend/src/app/globals.css:128-131`
- Все компоненты с заголовками используют `font-medium` и `text-2xl md:text-3xl lg:text-4xl`

**Объяснение:**  
Глобальные стили для `h1` переопределяются в компонентах, что создаёт конфликт и несоответствие между глобальными правилами и фактическим использованием.

**Рекомендация:**  
Либо обновить глобальные стили `h1` для соответствия новому стилю (меньший размер, `font-medium`), либо явно переопределить их в компонентах.

---

## 2. Визуальная консистентность Header

### 🟡 Важно: Смешение inline styles и классов для тени

**Проблема:**  
В `header.tsx` используется inline style `boxShadow: '0 1px 1px 0 rgba(0, 0, 0, 0.02)'`, который не соответствует переменной `--shadow-sm: 0 1px 1px 0 rgba(0, 0, 0, 0.02)`.

**Местоположение:**  
- `frontend/src/shared/ui/header/header.tsx:16`

**Объяснение:**  
Хотя значения совпадают, использование inline style вместо CSS переменной нарушает принцип единого источника истины и усложняет поддержку.

**Рекомендация:**  
Использовать класс с `box-shadow: var(--shadow-sm)` или создать утилитарный класс для тени header.

---

### 🟢 Рекомендовано: Размер иконок в header

**Проблема:**  
Иконки в header имеют размер `w-3.5 h-3.5`, что может быть слишком маленьким для удобства использования.

**Местоположение:**  
- `frontend/src/shared/ui/header/header.tsx:36, 44`

**Объяснение:**  
Хотя это соответствует минималистичному стилю, стоит проверить доступность и удобство использования на разных устройствах.

**Рекомендация:**  
Провести тестирование на мобильных устройствах и при необходимости увеличить до `w-4 h-4`.

---

## 3. Визуальная консистентность Footer

### 🟢 Рекомендовано: Использование inline opacity для разделителей

**Проблема:**  
В footer разделители используют inline style `opacity: 0.4`, что не соответствует системе дизайна.

**Местоположение:**  
- `frontend/src/shared/ui/footer/footer.tsx:38, 48`

**Объяснение:**  
Использование inline styles для opacity усложняет поддержку и не позволяет легко изменять стиль через CSS переменные.

**Рекомендация:**  
Создать CSS класс или использовать переменную для opacity разделителей.

---

### 🟡 Важно: Смешение inline styles и классов для тени

**Проблема:**  
Аналогично header, footer использует inline style для тени.

**Местоположение:**  
- `frontend/src/shared/ui/footer/footer.tsx:16`

**Рекомендация:**  
Использовать CSS переменную или класс для тени.

---

## 4. Проверка типографики

### 🔴 Критично: Конфликт глобальных стилей h1 с компонентами

**Проблема:**  
Глобальные стили для `h1` (2rem, semibold) не соответствуют фактическому использованию в компонентах (text-2xl, medium).

**Местоположение:**  
- `frontend/src/app/globals.css:128-131`
- Все компоненты с заголовками

**Объяснение:**  
Это создаёт конфликт между глобальными правилами и локальными переопределениями, что может привести к неожиданному поведению.

**Рекомендация:**  
Обновить глобальные стили `h1` для соответствия новому стилю или явно сбросить их в компонентах.

---

### 🟡 Важно: Использование text-6xl для эмодзи

**Проблема:**  
В `route-details-error.tsx` используется `text-6xl` для эмодзи, что слишком большое для минималистичного стиля.

**Местоположение:**  
- `frontend/src/modules/routes/features/route-details/ui/route-details-error.tsx:37`

**Объяснение:**  
Размер `text-6xl` (48px) слишком большой и выбивается из общей типографической системы.

**Рекомендация:**  
Уменьшить до `text-4xl` или `text-3xl` для соответствия стилю.

---

## 5. Проверка рамок, контуров и теней

### 🟡 Важно: Использование rounded-md вместо rounded-sm

**Проблема:**  
В нескольких компонентах используется `rounded-md` (4px), что соответствует новому стилю, но в некоторых местах может быть слишком большим для минималистичного стиля Skyscanner.

**Местоположение:**  
- `frontend/src/shared/ui/city-autocomplete/city-autocomplete.tsx:287` (выпадающий список)
- `frontend/src/modules/routes/features/route-details/ui/route-schedule.tsx:48`
- `frontend/src/modules/routes/features/route-details/ui/route-pricing.tsx:63, 83, 93`
- И другие компоненты

**Объяснение:**  
Для более минималистичного стиля Skyscanner рекомендуется использовать `rounded-sm` (3px) вместо `rounded-md` (4px) для большинства элементов.

**Рекомендация:**  
Пересмотреть использование `rounded-md` и заменить на `rounded-sm` там, где это уместно.

---

### 🟡 Важно: Использование shadow-md и hover:shadow-md

**Проблема:**  
В некоторых компонентах используется `shadow-md` и `hover:shadow-md`, что может быть слишком заметно для деликатного стиля Skyscanner.

**Местоположение:**  
- `frontend/src/modules/routes/features/route-details/ui/route-schedule.tsx:48`
- `frontend/src/modules/routes/features/route-details/ui/route-pricing.tsx:93`
- `frontend/src/shared/ui/offline-notification/offline-notification.tsx:41`

**Объяснение:**  
Для стиля Skyscanner тени должны быть очень деликатными. `shadow-md` может быть слишком заметным.

**Рекомендация:**  
Заменить `shadow-md` на `shadow-sm` для более деликатного эффекта.

---

### 🟡 Важно: Использование border-2 для спиннера

**Проблема:**  
В `routes/page.tsx` используется `border-2` для спиннера загрузки, что может быть слишком толстым.

**Местоположение:**  
- `frontend/src/app/routes/page.tsx:130, 415`

**Объяснение:**  
Для минималистичного стиля рекомендуется использовать `border` (1px) вместо `border-2` (2px).

**Рекомендация:**  
Заменить `border-2` на `border` для более тонкого вида.

---

### 🟡 Важно: Использование rounded-full для assistant-button

**Проблема:**  
В `assistant-button.tsx` используется `rounded-full`, что правильно, но размер кнопки `w-[70px] h-[70px]` может быть слишком большим для минималистичного стиля.

**Местоположение:**  
- `frontend/src/shared/ui/assistant-button/assistant-button.tsx:12`

**Объяснение:**  
Размер 70px может быть слишком заметным. Для более деликатного стиля рекомендуется уменьшить до 56-60px.

**Рекомендация:**  
Уменьшить размер до `w-14 h-14` (56px) или `w-[60px] h-[60px]` для более минималистичного вида.

---

### 🟡 Важно: Использование shadow-lg в assistant-button

**Проблема:**  
В `assistant-button.tsx` используется `hover:shadow-lg`, что может быть слишком заметно.

**Местоположение:**  
- `frontend/src/shared/ui/assistant-button/assistant-button.tsx:15`

**Объяснение:**  
Для деликатного стиля Skyscanner `shadow-lg` может быть слишком заметным при hover.

**Рекомендация:**  
Заменить `hover:shadow-lg` на `hover:shadow-md` для более деликатного эффекта.

---

## 6. Проверка полей ввода и форм

### 🟢 Рекомендовано: Консистентность использования класса .input

**Проблема:**  
Все поля ввода используют класс `.input`, что хорошо, но стоит проверить, что все поля действительно используют этот класс.

**Статус:**  
✅ Все поля ввода используют класс `.input` корректно.

**Рекомендация:**  
Продолжать использовать класс `.input` для всех полей ввода для единообразия.

---

### 🟡 Важно: Использование rounded-md в выпадающем списке city-autocomplete

**Проблема:**  
В `city-autocomplete.tsx` выпадающий список использует `rounded-md`, что может быть слишком большим для минималистичного стиля.

**Местоположение:**  
- `frontend/src/shared/ui/city-autocomplete/city-autocomplete.tsx:287`

**Объяснение:**  
Для более минималистичного стиля рекомендуется использовать `rounded-sm`.

**Рекомендация:**  
Заменить `rounded-md` на `rounded-sm` для выпадающего списка.

---

## 7. Проверка сетки и отступов

### 🟢 Рекомендовано: Использование фиксированной высоты в navigation-tabs

**Проблема:**  
В `navigation-tabs.tsx` используется фиксированная высота `40px` через inline style, что может быть слишком много для компактного стиля.

**Местоположение:**  
- `frontend/src/shared/ui/navigation-tabs/navigation-tabs.tsx:53`

**Объяснение:**  
Для более компактного стиля Skyscanner рекомендуется уменьшить высоту до 36px или использовать класс вместо inline style.

**Рекомендация:**  
Уменьшить высоту до `h-9` (36px) и использовать класс вместо inline style.

---

### 🟡 Важно: Смешение border-b-2 и inline borderBottomWidth

**Проблема:**  
В `navigation-tabs.tsx` используется `border-b-2` в className и `borderBottomWidth` в inline style, что создаёт конфликт.

**Местоположение:**  
- `frontend/src/shared/ui/navigation-tabs/navigation-tabs.tsx:45, 52`

**Объяснение:**  
Использование и класса, и inline style для одного свойства создаёт конфликт и усложняет поддержку.

**Рекомендация:**  
Использовать только классы или только inline styles, но не смешивать. Рекомендуется использовать классы для консистентности.

---

## 8. Проверка визуальной гармонии

### 🟡 Важно: Размер assistant-button

**Проблема:**  
Кнопка помощника имеет размер 70px, что может быть слишком заметным для минималистичного стиля.

**Местоположение:**  
- `frontend/src/shared/ui/assistant-button/assistant-button.tsx:12`

**Рекомендация:**  
Уменьшить размер до 56-60px для более деликатного вида.

---

### 🟢 Рекомендовано: Общая консистентность цветов

**Статус:**  
✅ Большинство компонентов используют CSS переменные корректно.

**Рекомендация:**  
Продолжать использовать CSS переменные для всех цветов для единообразия.

---

## Итоговая сводка

### Критичные проблемы (требуют немедленного исправления):
1. Несоответствие `--color-header-text` с новым стилем
2. Конфликт глобальных стилей `h1` с компонентами

### Важные проблемы (требуют исправления):
1. Использование `rounded-md` вместо `rounded-sm` в некоторых местах
2. Использование `shadow-md` и `hover:shadow-md` вместо более деликатных теней
3. Использование `border-2` для спиннера
4. Смешение inline styles и классов для теней и границ
5. Использование `text-6xl` для эмодзи
6. Размер assistant-button (70px)
7. Использование `shadow-lg` в assistant-button
8. Фиксированная высота 40px в navigation-tabs
9. Смешение border-b-2 и inline borderBottomWidth

### Рекомендованные улучшения:
1. Уменьшить размер иконок в header (опционально)
2. Использовать CSS классы вместо inline opacity для разделителей
3. Уменьшить высоту navigation-tabs до 36px
4. Общая проверка консистентности использования CSS переменных

---

## Приоритеты исправления

1. **Высокий приоритет:** Исправить критичные проблемы с цветами и типографикой
2. **Средний приоритет:** Исправить важные проблемы с тенями, радиусами и размерами
3. **Низкий приоритет:** Внести рекомендованные улучшения для полной консистентности

---

**Отчёт подготовлен:** 2025-01-XX  
**Статус:** Готов к исправлению

