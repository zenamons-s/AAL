# Отчёт: Связывание реальных остановок с виртуальной сеткой

## Проблема

После создания полной двусторонней сетки виртуальных маршрутов между виртуальными городами граф имел следующую структуру:

- **Виртуальные остановки**: полностью связаны между собой (вирт ↔ вирт)
- **Реальные остановки**: не связаны с виртуальной сеткой
- **Проблема**: реальные города были недостижимы из виртуальных и наоборот

**Результат:**
- PathFinder не мог найти маршруты между реальными и виртуальными городами
- Реальные узлы имели недостаточную связность (<2 соседей)
- Граф был разделён на две несвязанные части: реальные и виртуальные

## Решение

Реализованы **двусторонние связи между реальными и виртуальными остановками**. Для каждой пары (real, virtual) создаются два маршрута:

- **real → virtual** (прямое направление)
- **virtual → real** (обратное направление)

Это обеспечивает полную связность графа - любой реальный город достижим из любого виртуального и наоборот.

## Выполненные изменения

### 1. Создан метод `createRealToVirtualConnections`

**Файл:** `backend/src/application/data-loading/DataRecoveryService.ts`

**Метод:** `createRealToVirtualConnections()`

**Функциональность:**
- Получает все реальные остановки (не виртуальные)
- Получает все виртуальные остановки
- Для каждой пары (real, virtual) создаёт два маршрута: real → virtual и virtual → real
- Использует стабильные ID через `generateVirtualRouteId`
- Генерирует виртуальные рейсы на год вперёд для каждого маршрута
- Добавляет маршруты и рейсы в датасет

**Код:**
```typescript
private async createRealToVirtualConnections(
  dataset: ITransportDataset
): Promise<{ dataset: ITransportDataset; virtualRoutesCount: number }> {
  // Получаем все реальные остановки (не виртуальные)
  const realStops = dataset.stops.filter(stop => !stop.metadata?._virtual);
  
  // Получаем все виртуальные остановки
  const virtualStops = dataset.stops.filter(stop => stop.metadata?._virtual === true);
  
  // Для каждой пары (real, virtual) создаём ОБА маршрута
  for (const realStop of realStops) {
    for (const virtualStop of virtualStops) {
      // Создаём маршрут real → virtual
      const routeIdRealToVirtual = generateVirtualRouteId(realStop.id, virtualStop.id);
      if (!existingRouteIds.has(routeIdRealToVirtual)) {
        const virtualRouteRealToVirtual = this.createVirtualRoute(...);
        virtualRoutes.push(virtualRouteRealToVirtual);
        const flightsRealToVirtual = this.generateVirtualFlights(...);
        virtualFlights.push(...flightsRealToVirtual);
      }
      
      // Создаём маршрут virtual → real (обратное направление)
      const routeIdVirtualToReal = generateVirtualRouteId(virtualStop.id, realStop.id);
      if (!existingRouteIds.has(routeIdVirtualToReal)) {
        const virtualRouteVirtualToReal = this.createVirtualRoute(...);
        virtualRoutes.push(virtualRouteVirtualToReal);
        const flightsVirtualToReal = this.generateVirtualFlights(...);
        virtualFlights.push(...flightsVirtualToReal);
      }
    }
  }
  
  return {
    dataset: {
      ...dataset,
      routes: [...dataset.routes, ...virtualRoutes],
      flights: [...dataset.flights, ...virtualFlights],
    },
    virtualRoutesCount: routesCreated,
  };
}
```

**Гарантии:**
- ✅ Для N реальных остановок и M виртуальных остановок создаётся 2 * N * M маршрутов
- ✅ Каждая реальная остановка связана со всеми виртуальными в обе стороны
- ✅ Маршруты добавляются только если их нет (проверка по стабильным ID)
- ✅ Генерируются рейсы на год вперёд для каждого маршрута

### 2. Интеграция в метод `recover`

**Файл:** `backend/src/application/data-loading/DataRecoveryService.ts`

**Метод:** `recover()`

**Изменения:**
- Добавлен вызов `createRealToVirtualConnections` после создания виртуальной сетки
- Логирование процесса создания связей
- Обновление метаданных с учётом созданных маршрутов

**Код:**
```typescript
// 7. КРИТИЧЕСКИ ВАЖНО: Создание двусторонних связей между реальными и виртуальными остановками
// Это обеспечивает полную связность графа - любой реальный город достижим из любого виртуального и наоборот
console.log('[DataRecoveryService.recover] 🔄 Создание двусторонних связей между реальными и виртуальными остановками...');
const realToVirtualResult = await this.createRealToVirtualConnections(recoveredDataset);
recoveredDataset = realToVirtualResult.dataset;
if (realToVirtualResult.virtualRoutesCount > 0) {
  appliedOperations.push('createRealToVirtualConnections');
  recoveredCount += realToVirtualResult.virtualRoutesCount;
  console.log(`[DataRecoveryService.recover] ✅ Создано ${realToVirtualResult.virtualRoutesCount} маршрутов между реальными и виртуальными остановками`);
}
```

**Гарантии:**
- ✅ Связи создаются после виртуальной сетки, но до обновления метаданных
- ✅ Прозрачность процесса создания связей
- ✅ Детальное логирование результата

### 3. Улучшена проверка связности реальных узлов в RouteGraphManager

**Файл:** `backend/src/application/route-builder/RouteGraphManager.ts`

**Метод:** `doInitialize()`

**Изменения:**
- Добавлена проверка связности реальных узлов после создания связей с виртуальными
- Проверка количества соседей, исходящих и входящих рёбер для каждого реального узла
- Показ примеров реальных узлов с наибольшим количеством соседей (топ-10)
- Проверка достижимости между реальными и виртуальными узлами
- Итоговая статистика связности графа

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Проверяем связность реальных узлов
// После создания связей между реальными и виртуальными остановками
// каждый реальный узел должен иметь ≥2 соседей
const allNodesForRealCheck = this.graph.getAllNodes();
const realNodes = allNodesForRealCheck.filter(n => !n.stopId.startsWith('virtual-stop-'));

const realNodesWithFewNeighbors: Array<{ stopId: string; cityName: string; neighbors: number; outgoingEdges: number; incomingEdges: number }> = [];

for (const realNode of realNodes) {
  const neighbors = this.graph.getNeighbors(realNode.stopId);
  const outgoingEdges = this.graph.getEdgesFrom(realNode.stopId);
  
  // Проверяем входящие рёбра
  let incomingEdges = 0;
  for (const otherNode of allNodesForRealCheck) {
    const otherEdges = this.graph.getEdgesFrom(otherNode.stopId);
    if (otherEdges.some(e => e.toStopId === realNode.stopId)) {
      incomingEdges++;
    }
  }
  
  if (neighbors.length < 2 || outgoingEdges.length < 2 || incomingEdges < 2) {
    realNodesWithFewNeighbors.push({ ... });
  }
}

// Проверяем, что любой реальный город достижим из любого виртуального и наоборот
if (realNodes.length > 0 && virtualNodesAfterGrid.length > 0) {
  const testRealNode = realNodes[0];
  const testVirtualNode = virtualNodesAfterGrid[0];
  
  const realNeighbors = this.graph.getNeighbors(testRealNode.stopId);
  const virtualNeighbors = this.graph.getNeighbors(testVirtualNode.stopId);
  
  const realHasVirtualNeighbor = realNeighbors.some(n => n.startsWith('virtual-stop-'));
  const virtualHasRealNeighbor = virtualNeighbors.some(n => !n.startsWith('virtual-stop-'));
  
  if (realHasVirtualNeighbor && virtualHasRealNeighbor) {
    console.log(`[RouteGraphManager]   ✅ Реальные и виртуальные узлы связаны`);
  }
}
```

**Гарантии:**
- ✅ Все реальные узлы имеют ≥2 соседей, ≥2 исходящих рёбер, ≥2 входящих рёбер
- ✅ Детальная проверка для тестовых городов (реальные и виртуальные)
- ✅ Проверка достижимости между реальными и виртуальными узлами
- ✅ Итоговая статистика связности графа

### 4. Улучшено логирование процесса создания

**Файл:** `backend/src/application/data-loading/DataRecoveryService.ts`

**Метод:** `createRealToVirtualConnections()`

**Изменения:**
- Прогресс-индикатор (обработано пар: X / Y)
- Статистика созданных связей (ожидаемое количество, покрытие)
- Примеры созданных связей (первые 20)
- Проверка связей для тестовых реальных остановок
- Проверка итогового датасета

**Код:**
```typescript
console.log(`[DataRecoveryService.createRealToVirtualConnections] 🔄 Создание связей между реальными и виртуальными остановками`);
console.log(`[DataRecoveryService.createRealToVirtualConnections] Реальных остановок: ${realStops.length}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections] Виртуальных остановок: ${virtualStops.length}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections] Ожидаемое количество маршрутов: ${2 * realStops.length * virtualStops.length}`);

// Прогресс-индикатор
if (processedPairs % 100 === 0) {
  console.log(`[DataRecoveryService.createRealToVirtualConnections] Обработано пар: ${processedPairs} / ${totalPairs} (${Math.round(processedPairs / totalPairs * 100)}%)`);
}

// Статистика
const expectedRoutes = 2 * realStops.length * virtualStops.length;
console.log(`[DataRecoveryService.createRealToVirtualConnections] 📊 Статистика создания связей:`);
console.log(`[DataRecoveryService.createRealToVirtualConnections]   - Реальных остановок: ${realStops.length}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections]   - Виртуальных остановок: ${virtualStops.length}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections]   - Создано маршрутов: ${routesCreated}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections]   - Ожидаемое количество маршрутов: ${expectedRoutes}`);
console.log(`[DataRecoveryService.createRealToVirtualConnections]   - Покрытие: ${routesCreated >= expectedRoutes ? '✅ 100%' : `⚠️ ${Math.round(routesCreated / expectedRoutes * 100)}%`}`);
```

**Гарантии:**
- ✅ Полная прозрачность процесса создания маршрутов
- ✅ Детальная статистика для диагностики
- ✅ Проверка связей для конкретных реальных остановок

## Гарантии системы

### ✅ Полная связность между реальными и виртуальными узлами

- Для каждой пары (real, virtual) создаются два маршрута: real → virtual и virtual → real
- Граф становится полностью связным - любой реальный город достижим из любого виртуального и наоборот
- Для N реальных остановок и M виртуальных остановок создаётся 2 * N * M маршрутов

### ✅ Стабильные ID

- Все маршруты используют стабильные ID через `generateVirtualRouteId`
- Проверка существования маршрутов по стабильным ID
- Один маршрут = один стабильный routeId

### ✅ Генерация рейсов

- Для каждого маршрута генерируются виртуальные рейсы на год вперёд
- Рейсы создаются для обоих направлений (real → virtual и virtual → real)
- Рейсы добавляются в датасет вместе с маршрутами

### ✅ Проверка связности реальных узлов

- Все реальные узлы имеют ≥2 соседей после создания связей с виртуальными
- Все реальные узлы имеют ≥2 исходящих рёбер
- Все реальные узлы имеют ≥2 входящих рёбер
- Проверка достижимости между реальными и виртуальными узлами

### ✅ Детальное логирование

- Список всех реальных и виртуальных остановок
- Статистика созданных связей (ожидаемое количество, покрытие)
- Примеры созданных связей
- Проверка связей для тестовых реальных остановок
- Итоговая статистика связности графа

## Проверка работы

### Примеры пар городов для тестирования:

1. **Реальный город → Виртуальный город**: Якутск → Верхоянск
2. **Виртуальный город → Реальный город**: Верхоянск → Якутск
3. **Реальный город → Виртуальный город**: Мирный → Олёкминск
4. **Виртуальный город → Реальный город**: Олёкминск → Мирный
5. **Смешанные пары**: любые комбинации реальных и виртуальных городов

### Ожидаемые логи:

```
[DataRecoveryService.recover] 🔄 Создание двусторонних связей между реальными и виртуальными остановками...
[DataRecoveryService.createRealToVirtualConnections] 🔄 Создание связей между реальными и виртуальными остановками
[DataRecoveryService.createRealToVirtualConnections] Реальных остановок: N
[DataRecoveryService.createRealToVirtualConnections] Виртуальных остановок: M
[DataRecoveryService.createRealToVirtualConnections] Ожидаемое количество маршрутов: 2 * N * M
[DataRecoveryService.createRealToVirtualConnections] ✅ Создан маршрут: Якутск → Верхоянск, routeId="...", flights=365
[DataRecoveryService.createRealToVirtualConnections] ✅ Создан маршрут: Верхоянск → Якутск, routeId="...", flights=365
[DataRecoveryService.createRealToVirtualConnections] 📊 Статистика создания связей:
[DataRecoveryService.createRealToVirtualConnections]   - Создано маршрутов: 2 * N * M
[DataRecoveryService.createRealToVirtualConnections]   - Покрытие: ✅ 100%
[RouteGraphManager] 🔍 Проверка связности реальных узлов после создания связей с виртуальными...
[RouteGraphManager] Всего реальных узлов в графе: N
[RouteGraphManager] ✅ Все N реальных узлов имеют достаточную связность (≥2 соседей, ≥2 исходящих рёбер, ≥2 входящих рёбер)
[RouteGraphManager]   ✅ Реальные и виртуальные узлы связаны: реальный узел имеет виртуальных соседей, виртуальный узел имеет реальных соседей
[PathFinder] 🔍 Поиск пути: fromStopId="..." (реальный) -> toStopId="..." (виртуальный)
[PathFinder] ✅ Узлы найдены: fromNode="Якутск" (реальный), toNode="Верхоянск" (виртуальный)
[PathFinder] Рёбер из fromStopId: ≥M, из toStopId: ≥N
[PathFinder] ✅ Обновлено расстояние до "..." (...): Infinity -> X (через "...")
[PathFinder] Целевой узел найден! Итераций: N, расстояние: M
```

### Критерии успеха:

- ✅ `routesCreated >= 2 * N * M` (где N = количество реальных остановок, M = количество виртуальных остановок)
- ✅ Все реальные узлы имеют ≥2 соседей, ≥2 исходящих рёбер, ≥2 входящих рёбер
- ✅ Реальные узлы имеют виртуальных соседей
- ✅ Виртуальные узлы имеют реальных соседей
- ✅ PathFinder показывает "рёбер из fromStopId: ≥M" для реальных узлов
- ✅ PathFinder показывает "рёбер из fromStopId: ≥N" для виртуальных узлов
- ✅ "осталось непосещённых" уменьшается
- ✅ Достижимые узлы > 20
- ✅ Дистанция до целевого ≠ Infinity
- ✅ Путь строится через промежуточные узлы

## Результат

После изменений система гарантирует:

- ✅ **Полная связность между реальными и виртуальными узлами**: Каждая реальная остановка связана со всеми виртуальными в обе стороны
- ✅ **Стабильные ID**: Все маршруты используют стабильные ID
- ✅ **Генерация рейсов**: Для каждого маршрута генерируются рейсы на год вперёд
- ✅ **Проверка связности**: Все реальные узлы имеют ≥2 соседей, ≥2 исходящих рёбер, ≥2 входящих рёбер
- ✅ **Достижимость**: Любой реальный город достижим из любого виртуального и наоборот
- ✅ **Детальное логирование**: Полная прозрачность процесса создания маршрутов

## Файлы изменены

1. `backend/src/application/data-loading/DataRecoveryService.ts`:
   - Добавлен метод `createRealToVirtualConnections()` для создания связей между реальными и виртуальными остановками
   - Интегрирован вызов метода в `recover()` после создания виртуальной сетки
   - Улучшено логирование процесса создания
   - Обновлены метаданные с учётом созданных маршрутов

2. `backend/src/application/route-builder/RouteGraphManager.ts`:
   - Улучшена проверка связности реальных узлов после создания связей с виртуальными
   - Добавлена проверка количества соседей, исходящих и входящих рёбер для каждого реального узла
   - Добавлена проверка достижимости между реальными и виртуальными узлами
   - Добавлена итоговая статистика связности графа

## Критерий готовности

✅ **PathFinder должен показывать:**
- "рёбер из fromStopId: ≥M" для реальных узлов (где M = количество виртуальных остановок)
- "рёбер из fromStopId: ≥N" для виртуальных узлов (где N = количество реальных остановок)
- "осталось непосещённых" должно уменьшаться
- Достижимые узлы > 20
- Дистанция до целевого ≠ Infinity
- Путь строится через промежуточные узлы

✅ **Все реальные узлы должны иметь:**
- ≥2 соседей
- ≥2 исходящих рёбер
- ≥2 входящих рёбер

✅ **Реальные и виртуальные узлы должны быть связаны:**
- Реальные узлы имеют виртуальных соседей
- Виртуальные узлы имеют реальных соседей

✅ **Поиск работает с датой и без даты для всех пар городов:**
- Реальный → Виртуальный
- Виртуальный → Реальный
- Реальный → Реальный (через виртуальные узлы)
- Виртуальный → Виртуальный (уже было реализовано ранее)

✅ **Маршруты находятся в обоих направлениях (A → B и B → A)**

## Итоговая структура графа

После всех изменений граф имеет следующую структуру:

1. **Виртуальная сетка (вирт ↔ вирт)**: Каждый виртуальный город связан со всеми остальными виртуальными городами напрямую
2. **Связи реальных с виртуальными (реал ↔ вирт)**: Каждая реальная остановка связана со всеми виртуальными остановками в обе стороны
3. **Реальные маршруты (реал ↔ реал)**: Существующие реальные маршруты из OData остаются без изменений

**Результат:** Граф становится полностью связным - любой город (реальный или виртуальный) достижим из любого другого города (реального или виртуального) через прямые или промежуточные узлы.


