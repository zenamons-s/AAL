# Отчёт: Исправление несоответствия ключей между nodes и edges в RouteGraph

## Проблема

PathFinder не мог найти путь между виртуальными городами, потому что:

1. **`edges.get(stopId)` возвращал `undefined`** - ключи в `edges` Map не совпадали с `stopId` узлов в `nodes` Map
2. **PathFinder видел только стартовый узел** - хотя в графе было 36 узлов и 70+ рёбер
3. **Несоответствие ключей** - узлы и рёбра использовали разные `stopId`
4. **Старые нестабильные ID** - виртуальные остановки создавались с временными ID, которые конфликтовали с новыми стабильными ID

## Решение

Реализована полная система очистки и валидации для гарантии соответствия ключей между `nodes` Map и `edges` Map.

## Выполненные изменения

### 1. Полная очистка кеша при старте

**Файл:** `backend/src/application/data-loading/TransportDataService.ts`

**Метод:** `loadData()`

**Изменения:**
- Добавлена очистка кеша перед загрузкой данных
- Это гарантирует, что старые виртуальные stopId не будут использоваться
- Кеш очищается через `cacheRepository.invalidate()`

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Очищаем кеш при старте для удаления старых нестабильных ID
console.log('[TransportDataService] 🔄 Очистка кеша перед загрузкой данных...');
try {
  if (this.cacheRepository) {
    await this.cacheRepository.invalidate(this.config.cacheKey);
    console.log('[TransportDataService] ✅ Кеш очищен');
  }
} catch (error) {
  console.log('[TransportDataService] ⚠️ Не удалось очистить кеш (возможно, Redis недоступен), продолжаем...');
}
```

**Гарантии:**
- ✅ Кеш полностью очищается при старте
- ✅ Старые нестабильные ID не используются
- ✅ Виртуальные остановки создаются заново со стабильными ID

### 2. Полный сброс RouteGraph при запуске

**Файл:** `backend/src/application/route-builder/RouteGraphManager.ts`

**Метод:** `doInitialize()`

**Изменения:**
- Добавлена очистка кеша транспортного датасета
- Полностью очищается старый граф перед построением нового
- Очищаются старые нестабильные virtual-stop- ID из датасета
- Удаляются маршруты и рейсы, которые ссылаются на удалённые остановки

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Очищаем кеш перед загрузкой данных
console.log('[RouteGraphManager] Очистка кеша транспортного датасета...');
// ... очистка кеша ...

// КРИТИЧЕСКИ ВАЖНО: Полностью очищаем старый граф
if (this.graph) {
  console.log('[RouteGraphManager] Очистка старого графа...');
  this.graph.clear();
  this.graph = null;
}

// КРИТИЧЕСКИ ВАЖНО: Проверяем и очищаем старые нестабильные virtual-stop- ID
const cleanedStops = this.dataset.stops.filter(stop => {
  if (stop.metadata?._virtual === true) {
    const cityName = stop.metadata?.cityName || stop.name;
    const expectedId = generateVirtualStopId(cityName);
    if (stop.id !== expectedId) {
      console.log(`[RouteGraphManager] ⚠️ Удаляем виртуальную остановку с нестабильным ID: "${stop.name}" (ID="${stop.id}", ожидается="${expectedId}")`);
      return false; // Удаляем остановку с нестабильным ID
    }
  }
  return true; // Оставляем остановку
});

// Аналогично очищаем маршруты и рейсы
```

**Гарантии:**
- ✅ Старый граф полностью очищается
- ✅ Старые edges Map очищаются
- ✅ Граф пересоздаётся строго из нового датасета
- ✅ Нестабильные ID удаляются из датасета

### 3. Строгая проверка стабильности virtual-stop- ID

**Файл:** `backend/src/application/route-builder/RouteGraphBuilder.ts`

**Метод:** `buildFromDataset()`

**Изменения:**
- Добавлена проверка формата виртуальных остановок перед добавлением в граф
- Остановки с нестабильными ID пропускаются
- Логирование всех пропущенных остановок

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Добавляем узлы из остановок
// Проверяем, что все stopId соответствуют стабильному формату для виртуальных остановок
for (const stop of dataset.stops) {
  // Проверяем формат виртуальных остановок
  if (stop.metadata?._virtual === true) {
    const cityName = stop.metadata?.cityName || stop.name;
    const expectedVirtualId = generateVirtualStopId(cityName);
    if (stop.id !== expectedVirtualId) {
      console.log(`[RouteGraphBuilder] ⚠️ ВНИМАНИЕ: Виртуальная остановка "${stop.name}" имеет нестабильный ID="${stop.id}", ожидается="${expectedVirtualId}"`);
      // Пропускаем остановку с нестабильным ID
      continue;
    }
  }
  
  // Добавляем узел в граф
  graph.addNode(node);
  validStopIds.add(stop.id);
}
```

**Гарантии:**
- ✅ Только стабильные ID добавляются в граф
- ✅ Нестабильные ID отбрасываются
- ✅ Один город = один стабильный stopId

### 4. Строгая проверка существования узлов при добавлении рёбер

**Файл:** `backend/src/application/route-builder/RouteGraph.ts`

**Метод:** `addEdge()`

**Изменения:**
- Рёбра добавляются ТОЛЬКО если оба узла существуют в nodes Map
- Если узла нет - ребро НЕ добавляется
- Детальное логирование для диагностики

**Код:**
```typescript
addEdge(edge: IRouteEdge): void {
  // КРИТИЧЕСКИ ВАЖНО: Проверяем, что оба узла существуют в nodes Map
  // Если узла нет - ребро НЕ добавляется, чтобы избежать несоответствия ключей
  if (!this.nodes.has(edge.fromStopId)) {
    console.log(`[RouteGraph.addEdge] ❌ ОШИБКА: Узел fromStopId="${edge.fromStopId}" не существует в графе! Ребро НЕ добавлено.`);
    console.log(`[RouteGraph.addEdge] Доступные узлы (первые 10):`, Array.from(this.nodes.keys()).slice(0, 10));
    return; // НЕ добавляем ребро, если узел не существует
  }
  
  if (!this.nodes.has(edge.toStopId)) {
    console.log(`[RouteGraph.addEdge] ❌ ОШИБКА: Узел toStopId="${edge.toStopId}" не существует в графе! Ребро НЕ добавлено.`);
    console.log(`[RouteGraph.addEdge] Доступные узлы (первые 10):`, Array.from(this.nodes.keys()).slice(0, 10));
    return; // НЕ добавляем ребро, если узел не существует
  }
  
  // Инициализируем список рёбер для fromStopId, если его нет
  if (!this.edges.has(edge.fromStopId)) {
    this.edges.set(edge.fromStopId, []);
  }
  
  // Проверяем, не добавлено ли уже такое ребро (избегаем дубликатов)
  const existingEdges = this.edges.get(edge.fromStopId)!;
  const isDuplicate = existingEdges.some(e => 
    e.toStopId === edge.toStopId && 
    e.segment.routeId === edge.segment.routeId
  );
  
  if (!isDuplicate) {
    existingEdges.push(edge);
    this.edges.set(edge.fromStopId, existingEdges);
  }
}
```

**Гарантии:**
- ✅ Рёбра добавляются только для существующих узлов
- ✅ edges Map использует те же ключи, что и nodes Map
- ✅ Нет orphaned ключей в edges Map

### 5. Валидация соответствия ключей между nodes и edges

**Файл:** `backend/src/application/route-builder/RouteGraph.ts`

**Метод:** `validateEdgesKeys()`

**Изменения:**
- Добавлен метод для проверки соответствия ключей
- Удаляются рёбра, которые ссылаются на несуществующие узлы
- Удаляются ключи в edges Map без соответствующих узлов

**Код:**
```typescript
private validateEdgesKeys(): { removedEdges: number; fixedEdges: number } {
  let removedEdges = 0;
  let fixedEdges = 0;
  
  // Проверяем все ключи в edges Map
  for (const [stopId, edges] of this.edges.entries()) {
    // Если узла нет в nodes Map - удаляем все его рёбра
    if (!this.nodes.has(stopId)) {
      console.log(`[RouteGraph.validateEdgesKeys] ⚠️ Удаляем рёбра для несуществующего узла: stopId="${stopId}" (${edges.length} рёбер)`);
      this.edges.delete(stopId);
      removedEdges += edges.length;
      continue;
    }
    
    // Проверяем каждое ребро на существование целевого узла
    const validEdges = edges.filter(edge => {
      if (!this.nodes.has(edge.toStopId)) {
        console.log(`[RouteGraph.validateEdgesKeys] ⚠️ Удаляем ребро: fromStopId="${edge.fromStopId}" -> toStopId="${edge.toStopId}" (целевой узел не существует)`);
        removedEdges++;
        return false;
      }
      return true;
    });
    
    if (validEdges.length !== edges.length) {
      this.edges.set(stopId, validEdges);
      fixedEdges++;
    }
  }
  
  return { removedEdges, fixedEdges };
}
```

**Гарантии:**
- ✅ Все ключи в edges Map соответствуют узлам в nodes Map
- ✅ Рёбра с несуществующими целевыми узлами удаляются
- ✅ Orphaned ключи удаляются автоматически

### 6. Проверка соответствия в updateFromDataset

**Файл:** `backend/src/application/route-builder/RouteGraph.ts`

**Метод:** `updateFromDataset()`

**Изменения:**
- Валидация ключей перед обновлением
- Валидация ключей после обновления
- Проверка orphaned ключей
- Детальное логирование

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Перед обновлением проверяем и исправляем несоответствия ключей
const validationResult = this.validateEdgesKeys();
if (validationResult.removedEdges > 0 || validationResult.fixedEdges > 0) {
  console.log(`[RouteGraph.updateFromDataset] ✅ Исправлено несоответствий: удалено рёбер=${validationResult.removedEdges}, исправлено записей=${validationResult.fixedEdges}`);
}

// ... обновление графа ...

// КРИТИЧЕСКИ ВАЖНО: После обновления снова проверяем соответствие ключей
const finalValidation = this.validateEdgesKeys();
if (finalValidation.removedEdges > 0 || finalValidation.fixedEdges > 0) {
  console.log(`[RouteGraph.updateFromDataset] ✅ Финальная проверка: удалено рёбер=${finalValidation.removedEdges}, исправлено записей=${finalValidation.fixedEdges}`);
}

// Проверяем, что все ключи в edges Map соответствуют узлам в nodes Map
const edgesKeys = Array.from(this.edges.keys());
const nodesKeys = Array.from(this.nodes.keys());
const orphanedEdgeKeys = edgesKeys.filter(key => !nodesKeys.includes(key));

if (orphanedEdgeKeys.length > 0) {
  console.log(`[RouteGraph.updateFromDataset] ⚠️ ВНИМАНИЕ: Найдено ${orphanedEdgeKeys.length} ключей в edges Map без соответствующих узлов:`, orphanedEdgeKeys.slice(0, 5));
  // Удаляем orphaned ключи
  for (const key of orphanedEdgeKeys) {
    this.edges.delete(key);
  }
}
```

**Гарантии:**
- ✅ Валидация выполняется до и после обновления
- ✅ Orphaned ключи удаляются автоматически
- ✅ Детальное логирование для диагностики

### 7. Улучшенная диагностика в PathFinder

**Файл:** `backend/src/application/route-builder/PathFinder.ts`

**Метод:** `findShortestPath()`

**Изменения:**
- Проверка существования узлов перед поиском пути
- Детальная диагностика, если `edges.get(stopId)` возвращает `undefined`
- Фильтрация рёбер с несуществующими целевыми узлами
- Улучшенное логирование для отладки

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Проверяем, что узлы существуют и имеют рёбра
const fromNode = graph.getNode(fromStopId);
const toNode = graph.getNode(toStopId);

if (!fromNode) {
  console.log(`[PathFinder] ❌ ОШИБКА: Узел fromStopId="${fromStopId}" не найден в графе!`);
  console.log(`[PathFinder] Доступные узлы (первые 10):`, graph.getAllNodes().slice(0, 10).map(n => `${n.stopId} (${n.cityName || n.stopName})`));
  return null;
}

// КРИТИЧЕСКИ ВАЖНО: Получаем все рёбра из текущего узла
// Если edges.get(current) возвращает undefined, это означает несоответствие ключей
const edges = graph.getEdgesFrom(current);

if (edges === undefined || edges.length === 0) {
  const currentNode = graph.getNode(current);
  const nodeName = currentNode ? (currentNode.cityName || currentNode.stopName || current) : current;
  console.log(`[PathFinder] ⚠️ Узел "${nodeName}" (${current}) не имеет исходящих рёбер (edges=${edges === undefined ? 'undefined' : edges.length})`);
  
  // Диагностика: проверяем, существует ли ключ в edges Map
  const allNodes = graph.getAllNodes();
  const nodeExists = allNodes.some(n => n.stopId === current);
  if (nodeExists) {
    console.log(`[PathFinder] Диагностика: узел "${current}" существует в nodes Map, но edges.get("${current}") возвращает ${edges === undefined ? 'undefined' : 'пустой массив'}`);
  } else {
    console.log(`[PathFinder] ❌ КРИТИЧЕСКАЯ ОШИБКА: узел "${current}" не существует в nodes Map!`);
  }
  continue;
}

// Дополнительная проверка: убеждаемся, что все рёбра ссылаются на существующие узлы
const validEdges = edges.filter(edge => {
  const targetNode = graph.getNode(edge.toStopId);
  if (!targetNode) {
    console.log(`[PathFinder] ⚠️ Ребро ссылается на несуществующий узел: ${current} -> ${edge.toStopId}`);
    return false;
  }
  return true;
});
```

**Гарантии:**
- ✅ Детальная диагностика несоответствий ключей
- ✅ Фильтрация невалидных рёбер
- ✅ Улучшенное логирование для отладки

### 8. Улучшенная проверка в RouteGraphBuilder

**Файл:** `backend/src/application/route-builder/RouteGraphBuilder.ts`

**Метод:** `buildFromDataset()`

**Изменения:**
- Строгая проверка существования узлов перед добавлением рёбер
- Пропуск рёбер, если узлы не найдены
- Детальное логирование для диагностики

**Код:**
```typescript
// КРИТИЧЕСКИ ВАЖНО: Проверяем, что узлы существуют в nodes Map перед добавлением ребра
// Это гарантирует, что edges Map использует те же ключи, что и nodes Map
const fromNode = graph.getNode(fromStopId);
const toNode = graph.getNode(toStopId);

if (!fromNode) {
  console.log(`[RouteGraphBuilder] ❌ ОШИБКА: Узел fromStopId="${fromStopId}" не найден в графе при добавлении ребра!`);
  console.log(`[RouteGraphBuilder] Маршрут: "${route.name}" (${route.id})`);
  console.log(`[RouteGraphBuilder] Всего узлов в графе: ${graph.getAllNodes().length}`);
  // Показываем похожие stopId для диагностики
  const allNodes = graph.getAllNodes();
  const similarNodes = allNodes.filter(n => 
    n.stopId.includes(fromStopId.substring(0, 10)) || 
    fromStopId.includes(n.stopId.substring(0, 10))
  );
  if (similarNodes.length > 0) {
    console.log(`[RouteGraphBuilder] Похожие узлы (первые 5):`, similarNodes.slice(0, 5).map(n => `${n.stopId} (${n.cityName || n.stopName})`));
  }
  continue; // Пропускаем это ребро
}

// Оба узла существуют - добавляем ребро
// RouteGraph.addEdge() дополнительно проверит существование узлов
graph.addEdge(edge);
```

**Гарантии:**
- ✅ Рёбра добавляются только для существующих узлов
- ✅ Детальная диагностика при отсутствии узлов
- ✅ Пропуск невалидных рёбер

## Гарантии системы

### ✅ Полная очистка при старте

- Кеш транспортного датасета очищается полностью
- Кеш графа очищается полностью
- Старые виртуальные stopId удаляются

### ✅ Строгая проверка стабильности ID

- Только стабильные virtual-stop- ID используются
- Нестабильные ID отбрасываются
- Один город = один стабильный stopId

### ✅ Соответствие ключей между nodes и edges

- nodes Map и edges Map используют один набор stopId
- Рёбра добавляются только для существующих узлов
- Orphaned ключи удаляются автоматически

### ✅ Валидация на всех этапах

- Валидация при построении графа
- Валидация при обновлении графа
- Валидация перед поиском пути

### ✅ Детальная диагностика

- Логирование всех несоответствий
- Показ похожих узлов для диагностики
- Детальная информация о проблемах

## Проверка работы

### Примеры пар городов для тестирования:

1. **Верхоянск → Жиганск**
2. **Амга → Якутск**
3. **Вилюйск → Якутск**

### Ожидаемые логи:

```
[RouteGraphManager] 🔄 Начало инициализации с полной очисткой...
[RouteGraphManager] Очистка кеша транспортного датасета...
[RouteGraphManager] ✅ Кеш транспортного датасета очищен
[RouteGraphManager] Очистка старого графа...
[RouteGraphManager] 🔄 Полное пересоздание графа из очищенного датасета...
[RouteGraphBuilder] Добавлено узлов в граф: X из Y остановок в датасете
[RouteGraphBuilder] ✅ Добавлено ребро для Верхоянск/Олёкминск/Жиганск: ...
[PathFinder] 🔍 Поиск пути: fromStopId="..." -> toStopId="..." (дата: не указана)
[PathFinder] ✅ Узлы найдены: fromNode="Верхоянск" (...), toNode="Жиганск" (...)
[PathFinder] Рёбер из fromStopId: >=2, из toStopId: >=1
[PathFinder] ✅ Обновлено расстояние до "..." (...): Infinity -> X (через "...")
[PathFinder] Целевой узел найден! Итераций: N, расстояние: M
```

### Критерии успеха:

- ✅ `edges.get(stopId)` НЕ возвращает `undefined`
- ✅ "осталось непосещённых" уменьшается
- ✅ Достижимые узлы > 1
- ✅ Дистанция до целевого ≠ Infinity
- ✅ Путь строится через промежуточные узлы

## Результат

После изменений система гарантирует:

- ✅ **Полная очистка при старте**: Кеш и граф очищаются полностью
- ✅ **Стабильные ID**: Только стабильные virtual-stop- ID используются
- ✅ **Соответствие ключей**: nodes Map и edges Map используют один набор stopId
- ✅ **Валидация на всех этапах**: Проверка соответствия при построении, обновлении и поиске
- ✅ **Детальная диагностика**: Все проблемы логируются для отладки
- ✅ **Автоматическое исправление**: Orphaned ключи удаляются автоматически

## Файлы изменены

1. `backend/src/application/data-loading/TransportDataService.ts`:
   - Добавлена очистка кеша перед загрузкой данных

2. `backend/src/application/route-builder/RouteGraphManager.ts`:
   - Добавлена очистка кеша и старого графа при инициализации
   - Добавлена очистка нестабильных ID из датасета
   - Добавлена очистка маршрутов и рейсов с несуществующими остановками

3. `backend/src/application/route-builder/RouteGraph.ts`:
   - Улучшена проверка существования узлов в `addEdge()`
   - Добавлен метод `validateEdgesKeys()` для проверки соответствия ключей
   - Добавлена валидация в `updateFromDataset()`

4. `backend/src/application/route-builder/RouteGraphBuilder.ts`:
   - Добавлена проверка стабильности ID виртуальных остановок
   - Улучшена проверка существования узлов перед добавлением рёбер
   - Добавлено детальное логирование для диагностики

5. `backend/src/application/route-builder/PathFinder.ts`:
   - Улучшена проверка существования узлов перед поиском пути
   - Добавлена детальная диагностика несоответствий ключей
   - Добавлена фильтрация невалидных рёбер

## Критерий готовности

✅ **PathFinder должен показывать:**
- "рёбер из fromStopId: >=2"
- "осталось непосещённых" должно уменьшаться
- Достижимые узлы > 1
- Дистанция до целевого ≠ Infinity
- Путь строится через промежуточные узлы

✅ **Больше никогда не должно быть ошибок вида "осталось непосещённых: 36"**

✅ **Поиск работает с датой и без даты для всех пар виртуальных городов**


